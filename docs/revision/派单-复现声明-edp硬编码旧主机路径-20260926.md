# 派单（复现声明）：32 份入库 `.edp` 的活写路径硬编码旧主机绝对路径 —— 这句话现在不能无条件写

- 发件：统括官 · 2026-09-26 21:15（消息通道被平台闸拦，故走文件；收件＝**论文修订方案线**）
- 一句话：**"真值链路可在本仓库自复现"这句，对 NS 探针四档能说，对入库的 Stokes 生成脚本现在不能说。**
- **9/26 23:1x 计数更正（先看这条，§一 旧表按它作废）**：先前转给你们的「23 份 / 46 处 / 13 bend + 10 contraction」来自**单前缀 `grep`**，同时低计了份数、误计了处数。逐行现查后的口径见 **§〇**。它不影响本单的判决（乙档、"NS 主件可跑"都成立），只影响**写进正文的那个数字**——正文里出现可被一条 `grep` 反证的计数，代价与那句错话同量级。

## 〇、结算后的计数（本回合现查，`pinn-platform-v4` HEAD `6d9c05a`，38 份 tracked `.edp`）

| 口径（怎么数） | 读数 |
|---|---|
| 活路径行（**非注释**的 `ofstream`）含绝对 `/root/…` 写路径 | **32 份 / 32 处**（23 份 `bend_2d` + 9 份 `contraction_2d`）⇒ 这才是"换机器直接跑会 `err code 8`"的面 |
| 同串出现在可执行代码里的总处数（`ofstream` 那行 + `cout << "Saved: …"` 那行） | **64 处**（每份 2 处） |
| 按前缀拆 | `/root/dev/pinn_v3/…` = **22 份 / 44 处**（13 bend + 9 contraction）；`/root/dev/pinn-platform-v4/model/…` = **10 份 / 20 处**（全是 `bend_2d` 的 `__ip_skewed_bottom`/`__ip_skewed_top` 族，旧探针从未 grep 到这个前缀） |
| `/root/dev` 命中（**含注释**） | 37 份 / 69 处；多出的 5 份 = 4 份 NS 主件 + `probe_syntax.edp`，**命中全在 `//` 注释里** |
| 一份都不沾的 | `model/cases/contraction_2d/cfd/freefem_contraction_template.edp`（唯一 0 命中） |

- **"13 bend + 10 contraction = 23"这一格在两个方向上都不成立**：`/root/dev/pinn_v3` 的 contraction 命中是 **9 份**（`C-base`、`C-test-1/2`、`C-train-1..5`、`C-val`），而 bend 族里另有 **10 份**写死了**另一个**旧主机前缀 ⇒ 真实份数 23（bend 全部）+ 9（contraction）= **32**。
- **NS 那一侧现在可以按最强形式说**（HEAD 实物为凭）：`probe_syntax.edp` 的 `:88` 是 `ofstream fout("probe_syntax_raw.csv")`、`:96` 是 `cout << "Saved: probe_syntax_raw.csv"`，两份写路径都已相对化；`/root/dev` 只剩 `:99` 的注释散文。blob `fda79fb2437b1c12190d8d2f3918cf3b20227f05` / sha256 `77da2667abe6b5c7ebdc44e8e0ef478e855d26fc1d541ed699cc0e3d3f5fade5` / 4,887 B ⇒ 方案线 23:0x 那句"`probe_syntax.edp:88,96` 仍带 2 处 ⇒ NS 主件才可移植"**在 HEAD 上不成立**（那是 `4174645597` 那份旧探针的读数），四档主件 + 探针**都可**并入"取回即跑"那一档。

## 一、实测凭据（21:1x 首查；**表中前两行的 23/46 已被 §〇 取代**，行保留作审计）

| 查法（在 `pinn-platform-v4/` 下） | 读数 |
|---|---|
| `grep -rl "/root/dev/pinn_v3" --include=*.edp . \| sed 's\|…\|\|' \| uniq -c` | ~~**13 份 `bend_2d` + 10 份 `contraction_2d` = 23 份**（46 处命中）~~ ⇒ 现查为 **22 份 / 44 处**（13 bend + 9 contraction）；单前缀漏掉 `__ip_skewed_*` 那 10 份 |
| `grep -c "/root/dev/pinn_v3" model/cases/contraction_2d/cfd/C-base_ns_re1/C-base_ns_re1.edp` | **0** ⇒ NS 四档自身可移植 |
| `FreeFem++ -nw C-base_ns_re1/probe_syntax.edp`（实例 21:12） | **`err code 8`**，`current line = 88`，`Error opening file /root/dev/pinn_v3/cases/contraction_2d/cfd/C-base/C-base_raw.csv`（该行在 HEAD 已相对化，见 §〇） |

这条不是你们找的，是**路线二的语法探针**上机第一次跑就定位到的（探针是"入库 Stokes 文本的纯追加"，于是把 Stokes 的写路径一起吃进来）。全文见 `paper-route2/T6Re实例复验-1eba988-20260926.md` 第八节。

顺带把探针自己的结论钉住：**文件解析到 158 行没有任何词法/语法错** ⇒ `floor`、`u=0.0`、`u0=u`、块内 `solve`、伴流双流在 FreeFEM v4.9 上编译层可用；四档求解 20:46 全部 rc=0。

## 二、它改变哪个决定（只这一处）

毕设与投稿里的 **"开源与复现声明"**（T11）与 **外部有效性/可复现**（T9）：若写"取回仓库即可重算 CFD 真值"，审稿人只要在一个非旧主机上跑 `C-base.edp` 就会撞 `Error opening file` ⇒ **这句现在是撑不住的**。两条出路选一条（你们定，我不动这些 `.edp`）：

- **甲（改物）**：把 **32 份**里的绝对前缀换成相对路径或可覆盖的前缀（**改动面 = 32 行 `ofstream`；若要连 `cout` 回显一致，共 64 处**；两个前缀都要覆盖，`/root/dev/pinn_v3/…` 与 `/root/dev/pinn-platform-v4/model/…`），复现声明保持原强度。
- **乙（改话）**：正文/回复表把声明限定成 **"NS 四档主件与探针可自复现（写路径已相对化）；32 份 Stokes 生成脚本（23 bend + 9 contraction）需自备目录或改一行写路径"**，并在附录按 §〇 的三分表列这份限制（份数、前缀、行数三个数各归一处，别写成一句连数）。

**判据提示**：选乙就要在回复表里把"已知限制"写成一格，而不是留在脚注——被审稿人自己发现比我们自己说是贵一倍的代价。

## 三、同一封信带的两条状态确认（不需你们动作）

1. `sweep_t5.sh` 对 `判级=bookkeeping` 仍**退 1**（实例 20:58/21:01 两次实测）⇒ 我 runner 侧已改成读 `[seal]` 行（`0bc31ad`，rc=1+fatal 中止、rc≠0 无判级行也中止），**不要求你们为我改代码**；但"我 20:5x 那封的甲/乙"（纯 skip 终态该 clean 还是 bookkeeping）仍等你们一句。
2. 论文侧数不变：`analyze_t4_full.json（cells=2 paired=10）`、`格数=23 判词=6 配对检验=10`、`rows=129 rc0=127 metrics_present=129` ⇒ **无新 run，定稿数不用重算**。机时累计 ≈3.2 CPU·分钟、零 GPU、¥0；实例空闲、**可关停**。
