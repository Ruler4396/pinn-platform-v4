# CFD 真值同机复算 + 单机单价（2026-09-25 20:0x 直测）

- 执行：统括官会话 `685fbc75`（环境验证性质，未改仓内任何数据文件；产物写在 `route2_out/ffrepro*`，与 T5 在跑的 `results/` 隔离）
- 机器：魔搭 DSW CPU 档 `dsw-2210488`，**8 核配额 / 28 GiB**，`FreeFem++ v4.9`（Ubuntu jammy/universe `4.9+dfsg1-2build1`，本日 19:48 由 apt 装入）
- 被复算对象：**仓库里已入库的 `.edp` 与已入库的 `*_raw.csv`**（即论文真值的原始生成脚本与原始产物），不是重新建模
- 判据：`tr -d '\r'` 去掉换行差异后 `diff` 的**输出行数 = 0** 才算"逐位复算成功"；同时比行数

## 一、读数（全部直测，计时用 `date +%s%N`，本机无 `/usr/bin/time`）

| 工况 | 家族 | rc | 墙钟 | 入库行数 | 复算行数 | diff 行数 |
|---|---|---|---|---|---|---|
| C-base | contraction | 0 | **395 ms** | 2114 | 2114 | **0** |
| C-train-1 | contraction | 0 | **440 ms** | 2340 | 2340 | **0** |
| C-test-2 | contraction | 0 | **326 ms** | 1546 | 1546 | **0** |
| B-base | bend | 0 | **2417 ms** | 10494 | 10494 | **0** |
| B-train-1 | bend | 0 | **2478 ms** | 10494 | 10494 | **0** |

**判决：5/5 工况逐位复算一致。** 换主机、换 Python、换 FreeFEM 装法之后，论文的真值链路在"真值"这一环是**可复算的**。

## 二、5.7 表现在有同机单价（这是它唯一缺的那一列）

| 口径 | 收缩族 | 弯曲族 | 来源 |
|---|---|---|---|
| 论文 5.7 原写 | 0.481 s/工况 | 3.552 s/工况 | 旧机器，无同框凭据（审计 A42 标为 **E4 争议**） |
| **本回合同机实测** | **0.33–0.44 s/工况**（3 工况） | **2.42–2.48 s/工况**（2 工况） | 本表，`dsw-2210488` + FreeFem++ v4.9 |
| 训练侧同机实测（T5） | 稀疏 52.6–59.0 s、稠密 81.4 s | 稀疏 107.8–113.5 s、稠密 174.6 s | `docs/revision/T5矩阵配对读数-20260925.md` |

派生（**这是同一台机器上同时测两侧，才算同框**）：
- 收缩族单次训练 / 单次 CFD ≈ **53 s / 0.40 s ≈ 130×**；弯曲族 ≈ **110 s / 2.45 s ≈ 45×**。
- 与论文"快几十倍到几百倍"的表述**方向一致但量级需重写**：这个比值说的是"生成一份真值 vs 训练一个网络"，**不是**"推理时 PINN 比 CFD 快"。摊销后（同一模型跑多工况）谁占优取决于工况数 K，必须写成摊销式而不是一个倍数。
- 5.7 表若要留，须加表注：CFD 单价与训练单价**同机同次测得**，计时口径 `date +%s%N`，容器 CPU 配额 8 核，且 FreeFEM 版本已固定为 v4.9。

## 三、当场发现的缺陷：入库的 `.edp` 不能在任何别的机器上原样跑

- **33 个 `.edp` 里有 32 个把原作者机器的绝对路径写死在输出语句里**（`ofstream fout("/root/dev/pinn_v3/cases/...")` 与 `/root/dev/pinn-platform-v4/model/cases/...` 两种前缀）。
- 后果：换机器直接跑 ⇒ `Error opening file` ⇒ **rc=8**（我第一次尝试就是这样失败的，属设备错不是产品错，但审稿人不会知道差别）。
- 修法（已验证）：一条 sed 即可让任意机器可跑，不必改生成器——
  ```bash
  W=$PWD/route2_out/gen; mkdir -p $W/gen/$(dirname 相对路径)
  sed -E 's#/root/dev/[a-z0-9._-]*/cases#'"$W"'/gen#g' X_stokes.edp > X_run.edp
  FreeFem++ -nw X_run.edp
  ```
  注意：**目标目录必须先 `mkdir -p`**，FreeFEM 不会自己建目录，且它建不出目录时报的是 rc=8 而不是写权限错。
- 生成器本身没这个毛病（`model/src/data/contraction_freefem.py:114`、`bend_freefem.py:165` 用的是 `{raw_csv_path}` 模板变量），**问题只在入库的 `.edp` 快照**。所以正文里"可复现"要区分两件事：① 用仓库生成器重跑 = 可复现；② 用入库 `.edp` 直接跑 = 需要一处路径改写。
- 另一处：**入库 `.edp` 与 `*_raw.csv` 都是 CRLF**（Windows 工作树产物）。直接 `cmp` 会报差异，比对前必须 `tr -d '\r'`。

## 四、这条读数解锁/收紧了哪些声称

| 任务 | 影响 |
|---|---|
| **T9 外部有效性** | "第二实现交叉验证"仍**未做**（我用的是同一个求解器族，只是换主机+版本未记录于原始生成环境）。**不得写成"独立实现验证"**。可写的最强句：存在公开可得的 FreeFEM 4.9，能在另一台主机上逐位复算出论文全部抽检真值（5/5 工况、0 行差异） |
| **T6（Re 扫真值）** | 阻塞解除：同一条 sed + 同一个 `.edp` 模板即可加对流项出 Re=1/10/50。**机时按本表单价外推：收缩族 0.4 s/工况、弯曲族 2.45 s/工况** ⇒ 一整套 Re 扫（3 Re × 9 工况）是**秒级**，不是小时级 |
| **5.7 表** | 缺的那一手数已补（本表第二节）。审计对 A42 的 E4 争议可就此收口，但要按第二节改写倍数叙述 |
| **路线二 S1** | 工具可用且已验证；**同一颗坑（写死绝对路径 + 不建目录）必须在 `generate_t_case.py` 里避免**——它的 `.edp` 输出路径应由 `--out-root` 传入并自建目录 |

## 五、我做了什么、没做什么（边界声明）

- 做了：apt 安装 FreeFem++；把 5 个入库 `.edp` 复制到工作区外目录、改路径、跑、比对；T5 账本的只读汇总与配对差；把 7 个 route2 脚本按 commit `d882668` 投递到实例并逐字节核对 blob 哈希。
- 没做：**没有覆盖任何入库数据文件**（比对用的是 `route2_out/` 下的副本）；没有跑任何训练；没有改路线二的脚本；没有在 T5 跑完前 `git pull`（`model/` 本来就不是 git 仓，投递走 raw.githubusercontent）。
- 待办归属：S1 的真跑等路线二回一个钉住的 sha（它有 4 个脚本在途未提交）；T6 与 5.7 表的正文改法归方案线。
