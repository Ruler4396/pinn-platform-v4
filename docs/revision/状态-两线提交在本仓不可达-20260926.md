# 状态：两条任务线 22:0x 之后的提交在本仓完全不可达（对象层核过）+ 我要的两件文本

- 记于 2026-09-26 22:1x，统括官。为什么单独一篇：这已经不是"我没拉到"，而是**可交付物不在仓里**，距 9/29 冻结不足 26 小时。

## 一、实测（每条都是我这一回合现跑的命令）

| 检查 | 读数 |
|---|---|
| `git ls-remote origin` | 只有 `refs/heads/main = a532f17`（= 我那枚创新点二裁决） |
| `git ls-remote revision` | 只有 `refs/heads/main = fd2d90d`（= 方案线 21:50 那枚，之后没再动过） |
| `git cat-file -t 001d167 / 5f25d46 / d90b311 / 1e70c6a / d07a3f7` | **全部 `Not a valid object name`** |
| `git fsck --unreachable` 里这些前缀 | **0 个** ⇒ 不是"提交后被丢弃"，是**从未在这个对象库生成过** |
| `git log --all --diff-filter=A -- '*pod_baseline_contraction.json'` | **空** ⇒ 臂 C 的 JSON 从未作为对象进过本仓 |
| `ls model/results/.artifacts/` | 目录不存在 |
| `grep -c final_val docs/revision/正文改写工单-20260925.md` | **0**（该规矩只在 `SWEEP_DRYRUN.md` 有 1 处） |
| `grep -c "0.088086\|0.113265\|0.032633" docs/revision/NS真值代价表-20260928.md` | **0** |

⇒ **结论分三层**：① 两线 22:0x 之后报的 commit **不在这条我与实例共用的仓里**（他们的对象库里可能有）；② 由此"**臂 C 已入库、强度升 A**"这句**现在不能进正文**；③ 表 5-8 的两格 test 数目前只存在于对话文本 = **转引·B**，因为我的实例通道全 404、无法独立复算。

## 一B、22:2x 三方独立核：排除"是我这边缓存骗我"，锁定"两个 git 端点"

| 手段（绕开我的 remote 配置） | 读数 |
|---|---|
| `git ls-remote https://github.com/Ruler4396/pinn-platform-v4.git refs/heads/main` | **`a532f17`**（我那枚 B 裁决） |
| `gh api repos/Ruler4396/pinn-platform-v4/commits/main` | **`a532f17`**，标题逐字是"裁决：创新点二定档 B…" |
| `gh api …/contents/model/results/.artifacts/armC-repro-20260926/pod_baseline_contraction.json?ref=main` | **404 Not Found** |

⇒ GitHub 自己的 REST 层与直连 git 协议同答案 ⇒ 他们的 `origin` **不是这条仓**（能解释"tip=920b41a"与"ae2c3e8 是你推的"同时为真：两条端点各有历史）。
**定位只需两行，请两条线各跑一次**：`git remote -v` + `gh api repos/<那个 owner>/<那个 repo>/commits/main`。**不要只 `git ls-remote origin`** —— 那只证明"他们自己那把 URL 上有"，正是今晚全部"我已 push"的来源。
**我的措辞更正（一处）**：先前写"一律不采信"过头；准确说法是**不在这条共用仓里**，不否认它存在于他们那侧。**实例只从 `Ruler4396/pinn-platform-v4` 按 blob 哈希取件** ⇒ 一切"可复现凭据"必须落这条端点才算数。

## 二、我要的两件文本（贴回来就行，我落盘并推）

1. **臂 C 重跑产物的 638 B JSON 原样文本**（不是描述、不是 sha）⇒ 我写进 `model/results/.artifacts/armC-repro-20260926/pod_baseline_contraction.json`。若这份 JSON 实际躺在实例 NAS 上，请给路径：那措辞是"**在实例上、不在仓里**"，不是"已归档"。
2. **表 5-8 两格的逐工况原始读数**：`C-test-1` 五粒、`C-test-3` 五粒各自的 `rel_l2_speed`（+ 来源文件路径），我从头现算 mean±sd 与配对差，再写进表。

## 三、不用他们再做的（我已自己补）

两把 val 尺的命名规矩、"索引只覆盖账本 run"的用法句、以及"`final_val` 在论文侧 0 命中 ⇒ 没有任何声称挂在这把尺上"这条答复 —— 已随本篇一起补进工单 §9e（避让他们的编号，不改他们那几段）。

## 四、给用户的版本（这条只有你能做）

两条任务线看起来都在各自的会话里"提交并 push 成功"，但**推不到我能看到的两个 remote 上**。请在它们界面里各跑一次 `git rev-parse --show-toplevel` + `git log -1 --format=%H` + `git remote -v`，把原样输出贴给我或它们自己看；若 top-level 不是 `D:/PINN-restart/pinn-platform-v4`，那就解释了今晚全部"幽灵 commit"。

## 五、22:3x 两条裁决（消息通道被平台闸拦，本篇即交付；两条线都读这篇）

1. **创新点二 = 定档 B**。四句可粘文本见 `裁决-创新点二定档B-20260926.md`。方案线 22:2x 提的三处小修订（物理项在 5% 档有益处、可审计性=同框可重算、"基线更优正说明同框起作用"）**全部采纳**——它们与 B 不冲突，(c) 就是 B 该有的写法。**硬要求**：限定句"本文不提供精度优势"必须与贡献句**同段**，不进脚注，也不许写成"待更好基线补测"。
2. **臂 C 不等实例通道，也不写"已入库"**。表注固定措辞：**"数值取自运行日志逐工况打印行（已入库：`证据-armC日志摘录-20260926.md`）；JSON 产物在实例 NAS、未入本仓"**。谁能贴来 JSON 原样文本（638 B 或 1,113 B）我即刻写进 `model/results/.artifacts/armC-repro-20260926/`；否则等通道恢复由我重跑一次（~6 s）。**它不阻塞冻结，也不支撑任何精度结论**（只支撑"速度结构需要学习"）。
3. 端点规矩（今晚所有"我已 push"的根因）：**唯一权威 = `github.com/Ruler4396/pinn-platform-v4`**；实例 runner 只按 `raw.githubusercontent.com/Ruler4396/pinn-platform-v4/<sha>` 逐件取 blob。别的 remote 上的 commit 对实例等于不存在。定位两行：`git remote -v` + `gh api repos/<owner>/<repo>/commits/main`。

## 六、22:5x 穷尽核（把"阴性"的扫描范围钉住）+ 三条待你们读的答复

**扫描范围**：本账号下 5 条 PINN 相关仓的全部 ref + 直接按 sha 问 API：

| 仓 | refs | tip |
|---|---|---|
| `pinn-platform-v4` | `refs/heads/main` | **`54dc787`**（我的，含 B 裁决/臂C 证据/坐标表） |
| `pinn-thesis-revision` | `refs/heads/main` | `fd2d90d`（路线二 21:50 那枚，之后没动） |
| `pinn-analysis-workspace` | `refs/heads/master` | `e0553ff` |
| `pinn-reliability-paper` | `refs/heads/master` | `e84d1af` |
| `microfluidic-pinn-archive` | `refs/heads/main` | `e2a9956` |

`gh api repos/Ruler4396/pinn-platform-v4/commits/<sha>` 对 `3e595c8`/`1f82565`/`78d3254`/`920b41a` 全部 **No commit found**；仓内 grep `两把 val`/`不提供精度优势`/`0.088086`/`final_val`(除我 §9e 那两处) 均 **0**。⇒ **结论（带范围）**：这四枚不在本账号任何一条仓的任何一条分支上；"我 ls-remote 与 GitHub API 两边一致"这句在你们那侧也许真，但它证的仍是**另一把端点**（请打印 `git remote get-url origin` 原文，而不是仓库名的简称）。

**三条答复（都在这篇里，不用等消息通道）**：
1. **臂C / 解析对手的 JSON：贴文本给我**——回件里原样贴 1,113 B 与 638 B + 516 B，我写进 `model/results/.artifacts/armC-repro-20260926/`、`.../analytic-poiseuille-baseline/` 并推。
2. **那一臂登记成什么**：既不写"未做"也不写"已入库"，写 **"已做（实例侧，产物在 NAS、未入本仓；重跑 ~6 s 可入库）"**。理由：写"未做"会被读成可以不做；写"已入库"会被下一个读者推到。**这条是裁决，不必再议。**
3. **表 5-8 那三行"待入库"**：保留标"待入库"是对的，但必须**同时写来源与复算路径**（`out/ledger/step_analytic_real.txt` + `analyze_sweep` 的 `analytic` 根），否则读者会以为那是漏跑的实验而不是漏归档的文件。

## 七、23:1x 对延迟回执的统一答复（两条线都会反复问的六件，答完这篇就以此为准）

1. **`51acf7c` 那批我核到在仓**：工单里臂 B 单价 `199.3`（稠密中位）/`101.5`（分层5%）**3 处/2 处**命中、"臂 B 的账本行"规则在位、`SWEEP_DRYRUN` §11.5 的代码层归因（`_run_metrics_json`、`best_val_total`）**3 处**命中 ⇒ 三臂机时排序 **双模型 81.4 < 臂A 139.6 < 臂B 199.3（稠密）**、"去掉物理项省机时"在本实例不成立 —— **接受，并按你的规矩只引区间、不拿臂 B 算精确 K\***。
2. **旧索引哈希 `c80d6f55…` 不必再改**：三处命中全部自带取代说明（`T5矩阵配对读数-20260925.md:85`、`T5矩阵test口径读数-20260926.md:121`、工单 §12 第 8 条），现算值 `e9e4797e…`/129 条已在同段并注 ⇒ "请换掉"这条已经满足，别再为它开一轮。
3. **臂 B 的 `_run_metrics_json` 双 schema 改造：确认推后**（你给的理由成立——它属读数口径变更，且 seg14 当时在现场复验）。**登记成"未做+原因"，别留在"待办"语态**：`deferral 已在 §11.5，正文/回复表不引用臂 B 账本精度 ⇒ 不影响任何已定数`。
4. **端点规矩（今晚最大的隐性成本）**：唯一权威 = `Ruler4396/pinn-platform-v4`（tip 见本篇上方，实例 runner 按 blob 哈希从它取件）。你的 `merge-base`/`ls-remote` 若与 `gh api repos/Ruler4396/pinn-platform-v4/commits/main` 不一致，**先修 remote 再报"已 push"**；已到这条端点的成果我这边一律认（`51acf7c`、`3528f5e`、`523f409`、`fd2d90d`、`a5f23e6`、`fe0b074` 都在）。
5. **还欠的两件文本**（贴回来我落盘，别再 commit）：臂 C `1,113 B` 与解析对手 `638 B + 516 B` 的 JSON 原样内容 ⇒ 写进 `model/results/.artifacts/armC-repro-20260926/` 与 `.../analytic-poiseuille-baseline/`；在此之前表 5-8 三行、表 5-10、D2 表注一律写**"已做（实例侧，产物在 NAS、未入本仓）"**，不写"未做"也不写"已归档"。
6. **距 9/29 冻结最近的可动手项只有一个**：T12 正文 B1/C3/D1/表 5-9 的填空，数全在 `T5矩阵test口径读数`、`三臂test口径坐标表`、`裁决-创新点二定档B` 三篇里，**不需要等任何人**（G1 的 10.4 两行是例外，那需要你人工取原文，取不到就删行——你已登记）。

## 八、23:3x 定性收口：这条仓**从未收到你们任何一次 push**（GitHub 事件流为证）+ 新协议

`gh api repos/Ruler4396/pinn-platform-v4/events` 里最近的 **12 条 PushEvent 的 head sha 全部是统括官推的**（`0bc31ad → e3c418a → 504ab78 → f950a18 → 523f409 → a5f23e6 → 8614f50 → ae2c3e8 → e29e7b9 → a532f17 → 54dc787 → 61a549f`）；你们报的 `78d3254`/`920b41a`/`1f82565`/`3e595c8`/`d07a3f7`/`d90b311`/`e9d37c6`/`402f3c0` **一次都没作为 push head 出现过**。⇒ 今晚所有到达这条仓的你们的成果，都是**我 `git push` 推上去的**（因为你们的写盘偶尔会落进 `D:\PINN-restart\pinn-platform-v4` 工作树）；你们自己那句"`git ls-remote` 与 GitHub API 两边一致"也大概是真的——只是那两边指的是**你们 authenticated 的另一把仓**。事件流不可缓存、不可被我的本地 ref 骗，所以这条定性可以定死。

**新协议（即刻生效，省掉后面所有回合）**：
1. **两条线一律不再 push、也不再报 commit sha**。要做的是：**把文件写进 `D:\PINN-restart\pinn-platform-v4`，然后回件报"改了哪个文件 + 该文件的 grep 特征串"**。
2. 我这一侧负责：`git add <指定文件>` + commit + push，并在推完把 tip sha 回贴给你们；你们要引用坐标时**只引我给的 tip**。
3. 验收口径（我用来核你们"落没落"）：`gh api repos/Ruler4396/pinn-platform-v4/events` 的 push head + 仓内 grep 特征串。你们那侧的 `ls-remote origin` 与 `git log -1` 从今晚起**不作为凭据**。
4. 已确认在仓、不必重做的：臂 B 单价与 §11.5 归因（`51acf7c`）、封段三态（`504ab78`/`a5f23e6`）、创新点二 B 裁决、臂 C 日志摘录、三臂坐标表、§9e 命名规矩、状态篇一~八。仍在仓外的：**臂 C `1,113 B` 与解析对手两份 JSON 的正文**、创新点二改写的正文句子、§六 两把 val 尺的工单行（`grep "两把 val" docs/revision/正文改写工单-20260925.md` 仍 0）⇒ **贴文本或写进工作树后报我特征串**。

## 九、23:4x 最后两条裁决（0-点线 / 产物归属措辞）——今晚到此为止，下一条该出现的只剩文献线

1. **G0 转角重跑（0-点线，约 25 min）：不跑。** 理由三条，都可在回复表里直接引用：① 定档 B 之后**本文不主张精度优势**，那四行倍数只支撑"外推行为可测"，不支撑任何比较级声称 ⇒ 从 n=1 升到 n=5 **不改变任何句子的强度**；② 它会给冻结前 0:30–1:00 的窗口塞进一批新数，逼着再改一次表 5-7b 与 G4 的批次列（这正是我们今晚反复在修的"同一量两个定义"）；③ 成本不是钱而是**注意力**。**处置**：表 5-7b 保持 `n=1` 标注 + 局限性里写"转角外推只在旧批次单ckpt上测过；同批次重跑约 25 min CPU，留作投稿期补做"——**登记为未做+原因，不留待办语态**。
2. **臂 C / 解析对手的产物归属措辞（先更正我自己一句话）**：我上面写"你们 920b41a/1f82565 已把两份 JSON 写进仓，**我核过文件在位**"——**那半句不成立，我撤回**：本端点实测 `git ls-files "model/results/.artifacts/*"` 与目录枚举都是**空**，tip 仍是统括官推的 `56fb8af`。所以措辞回到 **"已做（产物在实例 NAS / 你们那侧仓，未入本端点仓）"**；他们若把文件写进 `D:\PINN-restart\pinn-platform-v4` 工作树并报特征串，我落盘后这句才升级为 **"已入库（fe0b074 重跑副本，与运行日志逐位一致）；原始 09:04 产物在实例 NAS"**。**规则重申：引用"在不在仓"只认 `git ls-files` / `os.listdir` 这一类本端点枚举，不认对方的字节级自述（哪怕它带了合法 JSON 结构与逐位一致的 md5）。**
3. **一条真属于你们的加分**：方案线 00:4x 指出我用 `grep -c "两把 val 尺"` 判"未落盘"可能是**我自己的命令把中文串弄坏了**——这条批评成立的可能性不低（今晚已三次遇到 bash 处理中文/反引号出错）。⇒ 从今天起：**判断"某段中文在不在文件里"一律用 `python3` 读文件数 `count()`，不用 bash grep**；本文件与工单里的中文特征串我已改用 Python 复核（结果见上）。
3. 我这边收口：runner 全部在现役 pin（T4=`504ab78`/NS=`fe0b074`），实例通道仍 404（不阻塞，你们 SSH 可用）；**明早我按 07:25/08:05 两拍自查冻结进度**，无回应即代派。剩下的外部依赖只有一件：文献线的 [15]/[22] 与 10.4 那 21 个 `#### 1 0.1 0.9`——取不到就删行（方案线已定）。

## 十、今夜收口（01:0x）：口径全冻、无人欠判定，明早三拍

- **已冻且在本端点可核**：定档甲（表 5-10 同源样本）、定档乙（T11 复现声明限定 + 已知限制成格）、**创新点二 = B**（不主张精度优势）、三臂 + 解析对手的 test 口径与坐标表、两把 val 尺命名规矩（§9e）、臂 C 日志摘录、K0/T6-Re 两篇实例读数、runner 全在现役 pin（T4=`504ab78`、NS=`fe0b074`）。
- **不在本端点、等原文落盘的只有两件**：臂 C `1,113 B` 与解析对手 `638 B + 516 B` 的 JSON 正文（方案线 00:5x 自查已确认"仓里没有"，措辞按**"已做（产物在 NAS/你们那侧），未入本端点仓"**）；以及 §六 那条"两把尺"的完整措辞若他们 Python 复核为"在位"则以他们版本为准。
- **一处我自己的账**：`model/results/.artifacts/*/*.json` 在本端点 `git ls-tree` / `glob` 双重为空 ⇒ 我先前那句"我核过文件在位"**不成立，已撤回**（见第九节）。引用"在不在仓"只认本端点的 `ls-tree`/`listdir`，不认任何人的字节级自述——包括我自己的记忆。
- **明早三拍（统括官主动）**：07:25 查 T12 落盘进度与 §10 自检表是否清空；08:05 查 §14 的 JSON 文本是否已贴；09:00 若无回应，按本篇口径代派并把结果写回本篇。**唯一外部依赖**：任务 #19（三个 worker 会话的 `git remote get-url origin` 需用户核一次）。

## 十七、02:3x 端点最终诊断（今夜定案，明早不再重查）

**三种"落盘"的可信度，实测排序**：
1. ✅ **消息正文里贴出的文本** → 我 `cat`/`Write` 落盘 = 唯一可靠链路（今晚 §9e、臂C 摘录、坐标表、三篇裁决全走这条）。
2. ⚠️ **worker 的 bash 写（`cat >>`、heredoc、`git add`）** → 会被工具层拦成"看似执行"（方案线 02:1x 实测：`cat >> f` 后 `wc -c` 不增、`git hash-object` 报 `No such file or directory`）。
3. ❌ **worker 的 `git commit`/`push`** → 它们的本地对象库自洽（`cat-file -t` 能认），但 `gh api …/events` 的 push head 全是统括官的 ⇒ **GitHub 上没有**。

⇒ **本协议即刻生效**：三条线**不再自己写仓、不再自己 commit**；要落的改动**一律把正文贴进回件**（可以长），由我落盘并推；你们引用坐标时**只问我一句 tip**，别写自己算出的 sha。**唯一权威 = `github.com/Ruler4396/pinn-platform-v4`**（实例 runner 只按它的 blob 取件）。

## 十八、明早三拍（我主动执行，无需你们回应）
- **07:25** 查 T12：B1/C3/D1/表 5-9 是否落盘、§10 自检表是否清空；G1 文献整合（[15] 年标/页码、[22] 作者名单与卷期、三件状态改判）若仍缺，我把已贴在回件里的文本落进去。
- **08:05** 查两条上机项：① T6-Re 代价表"实测重跑确认"5 s（通道没恢复则让路线二 curl 自跑并标"路线二自跑"）；② 臂C/解析对手两份 JSON 原文是否已到（未到则表注继续按"未入本端点仓"写，**不升级**）。
- **09:00** 无进展即按已冻口径代派并把结果写回本篇；**09:30 起停止新增实验性改动**，只允许文本收敛——距 9/29 冻结还有约 21 小时，够写不够再测。
