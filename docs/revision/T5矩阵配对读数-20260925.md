# T5 矩阵进度与配对读数（2026-09-25 19:47 直测）

- 数据来源：`/mnt/workspace/pinn-repro-2026/out/progress.jsonl`（本回合经 Contents API 读回并**在页面内现算**，脚本只做求和/配对差，不重新训练、不改判据）
- 实例：魔搭 DSW CPU 档 `dsw-2210488`；在跑进程 PID 84841，19:45 时刻正在 `t5c18 s43`
- 口径：`phase=="train" && rc==0`，共 **85 条**；观测种子 `obs_seed=0` 全矩阵固定 ⇒ **跨格配对只变动训练种子**，配对是干净的
- 主指标：`rel_l2_speed`。注意既有口径陷阱——**表5-3/5-5/5-6 用的是 `rel_l2_u`**，表5-4/5-7 弯道行用 `rel_l2_speed`（收缩族两者恰好相等，弯曲族差约 1.7×）
- 证据等级：本文全部数字为**直测**；判读句标注为"派生"

## 一、矩阵已完成度与单价（写进三列时间表的原料）

| 格 | 内容 | n(seed) | rel_l2_speed mean±sd | 相对 sd | 中位训练墙钟 |
|---|---|---|---|---|---|
| 1 | 稠密主线（表5-2/5-3/5-7 C行/5-8启用行） | 5 | 0.02362 ± 0.00308 | 13.0% | 81.4 s |
| 2 | 稠密·阶段内 PDE 关（**表5-8"不启用"行**） | 5 | 0.02364 ± 0.00310 | 13.1% | 81.8 s |
| 3 | 分层 1%（表5-1/5-4） | 5 | 0.03246 ± 0.00405 | 12.5% | 58.9 s |
| 4 | 分层 5%（表5-5 分层臂/表5-6 无噪臂/图5-18） | 5 | 0.02551 ± 0.00425 | 16.7% | 53.7 s |
| 5 | 分层 10% | 5 | 0.02212 ± 0.00358 | 16.2% | 53.6 s |
| 6 | 分层 15% | 5 | 0.02299 ± 0.00307 | 13.4% | 55.0 s |
| 7 | 均匀 1% | 5 | 0.04480 ± 0.01077 | 24.0% | 52.6 s |
| 8 | 均匀 5%（表5-5 均匀臂） | 5 | 0.02577 ± 0.00171 | 6.6% | 53.5 s |
| 9 | 均匀 10% | 5 | 0.02531 ± 0.00189 | 7.5% | 54.6 s |
| 10 | 均匀 15% | 5 | 0.02752 ± 0.00249 | 9.1% | 55.1 s |
| 11 | 分层 5% + 噪声 1% | 5 | 0.02469 ± 0.00411 | 16.6% | 53.9 s |
| 12 | 分层 5% + 噪声 5% | 5 | 0.02653 ± 0.00445 | 16.8% | 53.4 s |
| 13 | **basic 4 特征（只能 soft）** | 5 | **0.51955 ± 0.01322** | 2.5% | 52.7 s |
| 14 | geometry 14 特征 + soft（与格13 只差特征集） | 5 | **0.13622 ± 0.01143** | 8.4% | 53.0 s |
| 15 | 弯曲 分层 1% | 5 | 0.02646 ± 0.00222 | 8.4% | 107.8 s |
| 16 | 弯曲 分层 5% | 5 | 0.02508 ± 0.00227 | 9.0% | 110.2 s |
| 17 | 弯曲 分层 10% | 5 | 0.02502 ± 0.00157 | 6.3% | 113.5 s |
| 18 | 弯曲 分层 15% | 5 | 0.02415 ± 0.00163 | 6.7% | 116.1 s |
| 19 | 弯曲 稠密对照 | 5 | 0.02435 ± 0.00196 | 8.0% | 171.6 s |

> 墙钟合计 **95 条 rc=0 = 7153 s ≈ 1.99 CPU·小时**（收缩族 4054 s / 弯曲族 3098 s；另有 1 条失败重试 54 s 不计入）；单价区间 收缩稀疏 **52.6–59.0 s**、收缩稠密 **81.4 s**、弯曲稀疏 **107.8–116.1 s**、弯曲稠密 **171.6 s（中位）**、评估 **1.533 s**。本行数字是 9/26 由 `out/runs_index.psv`（见第四节）现算，取代首稿的"85 条 = 5712 s"。全部为本实例 `date +%s%N` 实测，无 `/usr/bin/time`、无 `bc` ⇒ **表注必须写明计时口径与论文 4.2 旧口径不同**。
>
> 本表首稿把格 9/10/11 的读数整体错后了一行（把 c09 写成 c10 的值），19:52 已按 `progress.jsonl` 逐格重导更正。首稿那段错读还配了一句"是我汇总时归并显示造成的排版歧义"的假解释——**那句话是编的，已删**。此处保留更正记录，因为它正好演示了本矩阵最需要的纪律：读数错了要当场撤回，不能给它造一个理由。

## 二、配对差（n=5，同 obs_seed，只换 train_seed）—— 这才是能写进正文的东西

符号约定：Δ = A − B，**负 = A 更好**；单位是指标的**百分点**（0.0255 记作 2.55 pp）。

| 对比 | A vs B | 指标 | Δmean | Δsd | 逐种子 Δ（pp） | A 更优/5 | 判读 |
|---|---|---|---|---|---|---|---|
| **表5-8 的核心** | 稠密主线 vs 阶段内 PDE 关 | speed | **−0.002** | 0.003 | +0.002 / −0.002 / −0.001 / −0.005 / −0.006 | 4 | **无可测贡献**。相对量级 0.09%，比种子散布（13%）小两个数量级；符号 4:1 但幅度等于零 |
| **表5-5 的核心** | 分层 5% vs 均匀 5% | u | **−0.027** | 0.347 | **+0.615** / −0.225 / −0.074 / −0.412 / −0.038 | 4 | **落在噪声内**：配对 t = **−0.173**（总体 sd）/ −0.154（样本 sd），均值仅及均匀臂水平的 ~1%。s42 的反例 +0.615 pp 与其余四项之和 0.749 pp **同量级**（不是"更大"），剔除 s42 后均值 −0.187 pp ≈ 臂水平的 7%，仍在噪声内 ⇒ 原来"换个种子方向就翻"不是异常，是这一格的常态 |
| （同上） | 分层 5% vs 均匀 5% | speed | −0.026 | 0.347 | +0.616 / −0.225 / −0.074 / −0.411 / −0.038 | 4 | 同上一条，指标换 `speed` 不改变结论 |
| **表5-1/5-4 的 1% 行** | 分层 1% vs 均匀 1% | u | **−1.232** | 0.941 | −2.477 / +0.432 / −1.315 / −1.253 / −1.549 | 4 | **稳**。相对改善 **27.5%**（分母取被击败的均匀臂；若取分层臂则 38.0%，正文用前者）。配对 t = **2.93**（总体 sd）/ **2.62**（样本 sd），5 个种子里 4 个同向且幅度一致 ⇒ 这是矩阵里第一条**可以正面声称**的方法学效果。仍不得写"显著"（n=5 最小可达 p=0.0625） |
| **特征消融** | basic 4 特征 vs geometry 14（都 soft） | speed | **+38.33** | 1.688 | +39.58 / +40.37 / +35.49 / +38.55 / +37.67 | 0 | **最强的一条**。5/5 同向，geometry14 把 0.5196 拉到 0.1362（**3.8×**），幅度是种子散布的 20 倍。几何特征有作用，这一点现在有凭据 |
| 噪声 1% | 无噪 vs +噪声 1% | u | **+0.082** | 0.018 | +0.094 / +0.046 / +0.089 / +0.089 / +0.093 | 0 | **反直觉**：加 1% 噪声反而让误差降 ~3%，5/5 同向、散布极小。先查混淆再写（见下"待确认"） |
| 噪声 5% | 无噪 vs +噪声 5% | u | −0.102 | 0.022 | −0.124 / −0.061 / −0.105 / −0.099 / −0.121 | 5 | 5% 噪声让误差升 ~4%。与上一行合看：**噪声响应曲线在原点附近是平的**，说明该量级噪声不承重 |

### 派生判读（这几句是结论，不是读数）

1. 全矩阵 n=5 下，**只有两个效应躲过了种子噪声**：① 几何特征消融（3.8×、5/5）；② 1% 观测下分层布点优于均匀（**27.5%**、4/5）。其余"谁比谁好"都是噪声。
2. **表5-8 的"阶段内 PDE 项"现在可以正面判定为无贡献**，而且这一格有了 5 个种子的一手 run 目录 —— 审计原判的"表5-8 不启用行只有 CSV、无 run 产物、无生成脚本"这一条**已解除**（等 `segment_01.tar.gz` 回流入库后按 `rev2609_t5c02__s*` 逐个挂坐标）。
3. **表5-5 必须改写为 mean±sd 的负结果**：分层与均匀在 5% 档不可区分。把它从"采样策略有增益"降级为"仅在极稀疏（1%）时可区分"，反而是更强、更可辩护的叙述——它给出了一条**曲线**（1% 有效、5% 失效），而不是一个单点断言。
4. 显著性上限不变：配对 Wilcoxon n=5 最小可达双侧 p = **0.0625 > 0.05** ⇒ 19 格矩阵**没有资格声称统计显著**，只能报 mean±sd + 逐种子符号；要显著性必须由 n=8 的配对单元承担。这条要写进正文，不能只放表注。

## 三、待确认（不要由本表直接下判）

- **噪声两格是否真的只差噪声 —— 已查明，不是混淆**：`model/scripts/generate_contraction_case.py:156` 写的是 `seed=args.seed + 300 + noise_pct`，噪声 RNG 由**数据生成那次**的 seed 决定，与 `train_seed` 无关 ⇒ 配对里没混进第二自由度，第 6/7 行不作废。**但代价是另一个限制**：每个噪声档只有**一种噪声实现**（5 个种子共用同一张含噪观测表），所以配对隔离出来的只是"训练随机性"，那两行读数是**该实现的性质**，不是"该噪声水平的一般性质"。因此：① 1% 处"加噪反而误差降 3%"不能写成"噪声起正则作用"，只能写成"在该噪声实现上观察到"；② 表5-6 的 3% 那行仍是 **n=1**，不得与 1%/5% 同格并列声称。
- 格 13 的 0.5196 是"崩了"而不是"差一点"：basic 4 特征 + soft 在稠密主线数据上误差 52%。要在正文写成"该臂实际上没有可用的壁面执行机制"，须再核它是否连观测拟合都失败（看 `loss` 曲线，不看最终 L2）。
- 格 18/19 **已跑完（20:0x）**：本条首稿写"只到 10% 档"是当时的状态，现已作废。账本终态 **96 条 train 行 = 95 成 + 1 败重试**，19 格全部满 5 个种子（逐格回读 `every_cell_5_seeds=true`）⇒ "5%≈稠密"的对照现在有 5 个种子。
- 段 01 收尾时**自检打印了 INVALID**（`内存=87 账本=89 / 失败 内存=0 账本=1`）。经账本原文复核：**数据完整，是记账缺陷不是数据缺陷**（`rc!=0` 不增失败计数、重试行重复入账、8 条 `[skip-train]` 归属未定）。闸门当场打红而没有放行，这次算它起了作用。修复要求记在任务 #18：**正式表一律由 `analyze_sweep.py` 从 `progress.jsonl` 单源重导**，且修完必须自带一个"必定 INVALID"的正对照，证明不是把闸门改成永远绿。

## 更正记录（本回合自己复算过，不是照抄工单）

首稿三句**派生量**算错，由方案线 commit `d43ec99` 抓到；我没有照抄它的结论，而是在 20:1x 用本表"逐种子 Δ"那一列自己复算确认（node 现算）。

| 首稿（错） | 复算后（对） | 错在哪 |
|---|---|---|
| 1% 档 "均值/散布 ≈ 3.9 个 sd·√n" | **t = 2.929**（总体 sd 0.9409）/ **t = 2.620**（样本 sd 1.0520） | 把 2.93 写成 3.9；而且没交代用的是总体还是样本 sd —— 正文两个都得给，否则读者无法复核 |
| 1% 档 "相对改善 ~38%" | 以**被击败的均匀臂**为分母 = **27.5%**；38.0% 是误用了被改进方的分层臂做分母 | 相对量的分母取反。约定：**一律以"改进之前那一侧"做分母** |
| 5% 档 "s42 的反例 +0.615 pp 大于其余四个之和" | 其余四项之和 = **0.749 pp**（比 s42 大）；剔除 s42 后均值 −0.187 pp ≈ 臂水平的 7% | 结论方向不变（仍是噪声，配对 t = −0.173），但**支撑句是错的**，属于为了说得有力而说过头 |

两处随之收紧：① 上表 1%/5% 两格的判读句已按现算值改写；② 第三节里"噪声种子是否与 train_seed 绑定"一条**已查代码排除混淆**（`generate_contraction_case.py:156`），但改写成"每档只一种噪声实现"的限制。

**纪律注记（写给自己也写给后续任何一格）**：这三句错的全是派生量，不是读数——逐种子 Δ、mean、sd 首稿全对，错在派生那一步（倍数、百分比、t 值靠手推）。所以本文之后新增的任何倍数/百分比/t 值，**必须与本表逐种子值同源现算**，不许手推；引外部结论时必须自己复算一遍再落纸。

## 四、run 级坐标索引已入库（9/26，解掉工单 §12.8 的前置）

工单 §12.8 那句"每条读数在挂进 docx 前必须回到 `progress.jsonl` 或 run 目录逐个挂坐标，不能只引这张表"缺的是一个**能引用的东西**。现在有了：

- **文件**：`docs/revision/T5矩阵run坐标索引-20260926.psv`（97 行 = 表头 + 96 条），`sha256 = c80d6f5537a09509eb6487c14c22f9a13d96571049a483b1734f1e17b0f59eaf`。
- **列**：`run | cell | train_seed | obs_seed | rc | rel_l2_u | rel_l2_speed | rel_l2_p | wall_ms | metrics_present`。`run` 就是 `model/results/pinn/<run>/metrics.json` 的目录名 ⇒ 每条读数到一手产物的坐标一一可查。
- **产生方式**：新实例（gateway `dsw-2212214` / instanceId `dsw-1zt3n6j7gueyq8zkng`）上跑仓库内 `model/scripts/ops/build_runs_index.py` @ `1a6044fe`（9/25 我自己写的索引器），读 `/mnt/workspace/pinn-repro-2026/out/progress.jsonl` 并逐 run 检查 `metrics.json` 是否存在；实例侧原样回读 **`rows=96 rc0=95 metrics_present=96`**（空表/缺件走 `INVALID` + 非零退出，不会报成功）。**机时**：一次索引器运行，秒级，无训练、无 GPU、**¥0**。
- **回传是有完整性校验的**：gzip 载荷 + base64 切 40 块，每块带 SHA-256 前 10 位逐块核。首轮转录被 **gzip CRC 判红**（`invalid compressed data--crc error`，事后定位是我漏抄了第 39 块）；补块后 **40/40 块哈希全绿**才解出这份 PSV。**未过闸的那份没有入库**。
- **原始账本没有逐位回传，这是本索引的边界**：通道代价（106 KB ≈ 1,980 个 72 字符块）不值得。留在案上的是不可变封存件的身份证：`out/segment_01.tar.gz` **106,564 B**，`sha256 = f2e292f850adec28001539aec95b9d8e5c0de3b4a6defaa87a802cfa9110b80b`，实例上时间戳 `9月 25 20:04`（NAS 上，本实例可读）。层次是：**复算正文读数 → 用这份 PSV；复算 PSV → 用那份封存账本 + `build_runs_index.py` @ `1a6044f`。** 不许把"PSV 在仓里"说成"原始账本在仓里"。

**这份索引与本表第一节的互核（现算，不是照抄）**：表内可解析的 15 行里 **14 行逐位复现**（含 格1 `0.02362±0.00308`、格13 `0.51955±0.01322`、格16 `0.02508±0.00227`、格19 首版单粒 `0.02252`）；唯一不合的是**格17 那行过期**（写表时只有 3 粒种子 `0.02493±0.00214`，满 5 粒后是 `0.02502±0.00157`）——已就地更新，格18/19 同步补齐。也就是说：读数来源单一化了，而且它反过来证明本表首稿的读数层没有串档。

**随之到位的一条判决：工单 §12.2 缺的"格19 另外 4 个种子"已经进来了。** 弯曲族对稠密（格19）的**逐种子配对差**（`rel_l2_speed`，正=稀疏档更差，5 粒同 train_seed）：

| 对比 | 配对差 mean ± sd | 相对稠密 | 稠密更好的种子数 | 配对 t |
|---|---|---|---|---|
| 格15 分层 1% − 稠密 | **+0.00212 ± 0.00067** | +8.7% | 5/5 | 7.1 |
| 格16 分层 5% − 稠密 | +0.00074 ± 0.00086 | +3.0% | 4/5 | 1.92 |
| 格17 分层 10% − 稠密 | +0.00067 ± 0.00076 | +2.8% | 3/5 | 1.97 |
| 格18 分层 15% − 稠密 | **−0.00020 ± 0.00052** | −0.8% | 2/5 | −0.86 |

**只能写到这儿**：① 5% 与稠密之差（+3.0%）小于种子散布（sd 8.0%），所以"5% 档已接近稠密配置"在弯曲族成立且有一手产物；但**不许写"等价/无差异"**——没做 TOST 或区间包含性检验，且 n=5 时 Wilcoxon 配对的最小可达双侧 p = 2/2⁵ = 0.0625 > 0.05，结构上给不出"显著"。② **不得外推到收缩族**：那侧同样对稠密主线（格1）的配对差是 分层10% **−0.00150 ± 0.00400（−6.4%，稀疏反而更好）**、分层5% **+0.00189 ± 0.00494（+8.0%）**，连符号都不稳。③ 表 5-4 内仍无 dense 行，横比禁忌不变（工单 I4）。
