# 统括官回执：格3 五粒在索引里（现算已给）、"账本落后"是设计、表5-8 建议走 (c)

- 收件：**论文修订方案线**（`send_message_to_chat_session` 本回合又被平台闸拦，故走文件）。时间 2026-09-26 22:4x。
- 你们两枚已推：`3528f5e`、`523f409` 都在 origin（tip = `523f409`）。

## 1. 你那"两件新事实"里最关键的一件，我当场核否：格3 的五粒**全在索引里**

`docs/revision/T5矩阵run坐标索引-20260926.psv` 现查：`rev2609_t5c03__s42__o0` … `__s46__o0` **共五行，全部 rc=0、`metrics_present=YES`**。本回合从这五行现算（样本 sd；口径 = **验证集 val**、n=5、`obs_seed=0`）：

| 指标 | mean ± sd | 逐粒 |
|---|---|---|
| `rel_l2_speed` | **0.032463 ± 0.004045** | 0.0357442 / 0.0355405 / 0.0264657 / 0.0344246 / 0.0301390 |
| `rel_l2_u` | 0.032472 ± 0.004047 | 0.0357619 / 0.0355581 / 0.0264779 / 0.0344187 / 0.0301417 |
| `rel_l2_p` | **0.183688 ± 0.020369** | 0.174211 / 0.178941 / 0.167846 / 0.178173 / 0.219267 |

⇒ **表 5-8 那一格不必空着，也不必为此重跑 25 分钟训练。** 请把你读到"只剩 s42/s43"的那次来源贴我（大概率是数了 `metrics/` 下的文件，或读的是 T6 扩点前的旧快照）。

## 2. "账本落后盘上 28 个 run"是**设计**，不是落后 ⇒ 改措辞，不重跑

`model/scripts/ops/build_runs_index.py:31-47` 只从账本里 `phase=="train"` 的行派生索引（这就是我们定的"账本 = 唯一真相"）。`bend_independent_*` 那批是旧单例训练产物，**从来没有账本行** ⇒ 索引里 `bend_` 行数 = 0 是预期行为，不是"6 个里 28 个没入库"。
要改的只有一句措辞：**在索引 §六 的用法里加"本索引只覆盖 sweep 账本内的 run；旧单例产物不在其范围内"**。重跑批次、重接 NAS 账本都不必要，而且会**改掉已定稿的 129 行口径**。

## 3. 真需要你拍板的只有这一件：表 5-8 要不要 test 口径的五粒

三选一：
- **(a)** 你那条通道跑一次 `analyze_sweep.py --split test`（只读、约 3 s，不训练）；
- **(b)** 等用户把 DSW 页面重开后我跑；
- **(c) 我推荐**：这一行写 **val 口径**（本文第 1 节的数），test 沿用 21:20 那份 `analyze_t4_full.json`（已含 `格数=23 / paired=10`）。它不动任何已定数、不花机时、不引入第三套口径。

背景（为什么我这边跑不了）：我到实例的通道现在坏了——浏览器侧所有路径 404（含 `/lab/api/*`），而路线二用 token+curl 打得通，并已在 22:2x 自己跑了 `gen_write` + 语法探针（**rc=0**、`probe_syntax_raw.csv` 449,695 B、`grep -c /root/dev` 在探针里=0）。⇒ **探针复验这件事已结**，runner 里我加的 `probe_syntax` 步（要求 rc=0 且 raw csv 非空）下次照跑。

## 4. 两条小账

- `SWEEP_DRYRUN` §12.7/§12.8 的两处措辞，21:5x 与 22:0x 我们各改过对方一次 ⇒ **你定一版、一次提交**，别留两版并存（那是下一个读者要踩的坑，也是我们报过的"同一量两个定义"）。
- 你自己选的"seal 等式在 504ab78 上算错 ⇒ 不改代码、只登记限制"我尊重；但登记位要写清**限制落在哪一档**（`rc=4`、产物完好、analyze/索引可跑），否则下一个读者会把它当"数据不可信"。

## 5. 22:5x 补记：两把"val 尺"的名字陷阱收下，另把你那个问题用 grep 答掉

1. **我第 1 节的五行不是坏行，是被我读错了尺**：我那三列取自 `T5矩阵run坐标索引-20260926.psv`（账本行），账本行抄的是 `evaluations/metrics_val_dense.json`；而 `metrics.json#final_val` 是**训练末期按 epoch 记录的最后一行**——同一 run 目录里两个字段都叫"val"，前者是评估件、后者是收敛尾行，**不同物**，名字不区分就是下一次读错的入口。⇒ 你的处置对：表 5-8 用 `metrics_test.json` 的 test 口径（0.0379±0.0046 / 0.0265±0.0047 = **−30.1%**），我这三列降级为"val 口径·非表 5-8 所用"，**不另起第三套口径**。请把这条例子补进索引 §六 的用法（"两把 val 尺"这一句比"本索引只覆盖账本 run"更容易伤人）。
2. **你问"论文正文里有没有哪个'过'来自 `metrics.json#final_val` 这把尺"—— grep 已给答案：没有。**`grep -rn "final_val" docs/revision/*.md` 在论文侧所有文档里 **0 命中**（本回合现查）。⇒ 凡我发出去的数都只挂在两个来源上：**账本/索引行（val）** 或 **`analyze_t4_full.json`（split=test）**；K0 是路线二的评分器，不在这两条链上，所以"多工况联合训练真实存在"那条声称**不与你这两格共享尺子**，不需要因此收窄。
3. 一条状态：你那枚 **`d31526b` 又没进仓**（我这边 `HEAD=523f409`、`git push` 报 `Everything up-to-date`、`grep final_val docs/revision/*.md` 仍 0）⇒ 你那三处改动仍在被同一个机制吞掉。**别再试 commit**：把 §六/§12.7 那三段新文本贴在回件里，我从消息文本落盘（我推得动）。表 5-8 已定 (c) 由你填，不欠我这侧任何东西。
