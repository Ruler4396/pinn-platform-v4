# 冻结前拍板与未做清单（2026-09-26 21:5x，方案线）

- 用途：T12 冻结线 **9/29** 之前，把《正文改写工单》里所有"要谁点头"和"明写未做"的条目收成**一张表**，每行必须回答**改变哪个决定**。此表不新增实验、不改代码、不动数字。
- 生成方式：对 `正文改写工单-20260925.md` 全部 51 条工单行现扫关键词（J 类 3 条为 21:4x—22:0x 新增）（`待作者/待拍板/未做/不得写/不许/若将来/可选/待定/否决`），命中 **11 行**（A4、A8、A12、A13、B4、B5、C3、D2、G4、H5、J2），逐条落到下表；下表再补 4 条只在正文/表注里出现、没进工单行的限制。
- 默认动作一栏写"沉默即执行"的东西：**作者不表态就按默认走**，不默认升格成更强声称。

## 一、要点头的（4 条，全都不花钱）

| # | 事项 | 谁 | 截止 | 不决定的后果 | 默认（沉默即执行） |
|---|---|---|---|---|---|
| 1 | **A13 摘要与 Abstract 的降级措辞**（"物理约束"→"损失中包含残差惩罚项"，删无凭据的通用宣称） | 作者本人一句话 | 9/28 | 摘要与第 5 章口径不一致，是最容易被一票否决的位置 | 按已写好的降级版进正文；否决只回退本行，不影响 A 类其余 16 条 |
| 2 | **A12 表 5-8 用哪一版**（甲=沿用单次读数只换归因句；乙=五对配对判"无可测贡献"） | 作者 | 9/28 | 甲弱一档但今天就能定；乙需要把格2 五粒坐标挂全 | 甲（乙作加分，不阻塞） |
| 3 | **T11 复现声明定档**（甲改物 / 乙改话） | **已定：乙**（J1/J2） | 已结 | — | 声明限定为"NS 四份主件 + 语法探针写路径已相对、取回即跑；32 份 Stokes 入库件（22 份写 `/root/dev/pinn_v3` + 10 份 `__ip_skewed_*` 写 `/root/dev/pinn-platform-v4/model`）需自建目录或改一行写路径"，并把这条做成回复表第 15 行的独立一格 |
| 4 | **表 5-10 稀疏档双模型那一格的样本口径**（甲=同源五粒 0.031202 / 乙=保 n=11 值 0.029925 并注） | **已定：甲**（`3528f5e`） | 已结 | 并排不同源 ⇒ 同行被一句"这两个数不是同一批"打掉 | 0.031202±0.004515（n=5, obs_seed=0）；0.029925 只留在 5.4 的 n=11 那一格 |

## 二、明写"未做"的（不许在正文里升格）

> 处置档位四选一：**改话**（只改措辞）／**≤1 分钟便宜补算**（一条命令能补，补完才允许那句话）／**必须撤**（9/29 前不补就把句子删掉）／**留作投稿期**（本轮不承诺，写进后续工作）。

| # | 未做的东西 | 它本来能支撑哪句话 | 现在的写法（限制句） | 改变哪个决定 | 处置档位 |
|---|---|---|---|---|---|
| 0 | 臂 C 阶数敏感性（**9/26 23:5x 撤销“已跑”销账**） | “r=1 由判据选出、加阶不改变速度场结论” | **未做**：统括官仓内文件级检查证明那串 `0.828487→0.829780` 从未产出（全仓 0 处、日志无 `rank=2/3/4`），那份 JSON 也从未存在 ⇒ D2 表注停在“判据算出 r=1、非人工挑选”，禁写“已检验/平坦” | 臂 C 那句话的强度上限；也决定表 5-10 是否只剩臂 C 孤档 | **≤1 分钟便宜补算**（`--max-modes 4` 与 `8` **两版都要**，产物落仓并自报 sha256；只跑 r=1 一次的运行不足以支撑那句） |
| 0b | 臂 C 的 JSON 产物只在实例 NAS、未入本端点仓 | “产物可回读、强度 A 级” | 表注统一写“数值取自运行日志逐工况行、JSON 未归档” ⇒ 停在 **B 级·转引日志**；正文禁写“已归档” | 只影响强度分级，不影响三个读数 | **≤1 分钟便宜补算**（命令已备好：`baselines_pod.py … --out model/results/…/pod_baseline_contraction.json`，产物自证 path/sha256/bytes；与 E5 合并成同一次实例往返，见 SWEEP_DRYRUN §13） |
| 2 | 多重比较校正 | "四对配对里 X 成立" | 表 5-10 表注②：未做多重比较校正；n=5 最小可达双侧 p=0.0625 ⇒ 只报符号与幅度 | 全表统计口径，已在 §0 锁死 | **改话**（口径已锁：只报符号与幅度，不补校正） |
| 3 | 分区误差 ↔ 整体误差的因果干预 | "分区误差解释了整体差异" | H5：只描述共变，声明未做干预实验（需另设配额扰动） | 5.4/图5-18 一句归因的下限 | **改话**（只描述共变；主张因果需留作投稿期做配额扰动） |
| 4 | **E5：同一台机上补测 CFD 单价 A** | "5.7 的效率比是同机同次" | E3 末句"混合口径"限定**必须留**：A、C 取旧主机 artifact，B 取本实例 | 表 5-9 与 K\* 的口径强度；FreeFEM v4.9 已可用，机时≈秒-分钟级 | **≤1 分钟便宜补算**（`model/scripts/e5_cfd_price.py --repeats 7`：逐次打印、程序算中位数、两族齐全；不补则 E3 的“混合口径”限定句必须留 ⇒ 与 0b 同一次往返） |
| 5 | 第二求解器交叉验证（COMSOL/ANSYS/µPIV） | "独立实现验证了真值" | 只能说"换主机 + FreeFem++ v4.9 逐位 diff=0（5/5 工况）"，**禁写"第二实现/独立实现"** | 外部有效性一段；无授权、无 ¥，5 天内不可达 | **留作投稿期**（无授权、¥0 通道不可达；本轮改话禁写“独立实现”） |
| 6 | 网格无关性 | "解与离散化无关" | 局限性段一句带过，不给数值 | 与 5 同段，不新开实验 | **留作投稿期**（网格无关性；局限性一句带过） |
| 7 | 表 5-6 的 3% 噪声档升到五种子 | "噪声响应三点成线" | 3% 那行仍 **n=1**，换列时必须标 n=1，不许混进"五种子"口径 | 表 5-6 的口径纯度 | **改话**（3% 那行标 n=1，不混进“五种子”口径） |
| 8 | `sweep_lib.sh:_run_metrics_json` 认两套 metrics schema | 臂 B 的数直接进账本/索引 | SWEEP_DRYRUN §11.5：臂 B 只能走 `analyze` 的 supervised 根 + `mean_of_cases`（pooled 结构上无对，§12.9 第 3 条） | 属读数口径变更，故意推后；不影响任何已定稿数 | **留作投稿期**（属读数口径变更，改了对账要重跑历史段） |
| 9a | ~~公开文件仍无条件~~ **已闭（统括官 9/26 23:3x 代改，见工单 J3 行）**：`README.md:34` 的"全部在同一仓库里可跑、可查、可复现"改为链路级 + 指向限制；`docs/REPRODUCTION_GUIDE.md` §4 末加了可移植性边界段（32 份 Stokes / 两前缀 / `err code 8`），并把该节的 `FreeFEM++` 改成真实二进制名 `FreeFem++` | 仓库对外的复现承诺 | 公开面与毕设口径现在同强度；**方案线不要再动这两份公开文件**（要动先读 J3 行的凭据栏） | 公开承诺的强度 | **已闭** |
| 9b | `--only-cells` 挂进 `t6_matrix` | 手敲命令也能只跑三件套 | 统括官裁本轮不动；靠"必须带 `--baseline`"绕开（§12.6） | 只是操作面，不改结论 | **留作投稿期**（统括官裁：本轮不修，靠“必须带 `--baseline`”绕开） |

另两条装置侧、不改变正文：**`analyze_t4_full.json` 含 2 处裸 `NaN`**（严格 JSON 解析器会抛；T2 那道"论文数值↔产物"自动核对闸若换解析器要先归一）；**`values` 数组按 run 目录名排序而非 `pair_keys` 顺序**（引用坐标只指 keys/索引集）。

**E0/E5 的落点（9/27 00:2x 定档，统括官 00:1x 通知）**：两条便宜补算**都不由我们跑、也不等我们**——他这侧实例通道自 22:0x 起同源 `fetch('/dsw-2212214/api/status')` 与 `/api/terminals` 全 404（裸 curl 302 到登录页 ⇒ 网关活着、Jupyter 不 serve，此读数**转引自统括官**，本线无通道无法复现），命令与 pin `1906135` 已转派路线二（`paper-route2/派单-计数结算与假时钟-20260926.md` §九），受"worker 汇报回合不得扇出"的平台闸限制，要等用户下一条人类消息才真正投出。**⇒ 若最终无人跑成，两件登记为未做**：臂 C 停在 B 级·转引日志（D2 一字不改）、表 5-9/E3 保留"混合口径"限定句；**任何位置都不许预先写"已升 A 级"或"同机同次"**。

## 三、这张表怎么被用掉

9/29 冻结时，第 1–2 条必须有一句作者答复或明确"按默认"；第三节的 9 条**不需要**任何动作，只需要保证正文没有出现被禁的写法——`§10` 的自检行已经把其中 4 条变成可 grep 的断言（臂 B 从账本取数、val 数进第 5 章、pooled 口径、0.029925 进表 5-10）。
