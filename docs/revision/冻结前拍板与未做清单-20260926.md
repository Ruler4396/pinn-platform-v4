# 冻结前拍板与未做清单（2026-09-26 21:5x，方案线）

- 用途：T12 冻结线 **9/29** 之前，把《正文改写工单》里所有"要谁点头"和"明写未做"的条目收成**一张表**，每行必须回答**改变哪个决定**。此表不新增实验、不改代码、不动数字。
- 生成方式：对 `正文改写工单-20260925.md` 全部 51 条工单行现扫关键词（J 类 3 条为 21:4x—22:0x 新增）（`待作者/待拍板/未做/不得写/不许/若将来/可选/待定/否决`），命中 **11 行**（A4、A8、A12、A13、B4、B5、C3、D2、G4、H5、J2），逐条落到下表；下表再补 4 条只在正文/表注里出现、没进工单行的限制。
- 默认动作一栏写"沉默即执行"的东西：**作者不表态就按默认走**，不默认升格成更强声称。

## 一、要点头的（4 条，全都不花钱）

| # | 事项 | 谁 | 截止 | 不决定的后果 | 默认（沉默即执行） |
|---|---|---|---|---|---|
| 1 | **A13 摘要与 Abstract 的降级措辞**（"物理约束"→"损失中包含残差惩罚项"，删无凭据的通用宣称） | 作者本人一句话 | 9/28 | 摘要与第 5 章口径不一致，是最容易被一票否决的位置 | 按已写好的降级版进正文；否决只回退本行，不影响 A 类其余 16 条 |
| 2 | **A12 表 5-8 用哪一版**（甲=沿用单次读数只换归因句；乙=五对配对判"无可测贡献"） | 作者 | 9/28 | 甲弱一档但今天就能定；乙需要把格2 五粒坐标挂全 | 甲（乙作加分，不阻塞） |
| 3 | **T11 复现声明定档**（甲改物 / 乙改话） | **已定：乙**（J1/J2） | 已结 | — | 声明限定为"NS 四份主件取回即跑；23 份 Stokes 入库件需自建目录或改一行写路径"，并把这条做成回复表第 15 行的独立一格 |
| 4 | **表 5-10 稀疏档双模型那一格的样本口径**（甲=同源五粒 0.031202 / 乙=保 n=11 值 0.029925 并注） | **已定：甲**（`3528f5e`） | 已结 | 并排不同源 ⇒ 同行被一句"这两个数不是同一批"打掉 | 0.031202±0.004515（n=5, obs_seed=0）；0.029925 只留在 5.4 的 n=11 那一格 |

## 二、明写"未做"的（不许在正文里升格；9 条）

| # | 未做的东西 | 它本来能支撑哪句话 | 现在的写法（限制句） | 改变哪个决定 |
|---|---|---|---|---|
| 1 | 臂 C 的 `--max-modes 4` 阶数敏感性 | "基线阶数选择稳健" | 只写"阶数由能量判据 0.9997 算出（r=1），非人工挑选；奇异值谱第 4 阶后降 9–11 个量级"（D2 表注） | 不加格子 ⇒ 只影响臂 C 一句话的强度 |
| 2 | 多重比较校正 | "四对配对里 X 成立" | 表 5-10 表注②：未做多重比较校正；n=5 最小可达双侧 p=0.0625 ⇒ 只报符号与幅度 | 全表统计口径，已在 §0 锁死 |
| 3 | 分区误差 ↔ 整体误差的因果干预 | "分区误差解释了整体差异" | H5：只描述共变，声明未做干预实验（需另设配额扰动） | 5.4/图5-18 一句归因的下限 |
| 4 | **E5：同一台机上补测 CFD 单价 A** | "5.7 的效率比是同机同次" | E3 末句"混合口径"限定**必须留**：A、C 取旧主机 artifact，B 取本实例 | 表 5-9 与 K\* 的口径强度；FreeFEM v4.9 已可用，机时≈秒-分钟级 |
| 5 | 第二求解器交叉验证（COMSOL/ANSYS/µPIV） | "独立实现验证了真值" | 只能说"换主机 + FreeFem++ v4.9 逐位 diff=0（5/5 工况）"，**禁写"第二实现/独立实现"** | 外部有效性一段；无授权、无 ¥，5 天内不可达 |
| 6 | 网格无关性 | "解与离散化无关" | 局限性段一句带过，不给数值 | 与 5 同段，不新开实验 |
| 7 | 表 5-6 的 3% 噪声档升到五种子 | "噪声响应三点成线" | 3% 那行仍 **n=1**，换列时必须标 n=1，不许混进"五种子"口径 | 表 5-6 的口径纯度 |
| 8 | `sweep_lib.sh:_run_metrics_json` 认两套 metrics schema | 臂 B 的数直接进账本/索引 | SWEEP_DRYRUN §11.5：臂 B 只能走 `analyze` 的 supervised 根 + `mean_of_cases`（pooled 结构上无对，§12.9 第 3 条） | 属读数口径变更，故意推后；不影响任何已定稿数 |
| 9a | 公开文件 `README.md`/`docs/REPRODUCTION_GUIDE.md:38` 的"重新生成 CFD 真值"仍无条件（工单 **J3** 已登记最小改法，本线未代改） | 仓库对外的复现承诺 | 若要点一句，两行注即可；不动则公开面与毕设口径不一致 | 公开文件所有权不在本线 |
| 9b | `--only-cells` 挂进 `t6_matrix` | 手敲命令也能只跑三件套 | 统括官裁本轮不动；靠"必须带 `--baseline`"绕开（§12.6） | 只是操作面，不改结论 |

另两条装置侧、不改变正文：**`analyze_t4_full.json` 含 2 处裸 `NaN`**（严格 JSON 解析器会抛；T2 那道"论文数值↔产物"自动核对闸若换解析器要先归一）；**`values` 数组按 run 目录名排序而非 `pair_keys` 顺序**（引用坐标只指 keys/索引集）。

## 三、这张表怎么被用掉

9/29 冻结时，第 1–2 条必须有一句作者答复或明确"按默认"；第三节的 9 条**不需要**任何动作，只需要保证正文没有出现被禁的写法——`§10` 的自检行已经把其中 4 条变成可 grep 的断言（臂 B 从账本取数、val 数进第 5 章、pooled 口径、0.029925 进表 5-10）。
