# G0 补测读数 · θ=60°（B-test-2）转角外推 —— 已跑完，n=1，两工况并排

- 执行：统括官（我）在实例 `dsw-2212214` 上代跑，2026-09-26 18:0x（北京时间）。**这条不是"派别人查"，是一手评估产物。**
- 机时：**两次评估、单次 6–8 s 量级、CPU、¥0、无训练**（模型是论文主线那个 run 的既有 checkpoint，只是从没在测试工况上评过）。
- 回答对象：`正文改写工单-20260925.md` 第 7 节 G0/G1/G2/G3 与审稿回复表第 9 条（"θ=60° 转角泛化是否被检验"）。**判决：检验过了，而且明确退化。**

## 一、被评的模型与它自己的账本（先把"这是论文那个模型"钉住）

| 项 | 值 |
|---|---|
| run | `model/results/pinn/bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419` |
| `best.ckpt` sha256 | `61c719579a3a5490a4edb42a0980817ce21c0b6dc124a7a6531c2c75bdcb4228`（288,305 B；仓库 blob 与实例落地件逐位一致） |
| `config.json` sha256 | `6ef30260794c4043a6dd4a82d22ec3c25d913dea52033a909e2380a33f595e5e`（LF 形式） |
| `train_cases` / `val_cases` | `B-base, B-train-1, B-train-2, B-train-3` / `B-val` ⇒ **B-test-1 与 B-test-2 都不在训练或验证集里**（这条是本次比较成立的前提） |
| 观测/评估源 | dense（该 run 本来就是稠密主线，不是 strict-sparse 批） |
| 特征 | `feature_mode=geometry`，`drop_features=[inlet_profile_star]` |

**投递事实**（顺手记一条环境账）：这枚 checkpoint **在仓库里是已跟踪文件**（`git ls-files` 618 个 `model/results/**` 之一），但**实例的 `model/` 树里没有历史 run 目录**（那边只有 `rev2609*` + `t0_repro_s42/43` + 我的 smoke）。⇒ 我按 pin `ee056c4` 从 raw 拉过去并逐位验哈希。**推论：以后任何"拿论文原模型在实例上评一次"的打算，都要先算这一步（288 KB、一条 curl），别默认实例有历史产物。**

命令（原样，可复跑）：
```
cd /mnt/workspace/pinn-repro-2026/model && PYTHONPATH=/mnt/workspace/pinn-repro-2026/pylibs OMP_NUM_THREADS=1 \
python3 scripts/evaluate_velocity_pressure_independent.py --family bend_2d \
  --run-name bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419 \
  --eval-cases B-test-1,B-test-2 --split-name test --eval-source dense
```
产物：**已随仓库入库** —— `model/results/pinn/bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419/evaluations/metrics_test.json`（3,613 B，`sha256 = de42799645564c3bf0d97b972e87375c01a4ea67ec03bea624a28ab73cdd980c`，与实例上的原件逐位一致：回传走 gzip+base64 分 24 块、每块 SHA-256 前 10 位逐块核，首轮 22/24 绿、两块红→重取那两块→24/24 全绿才落盘）。同目录另有 `manifest_test.json` 与 `predictions_test/`（未回传，可按上面命令 8 s 重生成）。**仓库里原本没有同名评估件，我没有覆盖任何既有产物**（`git ls-files` 该 run 目录只有 ckpt/config/history/metrics/一个 val 预测 CSV）。

## 二、读数（逐字段，原值，6 位有效以上照抄）

| 工况 | 转角 | `rel_l2_speed` | `rel_l2_p` | `pressure_drop_rel_error` |
|---|---|---|---|---|
| B-test-1 | θ=90°（同族外推） | **0.026912** | 0.035984 | 0.020407 |
| B-test-2 | **θ=60°（转角维度外推）** | **0.065474** | **0.235530** | 0.035673 |
| 两工况均值 | — | 0.046193 | 0.135757 | 0.028040 |

派生量（由本表现算，非手推）：**speed 退化 ×2.43（+143.3%）；压力退化 ×6.55（+554.5%）；压降退化 ×1.75（+74.8%）**。

## 三、判读（G3 的事前分支 + 三条不许越过的线）

1. **转角这一维从"从未评估"变成"1 个外推工况、n=1，且相对同族 90° 明显退化"**。表 3-3 的说明列、5.6.3、表 5-7 现在都有东西可引；但**每个引用都必须带 n=1**（一个几何、一粒种子、一次评估）。
2. 工单 G 类事前写过"若压力读数落在 **0.4 以上量级**则同时保留范围声明" ⇒ 实测 0.2355，**该分支不触发**。这句话现在就写死在这儿，跑完不许回头改判据。
3. **不得**据此说"模型能外推到任意转角"：只有一个角度、且退化方向明确；能说的是"在 60° 上速度场仍有 6.5% 量级误差、压降 3.6%，但压力场误差升到 23.6%，比 90° 高一个数量级不到、约 6.5 倍"。
4. **不得**把它当第 5 章稀疏口径的读数：这个 run 是 **dense 主线**，表 5-7 的 B 行是 5% 稀疏档 ⇒ 入表时"观测条件"列必须填 `dense`（工单 I4 新加的那列就是为这个）。
5. 与 §12 的关系：这条**不改变**"弯曲族结论目前只到 10%/15%/稠密档"的账（那是采样率维度），它补的是**转角维度**的缺口，两回事。

## 四、方案线要做的一步（不用重跑）

G1/G2/G3 落进 docx：表 5-7 加一行（B-test-2 / 弯曲流道 / dense(θ=60°) / 0.065474 / 0.235530 / 0.035673 + 表注 n=1），表 3-3 说明列改为"转角泛化测试工况（单次评估，n=1；同模型在 θ=90° 上为 0.026912/0.035984）"。**凭据坐标全部在本文件第一节**，不用再问实例。
