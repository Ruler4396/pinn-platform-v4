# 证据摘录：臂 C（POD+LS）三个逐工况读数 —— 唯一副本抢救进仓

- 为什么要有这份文件：`model/results/contraction_2d/baselines/pod_baseline_contraction.json` **不在 NAS 上**（方案线 23:0x 全树 `find` 为空），臂 C 的五个数此前**只活在对话里**，"臂C 已在 NAS 封存"这句是错的（我的 §12.8 也已撤回）。
- 但数值本身**有一手文字来源**：那次 `armC_real` 步骤的 stdout 就打了逐工况误差。下面五行是方案线 23:1x 从实例 `out/ledger/`（NAS）里读出来贴给我的**原样文本**，我照原样落盘。
- 强度标级：**转引·一手产物的文本副本（B+）**，不是产物本体。要升到 A 需要重跑臂 C（5.7 s、CPU、¥0）——见文末。

```
=== contraction_2d  eval_cases=['C-val', 'C-test-1', 'C-test-2']  rank=1 ===
  C-val: rel_l2(u)=0.833049 rel_l2(speed)=0.828487 rel_l2(p)=0.018883 dp_rel_err=0.011176 n_cases=2541
  C-test-1: rel_l2(u)=0.541891 rel_l2(speed)=0.531695 rel_l2(p)=0.058066 dp_rel_err=0.020943 n_cases=2541
  C-test-2: rel_l2(u)=0.617619 rel_l2(speed)=0.611451 rel_l2(p)=0.044503 dp_rel_err=0.015182 n_cases=2541
=== done: /mnt/workspace/pinn-repro-2026/model/results/contraction_2d/baselines/pod_baseline_contraction.json ===
=== rc=0 wall=5.709s  max_tokens=26581  账本行已落：model/results/.sweep_ledger.jsonl ===
```

## 由此定的两条口径（表 5-10 / D2 / 创新点二 都按这两句写）

1. **表 5-10 的臂 C 格不是空白，是"来源降级"**：三个速度误差 **0.828487 / 0.531695 / 0.611451**、压力误差 **0.018883 / 0.058066 / 0.044503**、压降误差 **0.011176 / 0.020943 / 0.015182**，`rank=1`（能量判据 0.9997 推得，非人为挑弱基线）、`n_cases=2541`、单次读数无种子散布、**不训练 ⇒ 墙钟不可与 PINN 行直比**。表注必须写：**"数值取自运行日志逐工况打印行（本文件），其 JSON 产物未归档"**；正文不得出现"产物已入库"。
2. **结论不变且这正是它要说的话**：零训练低阶模态基线在**速度场差 1–2 个数量级**（0.53–0.83 vs 本文 0.0286），在**压降上一同数量级**（1.1–2.1% vs 本文压降误差）⇒ "速度结构需要学习"这句靠的就是这一格，**它依赖的是"跑过并打了数"，不是"JSON 在不在仓里"**。

## 还欠的两件（都不动结论，但要在 9/29 前结）

- **(i) 重跑臂 C 一次并把 JSON 入库**：`cd model && python3 scripts/baselines_pod.py --family contraction_2d --eval-cases C-val,C-test-1,C-test-2 --obs-files obs_sparse_5pct.csv --out <仓内路径>`，5.7 s、CPU、¥0；跑完逐位对这三行日志 ⇒ 若一致，强度升 A 且"未归档"那句删掉；若不一致，**以新读数为唯一数**并撤回旧引用。谁跑：方案线（他们有 SSH）或我等实例通道恢复后跑；**不排队挤占 T12**。
- **(ii) 一个会答"你为什么不先试简单的"的低阶对手**：解析 Poiseuille（同族精确解）当第二基线。它是审稿人第一问，**不补就得在创新点二里明写"未解析对照"**（方案线 23:0x 自己列的那条）。裁决见当天回件。
