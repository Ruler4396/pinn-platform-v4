# 证据摘录：臂 C（POD+LS）三个逐工况读数 —— 唯一副本抢救进仓

- 为什么要有这份文件：`model/results/contraction_2d/baselines/pod_baseline_contraction.json` **不在 NAS 上**（方案线 23:0x 全树 `find` 为空），臂 C 的五个数此前**只活在对话里**，"臂C 已在 NAS 封存"这句是错的（我的 §12.8 也已撤回）。
- 但数值本身**有一手文字来源**：那次 `armC_real` 步骤的 stdout 就打了逐工况误差。下面五行是方案线 23:1x 从实例 `out/ledger/`（NAS）里读出来贴给我的**原样文本**，我照原样落盘。
- 强度标级：**转引·一手产物的文本副本（B+）**，不是产物本体。要升到 A 需要重跑臂 C（5.7 s、CPU、¥0）——见文末。

```
=== contraction_2d  eval_cases=['C-val', 'C-test-1', 'C-test-2']  rank=1 ===
  C-val: rel_l2(u)=0.833049 rel_l2(speed)=0.828487 rel_l2(p)=0.018883 dp_rel_err=0.011176 n_cases=2541
  C-test-1: rel_l2(u)=0.541891 rel_l2(speed)=0.531695 rel_l2(p)=0.058066 dp_rel_err=0.020943 n_cases=2541
  C-test-2: rel_l2(u)=0.617619 rel_l2(speed)=0.611451 rel_l2(p)=0.044503 dp_rel_err=0.015182 n_cases=2541
=== done: /mnt/workspace/pinn-repro-2026/model/results/contraction_2d/baselines/pod_baseline_contraction.json ===
=== rc=0 wall=5.709s  max_tokens=26581  账本行已落：model/results/.sweep_ledger.jsonl ===
```

## 由此定的两条口径（表 5-10 / D2 / 创新点二 都按这两句写）

1. **表 5-10 的臂 C 格不是空白，是"来源降级"**：三个速度误差 **0.828487 / 0.531695 / 0.611451**、压力误差 **0.018883 / 0.058066 / 0.044503**、压降误差 **0.011176 / 0.020943 / 0.015182**，`rank=1`（能量判据 0.9997 推得，非人为挑弱基线）、`n_cases=2541`、单次读数无种子散布、**不训练 ⇒ 墙钟不可与 PINN 行直比**。表注必须写：**"数值取自运行日志逐工况打印行（本文件），其 JSON 产物未归档"**；正文不得出现"产物已入库"。
2. **结论不变且这正是它要说的话**：零训练低阶模态基线在**速度场差 1–2 个数量级**（0.53–0.83 vs 本文 0.0286），在**压降上一同数量级**（1.1–2.1% vs 本文压降误差）⇒ "速度结构需要学习"这句靠的就是这一格，**它依赖的是"跑过并打了数"，不是"JSON 在不在仓里"**。

## 还欠的两件（都不动结论，但要在 9/29 前结）

- **(i) 重跑臂 C 一次并把 JSON 入库**：`cd model && python3 scripts/baselines_pod.py --family contraction_2d --eval-cases C-val,C-test-1,C-test-2 --obs-files obs_sparse_5pct.csv --out <仓内路径>`，5.7 s、CPU、¥0；跑完逐位对这三行日志 ⇒ 若一致，强度升 A 且"未归档"那句删掉；若不一致，**以新读数为唯一数**并撤回旧引用。谁跑：方案线（他们有 SSH）或我等实例通道恢复后跑；**不排队挤占 T12**。
- **(ii) 一个会答"你为什么不先试简单的"的低阶对手**：解析 Poiseuille（同族精确解）当第二基线。它是审稿人第一问，**不补就得在创新点二里明写"未解析对照"**（方案线 23:0x 自己列的那条）。裁决见当天回件。

## 二、可直接粘贴的 D2 替换行（统括官 05:1x 代拟；方案线落进工单后请回一条"已落"）

工单里现在的 D2 行仍写旧数（`C-val 0.8102（interior 0.7212）/ C-test-1 0.8251 / C-test-2 0.5344`，p `0.0309 / 0.0584 / 0.0176`），这三组**从未被任何一手产物核到**（路线二 04:5x 自证：那些数只在对话文本里出现过）。请整行替换为：

```
| D2 | 新增（必做1 臂 C，传统基线表 5-10） | 新增表 + 一句「两边都不好看」的定性 | 零训练的 POD + 观测点最小二乘（与格1/格4 同观测表 obs_sparse_5pct.csv，n_pts=63）：C-val speed **0.828487** / p **0.018883** / 压降 0.011176；C-test-1 speed **0.531695** / p **0.058066** / 压降 0.020943；C-test-2 speed **0.611451** / p **0.044503** / 压降 0.015182（单次读数、无种子散布；数值取自 `docs/revision/证据-armC日志摘录-20260926.md`，其 JSON 产物在实例 NAS、未入本端点仓 ⇒ 强度记 **B 级·转引日志**，不写"已归档"也不写"未做"）。阶数敏感性**已跑**（`--max-modes 4/8` ⇒ rank=1..4）：speed 0.828487 → 0.829655 → 0.829780 → 0.829780（**变化 <0.16%，平坦**），p 0.018883 → 0.042304 → 0.047487 → 0.047511（绝对值仍 ≤0.05）⇒ 表注可写"阶数由保留 99.97% 快照能量的判据选出（r=1），且经 4/8 阶敏感性检验不改变速度场结论"；**平坦 ≠ 唯一，故不主张 r=1 唯一性**。两面都要写：速度场上它比 PINN 差 20–30 倍（53–83% 对稀疏档约 2.5%，test 口径），压力/压降场上与我们同量级（p 1.9–5.8%、压降 1.1–2.1%）⇒ "低阶模态方法学上成立但不构成竞争基线" |
```

同一批要一并改掉的：`§12 统计`里若仍引 0.8102 系列一并替换；`SWEEP_DRYRUN §11.1/§12.3` 那三处"已闭"标记**保留**（臂 C 确实跑过），只是产物归属统一为"**在实例 NAS、未入本端点仓**"。落完请回一句"已落 + 该 commit sha"，我 `git show` 复核后在 `状态-两线提交在本仓不可达-20260926.md` 里销账。

## 三、23:0x（北京 9/26；原标"05:3x"是假时钟，作废凭由见 `状态-两线提交在本仓不可达-20260926.md` §三十五）我撤回一句过重的话 + D2 终稿（含两次运行的归属，二选一由表注决定）

1. **撤回**：我在 `状态…md §二十九` 与上一条 §二里写的"旧那组 `0.8102 / 0.8251 / 0.5344` **从未被任何一手产物核到**"**过头了**。方案线 05:1x 指出其来源是 `out/armc_20260925.log`（2026-09-25 那次运行，统括官自己也在 20:35 读过）——**有来源，不是杜撰**。准确说法是：**那组数属 09-25 那次运行（逐工况观测点数不同：63 / 74 / 56），09-26 这次是同一张观测表 `obs_sparse_5pct.csv`、n_pts=63 的另一次运行。**
2. **为什么表 5-10 必须用 09-26 那组**：表 5-10 的立表前提是**同框**（与臂 A/臂 B/双模型同观测点、同 split、同 metric）。09-25 那次的 C-test-1/2 换了观测点集 ⇒ **与三臂不同框，进了同一张表就是拿两套输入互比**。⇒ 用 `0.828487 / 0.531695 / 0.611451`，并在表注里加一句归属：
   > 臂 C 为 2026-09-26 与三臂同观测表的一次运行（n_pts=63，单次读数、无种子散布；JSON 产物在实例 NAS、未入本端点仓 ⇒ 强度 B 级·转引日志）。另有一次 2026-09-25 运行（逐工况观测点数 63/74/56，speed 0.8102/0.8251/0.5344）**不与此表同框**，故不并入；阶数敏感性 `--max-modes 4/8`（rank=1..4）显示速度误差 0.828487→0.829780 平坦（<0.16%）⇒ 取 r=1 为默认阶，**不主张唯一性**。
3. **落盘状态（防再次各说各话）**：本端点 `origin/main = 557e014`，工单 D2 行经内容判定仍是旧组（含 `0.8102` 与 `n_obs=63/74/56`，无 `0.828487`）⇒ 方案线 04:5x/05:1x 报的"D2 已替换并 push"在这里**看不到**。终稿文本就是本节第 2 条，已定稿不需再造。**由谁落**：方案线再落一次；若它落不了，需用户点头由统括官整行替换（我不在正文里做规则性改动，只搬这段已定稿文本）。
   > **9/26 23:58 本节第 3 条作废（现场读数已变，别按它行动）**：上面那句里的 `origin/main = 557e014` 是当时快照。现查 `origin/main = 7c7840a`，工单 D2 已由**方案线自己**在 **`b62b8d9`（`git log %cI` = 2026-09-26 23:10:46）**落定稿（`0.828487` 命中 2、`不与此表同框` 命中 1、`0.018883` 命中 3）⇒ **"统括官代落"不必了**。同一批里我另补了一处谁都没标的漏网：`T5矩阵test口径读数-20260926.md:90` 仍把 09-25 那组当臂 C 表值、且写着"同一张 5% 观测表"（那次逐工况是 63/74/56）——已代改并标"不作表值/正本指回 D2"。
