# 修订方案 v2（2026-09-24）— PINN 微流控毕业论文修订【当前版】

> **本 v2 取代 `修订方案-v1-20260924.md`；被推翻的表述不在本文改写，原文完整保留在 `docs/revision/archived/修订方案-v1-20260924.md`（429 行，逐字节未改）。** v1→v2 的六处实质变更逐条列在 §0.2，每条标"原→新"与凭据。

- 作者：方案架构线。硬截止 **2026-09-30 修改完并结题**（今天 09-24 周四）。
- 输入：`毕业论文汇编格式.docx`（正文，纯文本投影 `.scratch/thesis.txt` 1282 段）、`发给-健彬.docx`（6 短板/5 必做/3 加分，文本 `.scratch/jianbin.txt`）、仓库 `pinn-platform-v4`（main，本文写作时 HEAD=`35c9e93`，已推到新远端 `revision` = `Ruler4396/pinn-thesis-revision`，`main` 追踪 `revision/main`；`origin` = 原 `pinn-platform-v4.git` 仍在）、`docs/revision/独立审计报告-v1-20260924.md`（下称【审计】）、`docs/revision/交叉复核-C1C2C3-20260924.md`（下称【交叉】）、`docs/revision/SWEEP_DRYRUN.md`（下称【DRY】）、T0 实例实测（用户回报）。
- 引用格式：`文件:行`、`文件#JSON字段`。**路径约定**：以 `results/…`、`scripts/…`、`src/…` 开头者相对 **`model/`**；以 `docs/…`、`api/…`、`web/…` 开头者相对**仓库根 `pinn-platform-v4/`**；附录 M 里 `RP=model/results/pinn`、`RS=model/results/supervised`。**未测的项一律写"未证实"，不外推。**
- 本轮只写文档：未改代码、未上实例。

---

## 0. 判决前置

### 0.1 六条复算/交叉判决（当前有效版）

| 项 | 当前判决 | 凭据（我自己复算 + 交叉线独立复算，两者读数零偏差） |
|---|---|---|
| **R1** | **【成立】双模型在流体内部点没有精度优势** | 同一 5% 观测、同 14 特征、同 128³ 子网，只算 `boundary_type=='interior'`（n=1261）：单网络 speed **0.0137** vs 双模型 **0.0314**；分区 r0 0.0145/0.0320(536)、r1 0.0070/0.0209(328)、r2 0.0172/0.0391(397)。机制：360 个 wall 行真值 \|u\|≤2.89e-30，双模型 Σu²=**4.411e-9**、单网络 **6.067**，两臂分母相同 ⇒ 全点集口径下双模型"占优"完全由壁面点灌出。补一条：**压力内部点也是单网络优**（0.0623 vs 0.0685），"只输在速度"这个辩护不存在。输入 `results/supervised/contraction_single_mlp_geometry_sparse5_strict_20260503/evaluations/predictions_val_dense/C-val_predictions.csv` 与 `results/pinn/contraction_strict_geometry_sparse5_stagepde_20260503/` 同名文件；指标定义照 `scripts/train_supervised.py:361-362,622-663`；脚本 `.scratch/plan_recheck_R1.py` |
| **R2** | **【结论成立，理由按 C1 更正】臂间变量是 2 个不是 4 个** | 见 §0.2 第 4 条与下面 §2 短板3/4 |
| **R3** | **【成立，且比【审计】更宽】收缩族被零训练解析式支配 1.4–2.8 个量级** | 9 个工况全量：u **0.000190–0.001414**、speed 0.000189–0.001321、p 0.000243–0.000942、压降 0.000078–0.000855。对论文读数：C-test-1 speed 0.0162/0.000722=**22.4×**、p 0.0492/0.000666=**73.8×**、压降 0.0550/0.000521=**105×**；C-test-2 压降 0.3331/0.000547=**609×**。解析式真值压降对得极紧：415.06→414.87、1069.17→1068.62、1957.87→1956.80。式子自己推（`∇p=∇²u`、μ_无量纲=1，与 `src/data/contraction_freefem.py:91-102` 同口径）：u=1.5/W\*·(1−(2y/W\*)²)、v=0、−dp/dx=12/W\*³、p(Lout)=0；W\*(x) 复用 `src/data/contraction_geometry.py:39-49` 同一条 smoothstep5 ⇒ 几何偏差项=0。脚本 `.scratch/recompute_R3_analytic.py`，与【交叉】C3 表（`:145-157`）**逐位相同** |
| **R3-附加（原"换主战场"论据）** | **【已被 C2 推翻，v2 撤回】弯曲族只报"分区误差 + 准一维近似在何处失效"，不得写"PINN 在弧段赢了"** | 我原来的读数（弧段解析 0.0357 vs 双模型 0.0159 ⇒ "PINN 优 2.2×"）被【交叉】逐位复现（0.035700/0.015914），**但基线太弱**：【交叉】自写的三参数曲率剖面 F（`A(1−\|t\|^m)(1+S·t)`，参数只拟合于 4 个训练工况弧段、**零观测点、零 PDE、零 CFD**）在弧段拿 **0.0133（B-val）/0.0192（B-test-1）**，**追平并反超**模型的 0.0159/0.0236；拟合出的 m 停在 2.00，0.0357→0.0133 全部由一个 +6% 的反对称倾斜项换来（【交叉】`:94,:101`）。弧段**压力**解析仍优 **39.8×**（0.000956 vs 0.038017）/ B-test-1 优 **62×**（0.001176 vs 0.073123）。⇒ 结论：**不换主战场**；弯曲族值得报的是分区误差与非准一维位置，不是精度优势（【交叉】`:16,:126-133`） |
| **R4（新）** | **【成立】单次读数的对比全部落在换一种子的摆动带内** | T0 实测：同一配置 seed 42→43，C-val speed **0.0320841→0.0207178（−35.4%）**、p **0.0725301→0.0893818（+23.2%）**；而表5-5 判胜负的两臂差只有 **23.9%**（0.0321 vs 0.0259，`thesis.txt:1102,1107`）。35.4% > 23.9% ⇒ **现有任何单次读数对比都不足以判方向**。这条取代 v1 里"n=1 无统计"的抽象说法，成为 §5 的出发点 |
| **R5（新）** | **【成立】seed=42 可跨环境逐位复现** | T0 在实例端（Python 3.11.11 / torch 2.3.1+cpu / numpy 2.2.6，不同主机、不同 BLAS）重跑 `contraction_strict_geometry_sparse5` 那一格，四个指标与仓库落盘**逐位一致（delta=0）**：u 0.03208569064736366 / v 0.09887264668941498 / p 0.0725301206111908 / speed 0.03208403289318085（对齐 `results/pinn/contraction_strict_geometry_sparse5_stagepde_20260503/metrics.json#最终验证指标`）。⇒ 4.3 节"固定种子便于复现"从**声称升级为证据**；但**同一批数据仍不能说明"结果可重复"**（R4），两句必须分开写 |

### 0.2 v1 → v2 的六处实质变更（逐条"原→新"）

| # | v1 的说法（原） | v2 的说法（新） | 凭据 |
|---|---|---|---|
| 1 | §0 "R3-附加：换主战场到弯曲族弧段是一条不升 Re 也能让方法显出净增量的活路" | **不换主战场**。弯曲族报"分区误差 + 准一维近似失效位置"；F/G 曲率修正剖面**正式进基线清单**（§6.3） | 【交叉】C2（`:89-101,:126-133`）；判决见本表 R3-附加 |
| 2 | §0/§4 "G0 待做；复现判据 `≤1e-3`；墙钟按原主机 59.52/98.80 s 外推" | **G0 已关闭**：复现 delta=0（强于 1e-3）；实例单价 **train 55.145 s / eval 1.48–1.59 s，k=0.9265**；T5+T6 重算为**串行 ≈121 min（2 段）/ 6 路并行 ≈27 min（1 段）**；镜像**无 `/usr/bin/time`、无 `bc`** ⇒ 新墙钟与论文 4.2/5.7 引用的旧口径不同，**表注必须声明** | T0 实测（用户回报）+【DRY】§0/§1 |
| 3 | §0/§11 说 R2 的臂间变量"是 2 个，审计写 4 个属过度"，并保留"basic 臂无压力锚定"的余地 | 固定为：**臂间变量 = 特征 4↔14 + 壁面 soft↔hard（后者由前者派生）**；`--inlet-flux-weight/--outlet-pressure-weight/--pressure-drop-weight=0` 是**整批 strict 15 条训练命令的共同配置**（桩化实跑 44 条 argv 为凭）。**"basic 臂无压力锚定"这句从全部文字中删除**（答辩会被挑软）；表4-4 定性固定为**"与第五章不同源（串档）"，修法=新增表4-4b + 正文交代适用范围，一个数字都不删** | 【交叉】C1（`:22-45,:187-188`）；`run_strict_sparse_experiments.sh:29-32,59-66`；`train_*_strict_sparse.py:221-225,603,731` |
| 4 | §5 "n=5 最小可达 p=0.0625 故不做显著性声称"（只有理论论证） | 与 R4 的实测噪声合并，结论句写死：**"现有单次读数的对比全部落在换一种子的摆动带内，因此显著性只能由 T6 的 8 配对单元承担，其余格只报 mean±std"**；并给出 F vs PINN 的 17% 差同样低于抖动 ⇒ 弧段"优于"在多种子前不得写 | T0 seed43 读数；【交叉】`:129` |
| 5 | §11 的 M1/M2/M3 与 D1 作为"新发现"陈述 | 全部降级为**已定项**并给执行体：M1→T7-②、M2→T7-③、M3→T2/`analyze_sweep.py` 已实现两口径、D1(B-test-2)→T7-① 一次 eval。**T7 三项完成时限 9/26** | §3、§4 排期 |
| 6 | §10 列 3 个待拍板问题（G1 选项、实例凭据、措辞） | 只剩 **2 个真问题**：(a) 路线甲 vs 甲+乙，**判决交给一个 30 min 探针**（只出 NS 真值、不训模型，把解析/F/G 基线搬到 Re=10/50 重打分看是否失效）；(b) 摘要是否允许"在收缩族上不主张精度优势"这句。**G0、传输通道、环境版本三项已解决，删除** | T0 已关 G0/通道（Jupyter Contents API 16.7 MB/65 s，sha256 一致）/numpy 2.2.6 已定 |

---

## 1. 目标与验收定义（9/30 交什么才算结题）

三件同时到位才算，缺一件不算：

1. **修订版论文**。判据：
   - 【审计】第 0 屏 14 条 + 【交叉】更正清单 4 条，每条能指到"已改/已删/已重跑"三态之一，并指向 §11 台账行。
   - 正文每个误差数字都能在 §附录M 指到 `run 目录 + 文件#字段`；指不到的改成定性或删除。
   - 声称强度 ≤ 测量强度：n=1 的读数不得写成"表明"；每个 p 值旁边必须有 n 与"最小可达 p"。
   - 4.3 节把 R5（跨环境 delta=0）写成证据、把 R4（换种子 −35.4%）写成限制，**两句不许合并成"结果可复现"一句**。
2. **逐条回复表**：6 短板 + 5 必做 + 3 加分 = 14 行，每行四格（审稿原话 / 处置档位 a改写·b重跑·c撤回 / 落盘章表号 / 未做的代价与延期申请）。
3. **可复现仓库 tag**（当前 `git tag -l` 为空）：annotated tag `v4.1-revised-20260930`；tag 状态下核对闸门退出码 0；`model/requirements.txt` 补本次真用到的依赖（scipy；FreeFem++ 实际版本写进 `docs/ENV_LOCK.md` —— 论文称 4.9，仓库零处记录）；正文出现代码/数据可得性声明（现 0 次，全文"开源"仅指 FreeFEM，`thesis.txt:313`）。

**硬前置**：新 run 产物只在实例里，实例回收即消失 ⇒ 第 3 件是第 1、2 件的前置：**没 push 回持久层的 run，论文里不能引用。**

---

## 2. 六条短板的处置档位（(a)改写 / (b)重跑 / (c)撤回）

> 不默认"补实验更便宜"。(a) 档 0 机时纯写作；(b) 档要机时 + 一次会话排队；(c) 档 0 机时但降论文档位。**最要命的两条（R1、R2）已经是 0 机时**——用仓库现成产物复算就是反的，缺的是改写不是实验。

| 短板 | 最低成本结案档位 | 内容 | 映射 | 为什么不是"先补实验" |
|---|---|---|---|---|
| 1 物理过于简单（线性 Stokes 为何用 PINN） | **(b) 必须重跑，但先跑 0 机时的探针** | 先把 R3 的解析打分做成表（脚本已在 `.scratch`，需固化进 `scripts/baselines_classical.py`）；再做 **30 min NS 探针**决定路线甲/甲+乙（§10a） | 必做3、加分1 | 不升 Re 就必须承认"演示"定位；而升 Re 之前必须先知道**解析/曲率基线在有限 Re 下是否失效**，否则可能花 3 天做出一个仍被零训练式子打败的新表 |
| 2 纯合成闭环 | **(a)+(b) 混合，(b) 只在数值侧** | (a) 3.1/5.x 显式写"真值/训练/评估同源"；(b) 交叉验证走**网格无关性 + 第二离散格式 + 解析极限**（T11，分钟级）。不碰 COMSOL/µPIV | 加分1 | 6 天内 µPIV 不可达；"无第二求解器"最便宜的可交付替代是第二格式对拍，不是重训 |
| 3 缺基线对照 | **(c)+(a) 为主，(b) 只补两臂** | 单网络臂已有产物（R1 已复算）⇒ 5.8 改写为"未观察到正增益"，0 机时；真缺的是**单网络联合 PINN（等参数量+等预算）**与**POD/插值/曲率剖面**⇒ T8/T10 | 必做1 | 补"单网络纯监督"是白花钱（已有）；先把现成产物算干净再决定花那 12 min |
| 4 创新点出现负结果（分层采样） | **(b) 必须重跑，且要重跑点位** | 两批次方向相反已确证：strict 批分层压力占优 0.0725<0.1021（`thesis.txt:1094-1119`）；v4 批**三项指标均匀全优**（`results/field_map_checks/chapter5_materials/contraction_regionaware_vs_uniform5_notes.txt`：val 0.0234<0.0289、0.1199<0.1255、0.0956<0.1041）。两臂批次级差异之一正是压降损失开关（C1） | 必做5 | 改写不能结案：正文现在两头骑墙，且同仓有反例 |
| 5 单种子单次 | **(b) 必须重跑**，无替代 | 41 个 `config.json#seed` 全 = 42；抽样侧也固定（`sparse_sampling.py:16`、`meta.json#sampling.seed`）⇒ 全库 0 重复。R4 已给出单次对比不可判的定量证据 | 必做2 | — |
| 6 效率未量化 | **(a) 改写 + (b) 分钟级重测** | 论文那组数无来源（`thesis.txt:1193-1195`）；唯一 artifact 是 6.04/2.19/30.72/9.34×（`docs/benchmarks/pinn_vs_cfd_speed_benchmark_20260420.md:14-17`）。真正的破口不是来源而是**训练不入账**：`benchmark_pinn_vs_cfd.py:127-155` 从不计训练时间 ⇒ 拐点 K\*=17–236（§7） | 必做4 | 按审稿人原话"补一张表"会补出一张仍然错的表 |
| 加分2 更复杂几何 | 转**已定项 D1**（转角泛化补测或删声称，§2 末） | 不新建 T 型/蛇形（3–5 周不可达） | 加分2 | — |
| 加分3 文献与开源 | **(a) 纯改写** | 参考文献现 **18 条**（`thesis.txt:1265-1282`）→ 需 40–60；候选清单已在 `docs/revision/候选文献-20260924.md` | 加分3 | 0 机时，全部条目里性价比最高的加分项 |

### 已定项（不进档位讨论，直接执行）

**D1 · 转角泛化实际未测** — 统括官已核、我独立复核成立：全仓 `B-test-2` 只出现在早期纯监督 run（`results/supervised/bend_supervised_geometry_v1_20260331/evaluations/metrics_test.json`：speed 0.0627 / p 0.2010 / 压降 0.1123）；**所有双模型 run 的评估清单没有它**（遍历 `results/pinn/*/evaluations/manifest_*.json#eval_cases`，弯道只出现 `B-val__ip_blunted`、`B-test-1__ip_blunted`）。而 `thesis.txt:411,414`（表3-3）把 B-test-2（Rc/W=4.0、θ=60°）列为"转角泛化测试工况"，表5-7（`:1165-1182`）只有 C-test-1/C-test-2/B-test-1__ip_blunted。
二选一，**默认走补测**（T7-①，一次 eval）：`cases/bend_2d/data/B-test-2/field_dense.csv` 已在，抛物入口双模型 `results/pinn/bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419/best.ckpt` 已在（train=B-base,B-train-1..3，θ=90°），按 T0 评估单价 **1.48–1.59 s/次** ⇒ 机时忽略不计。**必须写明**：只有抛物入口版本、无 `B-test-2__ip_blunted`，所以只能用抛物主线评——否则又成跨批次比。

**D2 · 采样策略两批次方向相反** — 见短板4，归 (b)，重跑矩阵 = T6（§5）。

---

## 3. 任务矩阵 T0–T14

墙钟单价（**v2 已按 T0 换成实例读数**；旧主机落盘值保留作对照）：

| 单价 | v2 采用值 | 出处 |
|---|---|---|
| 双模型稀疏 5% 单次训练 | **55.145 s**（实例，k=0.9265）/ 59.52 s（旧主机） | T0 实测；`results/pinn/contraction_independent_geometry_sparse5clean_stagepde_v4.log:46 Elapsed 0:59.52` |
| 双模型稠密单次训练 | **91.9 s**（=98.80×k） | `…_stagepde_mainline_v4.log:46 Elapsed 1:38.80` × k |
| 单次离线评估 | **1.48–1.59 s**（旧落盘 1.52–3.95 s） | T0；`…_eval_{val,test}.log:41` |
| 一格的单元成本 | 收缩稀疏 **58.2 s**、收缩稠密 **94.6 s**、弯曲 **75.7 s（未标定占位）** | 训练 + 2 次评估 |
| 峰值内存 | 664 MB/进程 ⇒ 8 核可 6 路并行 | `…sparse5clean_stagepde_v4.log` `Maximum resident set size 664060 KB` |

**T5+T6 总墙钟：串行 ≈121 min ⇒ 2 个 90 min 段；6 路并行 ≈27 min ⇒ 1 段。**（脚本未覆盖 env 时按旧单价打印 ≈124 min，两者相差 <4%，属保守侧。已知粗估处：`sweep_t5.sh:unit_cost()` 对弯曲稠密格也套 `BEND_UNIT_SEC`，5 个单元合计低估约 1.2 min，标定时顺手校。）

| ID | 档 | 状态 | 输入产物 | 动作 | 通过判据 | 输出文件 | 依赖 | 预估墙钟 | 负责会话 |
|---|---|---|---|---|---|---|---|---|---|
| **T0** | 前置 | **已完成** | 仓库+实例 | 环境冒烟、单价标定、seed=42 对账 | 已达成：delta=0（R5）；k=0.9265；通道 Jupyter Contents API 16.7 MB/65 s | T0 回报（用户） | — | 已耗 | 实验线 |
| **T1** | a | 待做（0 机时） | `thesis.txt`、41 run 产物、`docs/revision/SWEEP_DRYRUN.md` 附录M 行 | 写"论文数字↔产物"核对闸门：附录M 每格一条断言，读产物重算并比对；**反向验证**（造改错数字的副本，确认每条能红） | 断言数=附录M 行数；末行打印 `N assertions across M tables`；正对照必红；三态 `PASS / DERIVED-ONLY,NO RUN DIR / MISSING-CREDENTIAL`，后两态保持红不许删断言 | `model/scripts/verify_tables.sh`、`docs/TABLE_PROVENANCE.json` | — | 本地 1.5–2 h 人工，**0 机时** | 工程线 |
| **T2** | a | 待做（0 机时） | 表4-2~4-8、2.2/2.3/4.3/5.x/6.x | 口径改写包：①表4-4→**4-4a(0.5/1e-4/1.0)+4-4b(0/0/0)**，一个数字不改；②每表标明速度列是 `rel_l2_u` 还是 `rel_l2_speed`；③test 平均声明为 **mean-of-cases**（非 pooled）；④每表加"观测点数"列（收缩 13/63/126/189；弯曲 76/382/763/1145）；⑤壁面残余改称构造性指标；⑥在线链路与离线方法分节；⑦流体参数=线性重标 | 附录M 的 21 处口径标注全落地；"壁面速度残余"出现处 100% 带"由输出构造恒等保证"；6.4 出现"ρ 不改变场形状"；**全文 grep 不得再出现"无压力锚定"** | 论文 2/3/4/5/6 章对应段 | — | **0 机时**，4–6 h 人工 | 论文线 |
| **T3** | c | 待做（0 机时） | 【审计】第0屏、【交叉】C1/C2 | 撤回包：创新点1"有效避免干扰"、5.6.2 的 11.7598→0.1516、2.3"更深网络未必稳定"、4.5"只改变一个主要因素"、"国内…寥寥无几"、5.7"十分显著"、**任何"弧段 PINN 赢了"的措辞** | 7 处原文均被替换/标注作废，并在回复表各有 1 行交代改成什么 | 论文对应段 + 回复表 | T2 | **0 机时**，1–2 h | 论文线 |
| **T4** | b | **代码已入库** `35c9e93` | `src/data/sparse_sampling.py`、`cases/*/data/*` | 观测生成参数化到 obs_seed（`generate_observations_seeded.py`） | 实例上 `--obs-seeds 0 --verify-committed` 必须**全 IDENTICAL**（否则抽样语义被动过，整套 obs_seed 作废）；两臂总点数相同断言通过 | `cases/…/obs_*__s{1,2,3}.csv`、`docs/OBS_MANIFEST.csv` | T0 | 生成 **<5 min** | 工程线→实验线 |
| **T5** | b | **代码已入库，待实例执行** | T4 的 obs 集、两族几何 | 19 格 × 5 训练种子主矩阵（`sweep_t5.sh`，格表见 §5 表M1） | 每格 n=5；新 run 的 `config.json#seed` 取值集合 = {42..46}；T1 闸门重跑后 21 格指向新 run；**表5-8 不启用行在本矩阵里有 run 产物** | 95 个 run 目录 | T4 | 串行 **110 min**（含 T6）；6 路并行 **≈25 min** | 实验线 |
| **T6** | b | 同上 | T5 的格4/格8 | 采样策略配对矩阵：{分层,均匀}×4 obs_seed×2 train_seed = **8 配对单元**（与 T5 同名去重，只新增 12 单元） | 配对单位 `(obs_seed,train_seed)` 落盘；两臂共享 obs_seed；输出显式打印**最小可达双侧 p=0.0078** | `docs/revision/T5_T6_stats.json` | T5 | **12 min 串行** | 实验线 |
| **T7** | b | 待实例执行 | 已有 ckpt/真值 | **三格补齐**：① D1 B-test-2 一次 eval；② 表5-8 不启用阶段内 PDE 臂（`velocity_stage_continuity=0, pressure_stage_momentum=0`）×5 seed；③ 编码单变量干净臂 basic+soft / geometry+soft ×3 seed | ① 评估清单出现 `B-test-2` 行；② 新 run `config.json#权重.速度阶段连续性=0` 断言通过（现全仓无此取值）；③ 两臂 `速度壁面约束.mode` 逐字相同 | 3 组 run/eval 产物 | T0 | ①**2 s**；②5×91.9≈7.7 min；③6×55≈5.5 min；**合计 <15 min**；**时限 9/26** | 实验线 |
| **T8** | b | 待写代码+执行 | `train_*_strict_sparse.py` | 单网络**联合** PINN 基线：14→183×3→3，同 Stokes 残差、同观测、同物理点数 | 落盘 `n_params` 与双模型 70,275 之差 **≤1%**；**每网络更新次数=280 且末轮检查点**（消除【审计】C.4 的预算不对等）；两族各 5 seed | `scripts/train_single_joint_pinn.py` + 10 run | T5 | 参数量核对 0 机时；10×~65 s≈**11 min** | 工程线→实验线 |
| **T9** | b | 部分已做（R1 已复算） | `train_supervised.py` | 纯数据 MLP 补口径：输出 interior/wall/inlet/outlet 与分区读数，补 3 seed | 复算命中我本轮读数（0.0137/0.0314）；**`rel_l2_v` 单独列出**（该 run 全点集 0.5358 被 speed 口径遮蔽） | `docs/baselines/mlp_segmented.json` | T1 | 复算 0 机时；3×~20 s | 工程线 |
| **T10** | b | 待写代码 | 【DRY】+ 我的解析脚本、`api/pinn_platform_api.py:891-901` | **经典基线四臂**：① Gaussian RBF 直接拟合观测；② POD 降阶；③ 零训练解析（局部 Poiseuille+润滑）；④**曲率修正剖面 F/G**（v2 新增，见 §6.3） | 四臂吃同一 obs CSV、同一 `relative_l2`；RBF 与在线链路的"先验+残差校正"区别写进表注；POD 的 m 与累计能量占比落盘；**F/G 的拟合预算与 PINN 对齐**（F 只用 4 个训练工况、零观测点，须注明 m=2.00、只有 +6% 倾斜项起作用）；无种子臂的方差只来自观测重抽，表注必须写 | `scripts/baselines_classical.py` + `docs/baselines/{rbf,pod,analytic,curvature}.json` | T4 | **<3 min** | 工程线 |
| **T11** | b | 待实例 | `src/data/*_freefem.py` | 交叉验证三件套：网格加密一档、第二离散格式、与解析极限对拍 | 压降随网格加密相对变化 <2%（否则效率表并列两个网格）；解析 vs FreeFem 差在 R3 量级 | `docs/validation/mesh_study.csv` 等 | T0 | FreeFem++ 安装 <10 min；求解 <10 min（**未证实**：段数翻倍按 1.24 次幂放大估） | 实验线 |
| **T12** | b | **探针版先行（30 min）** | 同上 | **NS 探针**：`.edp` 加对流项按 Re 参数化，**只出真值不训模型**，Re∈{1,10,50}；把解析/F/G 三臂搬到 NS 场上重新打分 | 每档落盘 `‖(u·∇)u‖/‖∇²u‖`；若某档基线仍 ≤模型误差 ⇒ 明写"该档无信息量"；探针结果直接决定 §10(a) 走甲还是甲+乙 | `cases_ns/…`、`docs/validation/stokes_vs_ns.csv`、`docs/validation/baselines_on_ns.csv` | T11 | 真值+打分 **≈30 min**（**未证实**倍率） | 实验线 |
| **T13** | b | 仅当 §10(a) 选甲+乙 | T12 的 NS 真值 | NS 上重开对照：{双模型,联合单网络,MLP,RBF,解析,F/G} × Re × 两族 × 3 seed | 结论只允许三写法之一且须由该表数字直接支持（§9 情形1/3/4） | `results/pinn_ns/**` + 论文 5.10 | T8/T10/T12 | 60 run ⇒ 串行 **1.0–1.8 h**；并行 **0.3 h** | 实验线 |
| **T14** | a+b | 待做 | 全部产物 | 效率章节重做（§7 三列表+拐点）+ 交付物组装（tag、回复表、文献 40–60、可得性声明、ENV_LOCK） | 效率表必含"训练时间"与"盈亏平衡工况数"两列；tag 可 checkout 且 T1 全绿；参考文献 ≥40 | 论文 5.7 + §1 三件交付物 | 全部 | 实测 **<5 min**；写作 0.5–1 天 | 工程线+论文线 |

**成本对比**：(a) T1/T2/T3 + T14 写作 = **0 机时 / 1.5–2 天人工**；(b) T4–T13 = 串行 **3.5–5.5 h**、6 路并行 **0.9–1.4 h**（不含 T13）；(c) T3 内 7 条 = 0 机时但降档位。⇒ **机时不是瓶颈，会话排队窗口与写作工时才是**；所以 (a) 排在 (b) 之前。

---

## 4. 排期与闸门

北京时间。今天 09-24（周四），截止 09-30（周三）。

| 日期 | 主线 | 并行 |
|---|---|---|
| 9/24 今天 | **G0 已关**（T0 完成）。本地做 T1/T2/T3；T4 的 `--verify-committed` 待实例 | 归档/实例凭据 |
| 9/25 | **G1 判定日**：T11 FreeFem++ + 网格无关性 → **T12 探针（30 min）** → §10(a) 定路线 | T4 实例校验；(a) 档继续 |
| 9/26 | 第 1 次实例作业（≤90 min）：**T5 全矩阵 + T7 三格补齐（T7 时限）** | T8/T9/T10 基线开发 |
| 9/27 | **G2 判定日（已判，见下注）**：T6 配对矩阵 + 检验落盘 ⇒ 采样策略判决 | T13（若甲+乙）；T14 效率实测 |
| 9/28 | 数字全部落表；(c) 撤回与 (a) 改写终合并；回复表成稿 | T1 闸门全仓重跑 |
| 9/29 | 冻结：tag、`TABLE_PROVENANCE.json`、送审格式；**留 1 次实例配额做补救** | — |
| 9/30 | 交题（缓冲日，不排新任务） | — |

  **【9/27 00:0x 已判，载体换了 —— 方案线提醒的那条 stale 排期行，指向上表 **9/27 那一行**】** 那一行**不必再执行**。G2 的判决 9/26 已按事先写死的规则落定，但**不是**靠跑「T6 8 配对单元 + `T5_T6_stats.json`」——那个 JSON **从未生成**（`git ls-files` 里 grep 不到）。实际凭据是 T5 五种子矩阵 + T6 多点位（`t5c04`/`t5c08` 取全部 obs_seed ⇒ **n=11**）的配对检验：**Wilcoxon W=25.0、双侧 p=0.519531；配对 t=−0.958、p=0.360848**（出处 `T5矩阵test口径读数-20260926.md:57`）⇒ 命中规则的"**否则**"分支 ⇒ 表 5-5 **不写**"分层在压力上占优"，改为**一条随稀疏度衰减的曲线**（工单 H2/H4/H5，见 `:153`/`:155`）。同格并行项现状：**T13 已完成**（独立效度审计，创新点二定档 B）；**T14 效率实测只做了一半**——E5 同机 CFD 单价因实例通道未通**仍未做**，所以表 5-9 与 E3 的"混合口径"限定句**保留**，不许写成"同机同次"。

### G0 · 环境冒烟 —— **已于 9/24 关闭**
- 达成：① seed=42 复算四指标 **delta=0**（判据原为 ≤1e-3）；② k=0.9265；③ 传输走 Jupyter Contents API，16.7 MB / ≈65 s（0.27 MB/s），sha256 逐位一致；④ 环境 Python 3.11.11 / torch 2.3.1+cpu（仓库钉 2.11.0+cpu）/ numpy 2.2.6。
- **遗留两条**：① torch 版本与 `requirements.txt` 钉的不一致 ⇒ 写进 `docs/ENV_LOCK.md` 并在 4.3 表声明；② 镜像无 `/usr/bin/time`、无 `bc` ⇒ 新旧墙钟口径不同，**表注必须声明**（R5 只证"同一份代码两次跑逐位一样"，不是"环境相同"）。

### G1 · 真值口径（9/25 判定，必须最早）
- **选项**：A（推荐）第五章主体保持 Stokes + 新增 5.10"有限 Re 对照"，**弯曲族只报分区误差与准一维失效位置（v2 已撤回"换主战场"）**；B 全量换 NS（41 旧 run 全废，9/30 不可达）；C 只做 Stokes-vs-NS 差异定量（必做3 部分交付）；D 不升，把"低雷诺数"论证补成定量（Re=0.0224，`meta.json#flow.Re`，与 `thesis.txt:415` 一致）并承认解析基线支配。
- **通过条件（判 A）**：① FreeFem++ 实例内可执行且版本落盘；② Re=10/50 的 NS 解收敛且 `‖(u·∇)u‖/‖∇²u‖ ≥ 0.05`；③ **T12 探针显示解析/F/G 在 Re=10 或 50 上失效（误差量级跳到 ≥1e-2）**；④ T11 网格无关性通过。
- **不通过的降级**：③ 不成立 ⇒ 走 C 或 D，**且不得声称"升到有限 Re 就需要本方法"**（那又是一次过度声称）；① 失败 ⇒ 走 C（连细网格 Stokes 都跑不了则 D）；④ 失败 ⇒ 效率表并列两个网格。

### G2 · 多种子数据裁决（9/27）
> **【G2 已判（9/26 判、9/27 标注）】** 判据一字未改，结论走事先写死的“否则”分支；读数与理由见上表之后那条注。本小节保留作预注册原文审计，**不要再按“9/27 去跑 8 配对单元”执行**。
- **通过条件**：T5/T6 产物齐且已 push；`T5_T6_stats.json` 落盘并带"最小可达 p"字段。
- **事先写死的判决规则**：分层 vs 均匀以 T6 的 8 配对单元 Wilcoxon 为准 —— p<0.05 **且**方向与 strict 批一致 ⇒ 保留"分层在压力上占优"并附 n 与 p；否则整节改"未观察到稳定差异 + 归因（批次级差异含压降损失开关）"。其余 19 格：mean±std 直接替换单次读数；某格 std 跨过两臂差 ⇒ 该对比句降级为"差异不超换一次种子的波动"（R4 已给出量级 35.4%）。
- **不通过的降级**：产物缺 ⇒ 该格退回单次读数并在回复表标"未完成（缺机时/缺产物）"，**不许用旧单次数冒领新数**。

---

## 5. 多种子统计方案

### 表 M1 · T5 的 19 格（`sweep_t5.sh:CELLS`，与代码逐项对应）

| # | 配置 | 族 | 观测源 | 监督 | 对应表 | 单元成本 |
|---|---|---|---|---|---|---|
| 1 | 稠密主线（stage PDE 开） | contraction | dense | dense | 表5-2/5-3/5-7 C行/5-8 启用行 | 94.6 s |
| 2 | 稠密·阶段内 PDE 关 | contraction | dense | dense | 表5-8 第二行（现无一手产物） | 94.6 s |
| 3–6 | 分层 1/5/10/15% | contraction | `obs_sparse_*` | 稀疏 | 表5-1/5-4/5-5/5-6 | 58.2 s |
| 7–10 | 均匀 1/5/10/15% | contraction | `obs_uniform_*` | 稀疏 | 表5-5 | 58.2 s |
| 11–12 | 分层5%+噪声 1%/5% | contraction | `obs_sparse_5pct_noise_*` | 稀疏 | 补 4.4.3 承诺的三档 | 58.2 s |
| 13 | basic 4 特征（**只能 soft**） | contraction | `obs_sparse_5pct` | 稀疏 | 消融 A 臂 | 58.2 s |
| 14 | geometry 14 特征 + soft | contraction | `obs_sparse_5pct` | 稀疏 | 消融 B 臂（与 13 只差特征集） | 58.2 s |
| 15–18 | 弯曲 分层 1/5/10/15% | bend | `obs_sparse_*` | 稀疏 | 表5-4/5-7 B行 | 75.7 s（未标定） |
| 19 | 弯曲稠密（同批次对照） | bend | dense | dense | 修【审计】#7"5%≈稠密"跨批次比 | 75.7 s（未标定） |

格 13/14 的存在理由（v2 新增）：硬包络要求特征里有 `wall_distance_frac`，`train_*_strict_sparse.py:171-173` 缺之即 `raise ValueError` ⇒ **basic+hard 在代码上不存在**，这正是原启动器给 basic 开 soft 的原因（`:29-32`）。消融因此报三格：basic+soft / geometry+soft / geometry+hard(=格4)，把"特征"与"壁面构造"两个效应分开。

### 种子与配对
- **两层种子分开**（`sparse_sampling.py:16,26,39,57` + `meta.json#sampling.seed` 的必然结论）：`train_seed∈{42..46}` 只改初始化/批序/物理配点（`train_*_strict_sparse.py:105-108`）；`obs_seed∈{0..3}` 改**点位实现**（现库全部是 42 的一份实现）。只换训练种子的"多种子"**测不到采样噪声**，而采样噪声正是分层 vs 均匀这条结论的主方差源。
- 表 M1 的 19 格：n=5（train_seed，obs_seed=0），**只报 mean±std，不做显著性声称**。
- 采样策略（表5-5）：8 配对单元 = 4 obs_seed × 2 train_seed，配对单位 `(obs_seed, train_seed)`，两臂共享 obs_seed。
- **两条硬限制写进正文**：① **n=5 时配对 Wilcoxon 最小可达双侧 p = 2/2⁵ = 0.0625 > 0.05，数学上不可能显著**；显著性只由 n=8（最小可达 p=0.0078）承担。② **R4 实测**：仅换 seed 42→43，speed 变化 **−35.4%**、p **+23.2%**，而表5-5 两臂差只有 **23.9%** ⇒ 固定结论句：**"现有单次读数的对比全部落在换一种子的摆动带内，因此显著性只能由 T6 的 8 配对单元承担，其余格只报 mean±std。"** 同理 F vs PINN 弧段的差（0.0133 vs 0.0159，17%）低于抖动 ⇒ 多种子前不得写"优于"。
- 检验：Wilcoxon 符号秩（双侧）为主 + 配对 t 并列；不一致以 Wilcoxon 为准并披露两者。区间用 bootstrap，**重抽单位 = 配对单元**。C-test-1/C-test-2 是 2 个独立几何对象，报 "n=2 geometries"，不参与显著性声称。scipy 缺失时 `analyze_sweep.py` 明确报错、**不降级手算**；注意 scipy/numpy 版本耦合（**最低兼容版本我未核**，以实例 pip 解析为准）。

---

## 6. 基线清单（v2 从"三件套"扩到四臂）

共用：同一 obs CSV、同一 `relative_l2`（`train_supervised.py:361-362`）、同一 `mean_rel_l2_*`/`pressure_drop_rel_error`（`:622-663`）、同一报告切分（全点集 / interior / r0-r1-r2 / 按 boundary_type），表注声明 **n（观测点数）/ 训练步 / 有无 PDE / 有无壁面构造 / 是 mean-of-cases 还是 pooled**。

### 6.1 单网络联合 PINN（T8）
`14→H,H,H→3`；参数量对齐算法 `P(H)=2H²+20H+3`，目标 `P*=70,275`（速度子网 14→128³→2=35,202 + 压力子网 14→128³→1=35,073）⇒ **H=183，P=70,641，偏差 +0.52%**（判据 ≤1%，断言落盘）。**预算对齐**：每网络更新 280 次（=200+80）、末轮检查点、无早停 —— 现有 strict 单网络 run 是 1183 最优轮/1383 停（`run_strict_sparse_experiments.sh:120-121`、`metrics.json#best_epoch`），三处不对等（【审计】C.4）必须消除。

### 6.2 纯数据 MLP（T9）
与 6.1 **只差 PDE 项**（同 H=183×3、同预算、同末轮规则、同 strict 归一化器、同 63 点）。现有 run 保留为"历史读数"，不作可比基线。强制披露 `rel_l2_v`（该 run 全点集 0.5358 vs 内部点 0.0495）。

### 6.3 经典重建与降阶 —— 四臂
1. **零训练解析**（局部 Poiseuille + 润滑压降）：R3 已给全量读数，**必须限定"收缩族及弯曲族直段"**（弧段方向相反，C2）。
2. **曲率修正剖面 F/G**（v2 新增，原 v1 没有）：F = `A(1−|t|^m)(1+S·t)`，参数只拟合于 4 个训练工况弧段、零观测点；G = 25 桶剖面查找表。**报告要求**：与 PINN 的拟合预算并列声明；注明 F 的 m 拟合停在 2.00、0.0357→0.0133 全部由一个 +6% 倾斜项贡献。**它们的角色是"抑制过度声称"的对照**，不是给 PINN 让路。
3. **Gaussian RBF 直接拟合观测**：`φ(r)=exp(−r²/ε²)`，ε 与岭系数沿用 `api/pinn_platform_api.py:891-901,920-924` 同族规则以便对比说明；**锚点=全部观测点、不做 K-center 次采样**；**不得复用在线链路的"先验场+残差校正"**（那需要一份已有基准场、恰好是用模型预测当先验，是最不该冒充传统基线的装置）。
4. **POD 降阶**：快照=6 个训练工况 interior 的 (u,v,p)，按分量拼列 SVD；m = 累计能量 ≥99% 的最小阶（m 与占比落盘）；系数由该工况 63 个观测点最小二乘定；**测试工况不进快照集**。预先承认：收缩族准一维 ⇒ 前 2–3 阶就吃掉绝大部分能量，POD 很可能大幅优于 PINN（§9 情形3），**这是该报的结论不是该躲的问题**。

---

## 7. 效率章节重做（表5-7a 三列表 + 摊销）

### 7.1 为什么整表必须重做
① 论文那组数无来源（`thesis.txt:1193-1195`）vs 唯一 artifact 0.0829/0.2283/0.5009、0.1184/0.3897/3.6380 ⇒ 6.04/2.19/30.72/9.34×（`docs/benchmarks/pinn_vs_cfd_speed_benchmark_20260420.md:14-17`，该文件 git 内仅 1 次提交 `565d346`）。② `benchmark_pinn_vs_cfd.py:65-82,127-155` **只计推理与在线重建、从不计训练**。③ 计时线走 API 预览路径（JSON `#metadata.pinn_mode = api ScenarioEngine preview inference`），含 `api/pinn_platform_api.py:998` 与 `:1002` 同一预测算两遍。④ 原主机 `iZ7xv19l7qsogyq3hzyhydZ` 产物已丢失 ⇒ 无法补测同机。

### 7.2 新表列
A 单工况 CFD（与真值同 `.edp`、同 `buildmesh` 段数）｜**A' 同精度 CFD**（网格无关性档位，T11）｜**B PINN 总训练时间**（稠密 98.8 s / 稀疏 59.5 s 旧主机，实例 55.1 s）｜C 单工况推理（**必须补"在 CFD 网格点数上"的计时**，现只有预览分辨率）｜D 在线重建路径（表注明写"含仿射校正+RBF，不是第5章方法"）。

### 7.3 摊销与拐点
```
CFD(K)=K·A      PINN(K)=B+K·C      加速比(K)=K·A/(B+K·C)      K*=B/(A−C)
```
| K | 收缩 B=98.8 | 收缩 B=59.5 | 弯曲 B=98.8 | 弯曲 B=59.5 |
|---|---|---|---|---|
| 1 | 0.005× | 0.008× | 0.037× | 0.061× |
| 10 | 0.050× | 0.083× | 0.364× | 0.599× |
| 50 | 0.243× | 0.396× | 1.737× | 2.80× |
| **K\*** | **236** | **142** | **28** | **17** |
| ∞ | 6.04× | 6.04× | 30.7× | 30.7× |

⇒ **必须如实写**：论文现在的 6–30× 是 **K→∞ 的渐近上限**；计入训练后，收缩族要 **142–236 个工况**、弯曲族 **17–28 个**才回本。两个方向都要报：把 CFD 细化到网格无关档位 ⇒ A↑ ⇒ K\*↓（对 PINN 有利）；把 CFD 折算到与预览同点数（【审计】B.1 的弯曲 ≈0.64 s/5.4×，**我未复算**）⇒ A↓ ⇒ K\*↑。诚实的表必须同时给 A 与 A'。
**测量条件声明**（原文出现在 5.7）：CPU 核数与型号、`OMP/MKL/OPENBLAS/NUMEXPR_NUM_THREADS=1`、`nice -n 10`、torch/numpy 版本、重复次数（PINN 15 / CFD 7）、统计量（中位+IQR）、计时器 `time.perf_counter`、**新旧墙钟口径差异（实例无 `/usr/bin/time`）**、是否计训练、K\*。

---

## 8. 魔搭环境规约

- **通道（已由 T0 定样）**：GitHub 直连不可用（实例上 raw 下载 90 s 得 0 字节、git clone 报 HTTP2 framing 错，而 `api.github.com` 0.32 s 通）。**走本地 → 实例的 Jupyter Contents API 分块上传**：16.7 MB / ≈65 s（0.27 MB/s），sha256 逐位一致。仓库侧 payload：`cases` 全量 60.5 MB（1392 个入库文件共 160.2 MB），第五章子集（contraction 全 9 工况 + 6 个 `B-*__ip_blunted`）= **17.4 MB** ⇒ 实测上传 16.7 MB 与之吻合。
- **实例工作区已成形**：`/mnt/workspace/pinn-repro-2026/{model,logs,scripts,out}`。
- **环境不是可选项**：镜像自带 numpy 1.26.4 会在 `train_supervised.py:588` 的 `np.trapezoid` 上 AttributeError ⇒ 必须 `pip3 install numpy==2.2.6`（副作用：现装 scipy 1.12.0 会警告需 numpy<1.29）。`preflight` 已把这条做成硬闸。
- **8 h / 空闲 1 h**：长任务 `nohup` + 分段（每段 ≤90 min 预算、提前退出）+ 边跑边落 `out/progress.jsonl` + 断点续跑（`metrics.json` 存在即跳过训练，照 `run_strict_sparse_experiments.sh:36`；启动脚本内**禁止递归删除输出目录**，已由 `lint_no_recursive_delete` 把住）。
- **产物落盘硬约束（最高优先）：每段跑完立即 push；未 push 的产物视为未发生。** 段末三步 commit+push（失败重试 3 次，仍失败则 tar 回传）；`seal_segment` 已改成"内存计数 vs 落盘账本"双向对账，**本段 0 行 = INVALID**（`grep -c` 返回的 0 是合法值，不能当通过）。旧结果不可恢复（5.7 那组数）就是前车之鉴。
- 体量：每 run ≈**1.05 MB**（best.ckpt 288 KB + history.csv 173 KB + evaluations 477 KB），107 run ⇒ **≈113 MB** 要回传。**`out/` 目前未在 `.gitignore` 里**（我本轮只写文档未动它），下次动代码时补一条，否则扫描产物会被当源码提交。
- 投递顺序与 6 条 dry-run 证据见【DRY】§1、§4。

---

## 9. 风险清单

| # | 风险 | 触发信号 | 应对写法（预先写好） |
|---|---|---|---|
| 1 | **多种子推翻现有结论** | T5/T6 某格 std 跨过两臂差，或分层方向翻转 | 结论句改"均值±标准差(n=5) 为 A vs B，差异幅度落在换一次种子的波动带内，本文不据该指标判定优劣"，并把该结论从摘要/结论删除；表5-5 若反转走 §5 预案文字。**不允许只删数字保留结论** |
| 2 | **Re 升级来不及 / 探针显示 NS 上基线仍不失效** | 9/25 G1 条件 ①②③ 任一不成立 | 走 C 或 D。5.10 改标题为"Stokes 与 NS 的解偏差定量估计"且只报数值；结论"后续工作"必须写"本文方法**未在** NS 上评估"。**影响**：必做3 只算部分交付，论文停在"系统与方法演示"，够不到"方法贡献" |
| 3 | **POD/插值/曲率基线打赢 PINN** | T10 读数低于 PINN | **预期内**（R3 已预告 22–609×，F 臂已预告弧段平手）。比较维度显式扩到 5 列（平均误差 / 无滑移满足方式 / 含噪退化 / 跨几何外推 / 一次训练 K 工况复用成本），正文明写"在本文几何族上，准一维解析式与 POD 在平均误差上优于 PINN，故本文不主张精度优势，主张 [参数化链路 + 构造化边界 + 可复现演示系统]"。**注意 C3 的限定**：弯曲族"不是全败，是速度一列小胜、压力一列大败"（弧段压力优 40–62×、出口直段优 4.2×）⇒ 五列表必须两族都出，不能只出收缩族 |
| 4 | **单网络联合 PINN 反而更好** | T8 在等参数量等预算下 interior 误差 ≤ 双模型 | R1 已预告该方向。创新点1 从"双模型优于单网络/避免干扰"改为"把无滑移与压降约束写成可分离的构造项/损失项；等容量等预算下未观察到精度增益，代价是 1.99× 参数与 2× 前向"。**影响**：方法贡献只剩"严格稀疏口径 + 构造化边界"，这条目前无对手做过同口径对照，是 6 天内守得住的位置 |
| 5 | 实例回收丢产物 | push 失败 / progress 有行而 git 无 | §8 硬约束；发生即该格判"未发生"，用 9/29 补救配额重跑 |
| 6 | **T4 的 obs_seed=0 复现校验不过** | 实例上 `--verify-committed` 出现非 IDENTICAL | 说明抽样语义被动过 ⇒ **整套 obs_seed 作废**，T6 退回"只换训练种子"（此时分层 vs 均匀只能报 mean±std，不得报显著性），并在回复表如实标 D2 未闭合 |
| 7 | 弯曲单价未标定导致段规划错 | 首个弯曲 run 的 `wall_ms` 与 75.7 s 差 >30% | 立即把标定值经 `BEND_UNIT_SEC` 传入重投；不重投前不引用任何弯曲 ETA |
| 8 | n=5 被误报成"已做显著性检验" | 检验产物缺 min-p 字段 | `analyze_sweep.py` 每条配对都打印最小可达 p；T1 闸门加断言：声称 p 的表其 n≥6 且 JSON 存 min_p，否则红 |
| 9 | 效率表自曝"PINN 更慢" | §7.3 K\*=142–236 | 不删：与 A'（网格无关档位）并列。**若** T11 显示达标网格显著抬高 A ⇒ K\* 下降，结论可回正；这是唯一诚实的回正路径 |
| 10 | 旧单次数字与新环境读数并存 | 同一指标出现两个值 | T1 闸门锁定"每格只允许一个 run 来源"；旧值进 `docs/ARCHIVED_NUMBERS.md` |

---

## 10. 不做项 + 需要用户拍板（两个真问题）

### 不做（写进延期申请，不写进论文承诺）
1. **µPIV / 任何自采实验**（硬件/流片/示踪粒子/标定，6 天不可达）⇒ 短板2 的实验半边永久缺口，论文只能主张数值交叉验证。
2. **T 型交叉口 / 蛇形混合器**（审稿人自估 3–5 周）。
3. **湍流 / 非定常**（与"二维稳态"题目承诺直接冲突）。
4. **COMSOL/ANSYS 对拍**（无授权、无安装路径）⇒ 用 T11 替代，并**明写这不是第二求解器**。
5. **把第五章全量换成 NS（G1-B）**。
6. **把主战场换到弯曲族**（v2 撤回；理由见 §0.1 R3-附加）。

### 如果不再补任何实验，只靠改写与加限定 —— 能停在哪个档（诚实选项）

| 维度 | 只走 (a)+(c) |
|---|---|
| 数字与产物 | **对齐**。21 处口径 + 7 处撤回 + 效率改用 artifact 读数，T1 闸门可全绿（0 机时） |
| 两条创新点 | **不成立但可降级**：创新点1→"未观察到正增益"（R1 已支撑）；创新点2→"干净组 94.6% 速度 / 31.2% 压力"（R2/C1 后仓库现成数） |
| 物理层第一问 | **答不了**。Re=0.0224 + 解析优 22–609×（收缩族），改文字改不掉 |
| 统计层 | **仍是 n=1**。必做2 整条不交付；R4 的 35.4% 抖动无法反驳 |
| 基线层 | **半交付**：单网络臂可复算报出；联合单网络、POD、插值、曲率四臂缺 |
| 最终档位 | 与【审计】I.1 + 可辩护率 **54%（E1+E2=31/57）、能原样写进摘要只剩 ~39%（22/57）** 一致：**应用型毕设中上、链路完整可核的演示系统**；过得了"论文与证据一致"，过不了"方法贡献" |

⇒ **(a)+(c) 能把论文做到"无可指责"但做不到"有贡献"**。决定档位的只有两件事且都便宜：T5+T6（n=1→n=5/8，≈2 h 机时）与 T12 探针→T13（回答第一问，0.5–2 h）。**建议两件都做**；只做一件则做 T5+T6 —— 它是 6 条短板里唯一"每一条都被它连坐"的那条。

### 两个真问题（其余原问题已解决，删）

**(a) 路线：甲（只把论文改成与证据一致）vs 甲+乙（加 Re=1/10/50 一节）。**
**不靠讨论，靠一个 30 min 探针判**（=T12 探针版）：只出 NS 真值、**不训任何模型**，把解析 / F / G / RBF / POD 五臂搬到 Re=10、50 的真值场上重新打分（同一 `relative_l2`、同一行集）。
- 若某档上五臂误差跳 ≥1e-2（即准一维/曲率近似失效）⇒ **走甲+乙**，该档就是"为什么需要本方法"的第一手证据，且它同时兑现必做3。
- 若五臂仍 ≤模型读数 ⇒ **走甲**，并按 §9 情形2 的写法承认"有限 Re 在本几何族上也不构成 PINN 的必要性"，把力气全压在 T5/T6 上。
- 探针成本：真值 2 族 × 3 档 ≈ 6–27 次求解 + 打分，**<30 min**，且完全可并入 T11/T12 的既有产物路径。**我推荐先跑探针再选路线，不要现在拍。**

**(b) 摘要里是否允许出现这句**：
> "在收缩流道（等宽直段 + 平滑收缩，Re=0.0224）上，零训练的准一维解析式的速度/压力误差低于本文模型 1.4–2.8 个量级，因此**本文不主张该族上的精度优势**；方法学贡献限于严格稀疏观测口径、构造化边界与参数化复用链路。"

我判断**必须出现**（否则摘要就是【审计】#1/#10 指的过度声称）。但它会把论文定位从"方法贡献"明确写成"系统 + 方法演示"，需要你先认这个降档。**若不接受**，唯一替代路径是走 (a) 的甲+乙并且指望探针显示基线失效——那就不能同时删掉这句又不补实验。

（原第 2、3 问已解决：G0 关闭；传输通道 = Jupyter Contents API（16.7 MB/65 s、sha256 一致）；环境 = Python 3.11.11 + torch 2.3.1+cpu + numpy 2.2.6，只需写进 `ENV_LOCK.md`。）

---

## 11. 对统括官/审计线的更正台账（v1 §11 + 本轮采纳项，全部落到"已定项"）

### 11.1 需要更正**统括官**的 2 条（v1 已提，状态更新）
- **E1 · 表4-4 是"串档"不是"表写错"**（v1 §11.1）。41 个 run 按三元组 `(入口流量,出口压力,压降)` 聚类：**23 个 = (0.5,1e-4,1.0)**（= 表4-4 所描述的 v4/v1 批）、**14 个 = (0,0,0)**（= 第五章 5.3–5.8 数据来源的 strict 批）、**4 个纯 MLP 无 `权重` 字段**；合计 41。**修法 = 加表4-4b + 正文交代适用范围，不删任何数字**。C1 的桩化实跑进一步把这条升级为"整批 15 条训练命令恒为 0"（【交叉】`:43,:188`）。
- **E2 · "仓库里没有任何 RBF"半错**（v1 §11.1）：`api/pinn_platform_api.py:891-901,949-952,1045-1048` 有高斯核 + K-center 锚点 + 岭权重，但它是"先验场 + 残差校正"，**不能当传统插值基线**（先验来自模型自己的预测）。⇒ T10 的插值臂不是从零写核函数，但不能复用那段代码。

### 11.2 我 v1 的三处发现 → v2 降级为已定项
| 编号 | 事实（凭据） | v2 处置 | 时限 |
|---|---|---|---|
| **M1** | 表5-8"不启用阶段内 PDE"整行**只有派生 CSV**：`results/field_map_checks/chapter5_materials/contraction_stagepde_comparison.csv`（入库 commit `5bca68d`，被 `scripts/prepare_chapter5_assets.py:330` 消费）给出 0.174478/0.022100/0.120579/0.180740，但**96 个 metrics/evaluations 文件里 0 命中、无 run 目录、无生成脚本**；"启用"行四值逐位命中 `…mainline_v4/metrics.json` | **T7-②**：以 5 seed 重跑不启用臂，`config.json#权重.速度阶段连续性=0` 断言把关 | 9/26 |
| **M2** | 图5-18 的单网络臂与双模型臂**三处不对等**：选点规则（最优轮 `best_epoch=1183`/`stop_epoch=1383` vs 末轮）、参数量（35,331 vs 70,275 = 0.50×）、有无壁面构造 | **T7-③ + T8/T9**：等预算等容量重做对照；图注披露 | 9/26–9/27 |
| **M3** | 论文 test 平均 = **mean-of-cases**（0.0390/0.5612/11.7598），仓库 JSON 顶层 = **pooled**（0.0471/0.5684/9.0377）；两口径下"压力改善"从 77× 变 35× | **T2 ③ + `analyze_sweep.py` 双口径输出**（已实现，【DRY】§4.3） | 9/28 落文 |
| **D1** | B-test-2 从未被双模型评估（见 §2 已定项） | **T7-①** 一次 eval | 9/26 |

### 11.3 采纳**审计线**的更正与自纠
- **C1**：删"basic 臂无压力锚定 / 四变量"表述，改 2 变量（特征、由特征派生的壁面模式）；判决不变（5.6.2 那组数字仍须撤）。
- **C2**：v1 的"换主战场"论据**撤回**；弯曲族只报分区误差与准一维失效位置；**F/G 进基线清单**。
- **C3**：解析式区间下限由 0.0007 改 **0.00019**（【审计】只跑了 5 工况，非冲突）；"优 22×""压降优 609×"两句可写，但必须带工况名 + "收缩族，不含弯曲弧段"限定 + 压降的绝对量（0.3331 = **0.29 Pa**，真值 0.87 Pa，p 尺度 μU/W=4.451e-4 Pa/单位）。
- 我 v1 把 M1 判成"无凭据"是**判重了**（实为派生-only），已在 v1 §11 就地改正并保留该次误判记录。

---

## 12. 9/30 各时间点应交出什么（可检查清单）

| 时间（北京） | 必须交出的东西 | 怎么算过 |
|---|---|---|
| **9/24 EOD** | T1/T2/T3 三项改写落地：21 处口径标注 + 7 处撤回句替换 + 表4-4b 草稿；"无压力锚定"字样在全部仍写处清零 | `verify_tables.sh` 能跑并输出三态计数；正文 grep 三项目标串 0 命中 |
| **9/25 EOD** | G1 判定完成：T11 网格无关性表 + **T12 探针打分表（五臂 × Re=1/10/50）** + 路线甲/甲+乙 的**一句话决定** | `mesh_study.csv` 压降变化 <2%；探针表每个 Re 有 `‖(u·∇)u‖/‖∇²u‖` 与五臂误差；决定是否写 5.10 |
| **9/26 EOD** | 第 1 次实例作业回传：**T5 全 19 格 × 5 seed = 95 run**（若并行不足则至少 16 格）+ **T7 三格**；`obs_seed=0 --verify-committed` 全 IDENTICAL；`BEND_UNIT_SEC` 标定值 | `out/progress.jsonl` 段末打印 `%d runs / %d failures` 且 INVALID=0；commit 已 push；弯曲单价误差 <30% |
| **9/27 EOD** | **T6 8 配对单元 + `T5_T6_stats.json`** ⇒ G2 判决：表5-5 保留/降级二选一，措辞按 §5 预案；T10 四臂基线表出数 | JSON 含 `min_reachable_two_sided_p`、`n_pairs=8`、两口径并列；五臂表含 F/G 的"零观测点"注 |
| **9/28 EOD** | 全部数字落表；(c) 撤回与 (a) 改写终稿合并；**逐条回复表 14 行写完** | 每个正文数字在 §附录M 有 run+字段；回复表每行有档位与落盘章表号 |
| **9/29 EOD** | 冻结：**tag `v4.1-revised-20260930`** + `TABLE_PROVENANCE.json` + `ENV_LOCK.md` + requirements(scipy 补入) + 参考文献 ≥40 + 正文代码/数据可得性声明；送审 PDF 格式校 | `git checkout <tag>` 后 `verify_tables.sh` 退出码 0；`git tag -l` 非空；参考文献计数 ≥40 |
| **9/30** | 交题。缓冲日，**不排任何新实验** | — |

---

## 附录 M · 论文表格 → run 目录 + metrics 字段 映射（自动核对闸门的输入）

约定：`RP=model/results/pinn`、`RS=model/results/supervised`。三态：`直测` / `DERIVED-ONLY` / `MISSING-CREDENTIAL`。

| 表/图 | 论文位置 | run 目录 | 文件#字段 | 态 | 备注 |
|---|---|---|---|---|---|
| 表2-1/2-2 物性工况 | `thesis.txt:31,42` | — | `model/cases/*/data/*/meta.json#{rho,mu,nu,flow.Re}` | 直测 | `flow.Re=0.022400584138395865` |
| 表2-5/2-6 特征 | :184,:234 | `RP/…mainline_v4` | `config.json#feature_cols,#base_feature_cols` | 直测 | 14 列=15 列去 `inlet_profile_star` |
| 表2-7 子模型 | :289 | 同上 | `config.json#velocity_hidden_layers,#pressure_hidden_layers,#activation` | 直测 | 参数量 70,275 为解析推算，T8 断言 |
| 表3-2/3-3 工况 | :340,:382 | — | `cases/*/data/*/geometry.json`、`trainval_manifest*.json` | 直测 | **D1**：B-test-2 真值在、评估不在 |
| 表3-6/3-8/3-9 划分与配额 | :492,:543,:557 | — | `cases/*/data/*/obs_*.csv` 行数 + `#region_id` | 直测 | 收缩 C-val interior 1261→5%=63（r0/r1/r2=13/28/22）、1%=13（2/6/5）；均匀 5%=63（21/18/24）。弯曲 B-val__ip_blunted interior 7633→5%=382（95/153/134）⇒ **两族"5%"绝对预算差 6.06×** |
| 表4-2/4-3 环境超参 | :644,:658 | 主线/strict | `config.json#{seed,epochs,学习率,max_physics_points,strict_sparse_scalers}` | 直测 | seed 全=42；**torch 版本需按实例改写** |
| **表4-4 损失权重** | :716-754 | **v4 主线** `RP/…mainline_v4` | `config.json#权重` = 0.5/1e-4/1.0 | 直测 | **须加 4-4b** ← `RP/contraction_strict_geometry_sparse5_stagepde_20260503/config.json#权重`=0/0/0；开关在 `run_strict_sparse_experiments.sh:59-66` |
| 表4-5/4-6 壁面与选点 | :756,:766 | 同上 | `config.json#速度壁面约束.mode,#hard_wall_sharpness`；`metrics.json#最终验证指标` | 直测 | strict 批另有 `#权重.壁面=0.0`；basic→soft 见 `:29-32` |
| 表4-7 指标定义 | :784 | — | `scripts/train_supervised.py:361-362,622-663` | 直测 | 须补 mean-of-cases vs pooled 声明（M3） |
| 表5-1 总览 | :864-990 | **混合批次** | 各行分别指向下列各行 | 混合 | 5.1-5.2 行=主线；5.3-5.8 行=strict 批 |
| 表5-1/5.6.2 消融 | :960-975,:1185 | `RP/contraction_strict_{basic,geometry}_sparse5_stagepde_20260503` | `evaluations/metrics_test_dense.json#case_metrics[*].rel_l2_speed` 两工况算术均值 | 直测 | 0.5612/0.0390；pooled 为 0.5684/0.0471；**双变量⇒不可归因给编码（R2/C1）**；干净组 `docs/ablations/geometry_encoding_ablation_20260420/*_summary.csv` 给 0.5753→0.0308、0.2681→0.1843 |
| 表5-1 壁面u残余 | :983-984 | `RP/contraction_strict_geometry_sparse5_stagepde_20260503` | `evaluations/metrics_val_dense.json#summary.mean_wall_max_abs_u_pred=3.8395846786443144e-05` | 直测 | **构造保证**：`train_*_strict_sparse.py:195-225`，298/360 行 u_pred 精确为 0 |
| 表5-2 三阶段 | :993-1013 | `RP/…mainline_v4` | `metrics.json#{速度阶段验证指标,压力阶段验证指标,最终验证指标}.rel_l2_{u,p}`；起止值 `history.csv` 首末行（0.485679/0.707879/0.027619/0.174260 逐位命中） | 直测 | n=1 轨迹，无 no-coupling 对照 |
| 表5-3 基线重建 | :1020-1044 | 同上 | `evaluations/metrics_val.json#summary.mean_rel_l2_{u,p},mean_pressure_drop_rel_error`；test 行 `metrics_test.json#case_metrics[*]` | 直测 | C-val 0.0221/0.1041/0.0951；C-test-1 0.0162/0.0492/0.0550；C-test-2 0.0397/0.3127/0.3331 |
| **表5-4 采样率（弯曲）** | :1055-1080 | `RP/bend_strict_blunted_sparse{1,5,10,15}_stagepde_20260503` | `evaluations/metrics_val_dense.json#global_metrics.`**`rel_l2_speed`**`,rel_l2_p`；`#case_metrics[0].pressure_drop_rel_error` | 直测 | ⚠ 速度列是**速度模**（0.0248/0.0228/0.0237/0.0228），而 `rel_l2_u` = 0.0414/0.0388/0.0401/0.0378（差 1.7×）⇒ 表头必须点名 |
| **表5-5 分层 vs 均匀** | :1094-1119 | `RP/contraction_strict_geometry_{sparse5,uniform5}_stagepde_20260503` | `#case_metrics[0]`（val）与两工况算术均值（test） | 直测 | ⚠ 反向批次实物：`results/field_map_checks/chapter5_materials/contraction_regionaware_vs_uniform5_{casewise.csv,notes.txt}`（v4 批，均匀三项全优）⇒ **D2 重跑**；两臂差 23.9% < seed 抖动 35.4%（R4） |
| 表5-6 噪声 | :1132-1147 | `RP/contraction_strict_geometry_sparse5{,noise3}_stagepde_20260503` | `#case_metrics[0].#{rel_l2_speed,rel_l2_p,pressure_drop_rel_error}` | 直测 | 0.0321/0.0725/0.1093 与 0.0325/0.0777/0.1148；4.4.3 承诺的 1%/5% 档未进表（数据在 `cases/…/obs_sparse_5pct_noise_{1,5}pct.csv`）⇒ T5 格11/12 |
| 表5-7 未见几何 | :1158-1182 | C 行 `RP/…mainline_v4`（dense）；B 行 `RP/bend_strict_blunted_sparse5_stagepde_20260503` | `evaluations/metrics_test*.json#case_metrics[*]`；B 行速度=`rel_l2_speed`(0.0274)/p 0.0563/压降 0.0331 | 直测 | ⚠ C 行 dense、B 行 5%，同名指标不可横比；⚠ B-test-2 缺（D1） |
| **表5-8 阶段内 PDE** | :1198-1213 | 启用=`RP/…mainline_v4`；**不启用=无 run 目录** | 启用：`metrics.json#{压力阶段验证指标.rel_l2_p=0.174446,最终验证指标.rel_l2_speed=0.022113,.rel_l2_p=0.104091,.max_p_err_over_p_range=0.145989}`；不启用：`…chapter5_materials/contraction_stagepde_comparison.csv#no_stagepde` | 直测 / **DERIVED-ONLY** | **M1 → T7-②** |
| **图5-16/5-17 效率** | :1192,:1194 | 无（原主机产物丢失） | 论文 0.111/0.242/0.481、0.117/0.394/3.552 → **无来源**；artifact `docs/benchmarks/pinn_vs_cfd_speed_benchmark_20260420.{md,json}` = 0.0829/0.2283/0.5009、0.1184/0.3897/3.6380 → 6.04/2.19/30.72/9.34 | **MISSING-CREDENTIAL**（论文侧） | 整表按实测重做 = T14/§7；`benchmark_pinn_vs_cfd.py:127-155` 从不计训练 |
| **图5-18 单网络 vs 双模型** | :1215-1216 | 双 `RP/contraction_strict_geometry_sparse5_stagepde_20260503`；单 `RS/contraction_single_mlp_geometry_sparse5_strict_20260503` | 双：`#case_metrics[0].{rel_l2_speed=0.032084,rel_l2_p=0.072530,pressure_drop_rel_error=0.1093,wall_max_abs_u_pred=3.84e-5}`；单：`#summary.{mean_rel_l2_speed=0.0410928,mean_rel_l2_p=0.0753176,mean_pressure_drop_rel_error=0.0760912,mean_wall_max_abs_u_pred=0.2332939}` | 直测 | ⚠ 单网络 `mean_rel_l2_v=0.5358` 未披露；⚠ 三处不对等（M2）；⚠ **interior 口径方向相反（R1）** ⇒ 图注重写；画图脚本 `tmp_remote_make_coupling_single_figure.py:30-31` 读的是非 strict run（20260502），属命名卫生 |
| （新增）解析/曲率/RBF/POD 基线 | 修订版新增 | 无 run | `.scratch/recompute_R3_*.py` → 固化为 `scripts/baselines_classical.py`；【交叉】C2/C3 表 | 待生成 | 收缩族 0.00019–0.00141；弧段 F 0.0133/0.0192 vs PINN 0.0159/0.0236；弧段压力解析优 39.8×/62× |

**用法**：T1 闸门逐行读"文件#字段"、重算、与 `thesis.txt` 对应段比对，出三态；两个非 `PASS` 行（表5-8 不启用行、图5-16/5-17 论文侧）在 T7/T14 补齐前**必须保持红**，不得为绿灯删断言，也不得把 `DERIVED-ONLY` 当 `PASS`。
