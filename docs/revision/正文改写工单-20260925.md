# 正文改写工单（2026-09-25）

> 交付形态：**逐条可执行工单 + 可直接粘贴的新文本**。本工单不改 `thesis.txt` 本体、不改 docx、不跑实验、不改代码。
> 段号一律指 `.scratch/thesis.txt` 的行号（= 段落序号，与【审计】和【物审】用的同一套编号）。
> 方向已定（统括官 9/25 裁决）：**毕设只降级措辞、不重跑**；物理式的修正归路线二。

## 0. 使用说明与凭据分档

| 档 | 含义 | 本工单里的表现 |
| --- | --- | --- |
| 直测 | 有 run 目录 + JSON 字段可逐位读回 | 可直接写进正文 |
| 单次读数 | 有产物但 n=1（无多种子） | 正文必须带"一次训练的结果"限定 |
| 派生 | 由别的量算出（如占比、K\*） | 必须同时给公式与输入值 |
| 无凭据 | 产物不存在（如论文 5.7 那四组耗时、表5-8 的"不启用"行） | **只能删或换成有凭据的数** |

**sd 口径（9/26 定，全文只此一把）**：本文所有标准差一律为**五个训练种子的样本标准差（ddof=1）**，与统计管线 `analyze_sweep.py:139` 的 `statistics.stdev` 同口径；表注固定写「本表 sd 为 5 个训练种子的样本标准差（ddof=1）」。**不得与总体标准差（ddof=0，值小 √(5/4)=1.118 倍）并存**；确需引用他处 ddof=0 的数，须在同一格内括注口径。

**val / test 口径必须分列（9/26 补）**：run 坐标索引 `.psv` 那一列是**验证集**读数；填第 5 章 test 表只能用 `docs/revision/T5矩阵test口径读数-20260926.md`（test / mean-of-cases）。本工单凡由 .psv 得来的数一律标 (val)，凡进正文表的数一律标 (test)，两列并排时各自带口径。例：消融 0.5196→0.1362 是 val，test 是 0.5399→0.1487；弯曲「5%≈稠密」在 val 是 +3.0%、在 test 是 +7.8%——不标口径就是假数。

三条铁律：① 数值只从本工单标出的产物取，不得沿用无凭据旧数；② 撒下任何因果归因句时，**表里的数字不删**，只改归因与口径；③ 凡 `n=1` 处不得出现"优于/表明/说明…更…"（见 H 节）。

**冻结前的"谁点头/什么没做"清单在 `docs/revision/冻结前拍板与未做清单-20260926.md`（一张表，每行带默认动作与"改变哪个决定"）。**

本轮改动量：**51 条**（A 17、B 5、C 3、D 2、E 5、F 2、G 5、H 5、I 4、**J 3**；首版 43 条，9/26 并入三臂与批次标注到 48，同日 21:4x—22:0x 并入复现声明 J 类到 51。此数由本文件行号现算而非手填）。同日傍晚又并入 T5 第一批可写读数：D1（几何特征消融，五种子配对 3.8×）、H2/H4/H5（表5-5 改写成"只在 ≤1% 可区分"的曲线结论）、A12（表5-8 判为无可测贡献 + 原"无凭据"判定解除）、A17（噪声响应与混淆核查）、E3/E5（实测单价与 K\* 更新、CFD 同机单价列入下一批次）。**动表格数字的仍是 1 处**（E 类换 5.7 整节数据），另新增四处表格内容：表 4-4b、表 5-9、表 5-10（三臂 + 臂 C 对照，见 B5/D2）、表 5-7 的新列与 B-test-2 行；表 5-5/5-6 是**加 mean±sd 与逐种子符号列**，原单次读数保留。所有被"撤下归因"的读数本身保留。

---

## 1. A 类 · 物理项/动量项（承重：动量项不是 Stokes 残差）

统一事实底座（下文反复引用，称【物审】）：`paper-route2/v4物理项尺度审计-20260925.md`
—— 归一化因子 `velocity_scale=max‖u‖`、`pressure_scale=max p−min p`（`model/scripts/train_velocity_pressure_independent.py:369-379`）；
两边失配比 **P/U = 328（稠密主线，U=3.0000、P=985.253）/ 492（严格稀疏批，`config.json#速度标准化.std、压力标准化.std`）**；
把 CFD 真值代入该动量式得 **554.6 / 19990**，而收敛模型读数只有 **2.275e-4 / 2.078e-3**（差 2.4e6–9.6e6 倍）；
自动微分只对第 0、1 列（`x*,y*`）求导（`:337,350-365`；strict `:348,361-369`），其余 12 列几何特征按常数处理。

| # | 位置：段号 + 原句摘引 | 处置 | 新文本（可直接粘贴） | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| A1 | `:143`「本文先对速度导数项和压力梯度项做尺度平衡……减少不同物理量之间的量级差异对训练的影响」 | **撤回 + 换事实陈述** | 式(2-9a)至(2-9c)给出连续性残差与动量残差的构造形式。需要说明的是，动量残差中的速度二阶导项与压力梯度项分别以峰值速度 max‖u‖ 与压强极差 max p−min p 作除因子，两者并不属于同一导数量纲；在本文训练工况上，两项的幅度比分别为 328（稠密主线）与 492（严格稀疏批）。因此该处理并未使两项相互平衡，压力梯度只占残差幅度的 0.31%（主线）与 0.21%（严格稀疏批）。 | 【物审】第一屏第 1 条；`train_velocity_pressure_independent.py:369-383` | 否 |
| A2 | `:144-150`（式 2-9a–c 的式下符号说明） | **新增限定句** | 式中，u、p 为网络输出经逆变换后的无量纲速度分量与压强；下标表示对该物理坐标的一阶或二阶偏导；除因子 V 与 P 分别取训练真值场的峰值速度与压强极差，其取值见 4.2 节表 4-3 的补充说明。 | `:369-379`；表4-3 补行（见 A3） | 是→表4-3 仅新增行 |
| A3 | `:151`「式中，为用于计算控制方程残差的内部采样点数」 | **补限定** | 式中，N_p 为用于计算控制方程残差的内部采样点数，本文取 512，由内部点索引序列等距确定抽取（`train_velocity_pressure_independent.py:334-336`，`config.json#max_physics_points`），因此残差评估点不随种子变化。此外，本文的二阶导数由自动微分沿网络前向图对归一化坐标列 x\*、y\* 求得，其余几何增强特征在该求导中作为常量输入处理（`:337,350-365`）。 | 同上；【物审】第 3 条 | 否 |
| A4 | `:131`「监督误差在标准化空间中计算；控制方程残差、边界项和压降项则在逆变换后的无量纲物理空间中计算。这样既能减轻……也能保留物理残差的实际含义」 | **保留 + 加限定（并把"减轻训练不平衡"降为中性描述）** | 需要指出，监督误差在标准化空间中计算，控制方程残差、边界项和压降项则在逆变换后的无量纲物理空间中计算。两类量的数值尺度不同，其权重不可跨批次迁移，本文按批次分别标定（见表 4-4 与表 4-4b）；其中出口压力损失未做归一化，两批次的原始量级相差约 24 倍。 | `:286-291`；【物审】§1.3 表末两条 | 否 |
| A5 | `:639`（第 4 章重复同一表述处） | **同上，句尾加同一限定** | 监督损失在标准化空间中计算；控制方程残差、边界约束、流量约束和压降约束先将预测结果逆变换回无量纲物理空间再计算。因两类项量级不可直接比较，第 4 章给出的权重取值仅对其所属批次有效。 | 同 A4 | 否 |
| A6 | `:592`「PINN 训练并不只依赖观测值，还会受到控制方程约束；因此在低强度噪声下，模型有机会在监督项和物理项之间取得折中」 | **撤回因果解释** | 含噪观测同样固定随机种子。需要说明的是，按本文实现计算，第三阶段损失中与方程直接相关的份额很小（以本文归一化口径估算约占 0.1% 量级），因此 5.5 节的含噪结果只能由监督项与几何增强特征解释，不能作为"物理约束参与训练"的证据。 | 【物审】§1.3、第一屏第 1 条 | 否 |
| A7 | `:715`「损失权重配置上……关键权重见表4-4」 | **补适用范围** | 损失权重配置上，速度阶段强调速度监督、入口流量一致性和连续性约束；压力阶段强调压力监督、压降一致性和动量项；耦合阶段再提高动量项权重。表 4-4 给出稠密主线批的设置；严格稀疏观测批在此基础上另有三项权重置零，见表 4-4b。 | `run_strict_sparse_experiments.sh:59-66` | 否 |
| A8 | `:736-738`（表 4-4 中"出口压力损失权重""压降损失权重"两行） | **补表注** | 表 4-4 注：出口压力损失未做归一化，其权重为对应批次下的手调值，不可跨批次复用；耦合阶段动量权重 10.0 的作用对象在当前归一化下以速度剖面曲率项为主。 | `:286-291,382-383`；【物审】§2 第 5 行 | 是→表 4-4 加表注，数值不动 |
| A9 | `:829`（4.4.5 标题「PDE约束与模型结构对比实验」）、`:830`「第一组对比考察阶段内PDE约束」 | **换词（术语中性化）** | 4.4.5 阶段内残差惩罚与模型结构对比实验。除几何因素外，本文还单独检查模型结构与阶段内残差惩罚项的作用。第一组对比考察速度阶段与压力阶段是否提前加入连续性项或动量项；第二组在 5% 稀疏观测口径下比较单网络 MLP 与双模型三阶段流程；第三组放在同一双模型主线内部，观察耦合前后压力误差的变化。需要说明，此处的单网络对照不是纯粹的结构消融，因为双模型还带有硬壁面构造项与阶段内残差惩罚项。 | `:195-225`（硬包络）；【物审】§1.1 | 否 |
| A10 | `:1014`「耦合阶段再把速度、压力和PDE残差放在一起约束」、`:1016`「耦合阶段继续降低压力偏差」、**`:1017`「其二，耦合阶段的收益主要体现在压力场修正，尤其是最大压力误差的控制」** | **撒回结论二**（A 类里最硬的一条） | 表 5-2 给出的变化可以看出，速度阶段较快下降的是速度监督误差，压力阶段之后压力误差开始收敛，第三阶段以较小学习率交替更新两个子模型。至于耦合阶段读数中压力误差的下降，在当前实现下不能归因于控制方程约束的作用：一方面该项两侧归一化不一致（见 2.2.1 节），另一方面本表只有一条训练轨迹（n=1），且同类配置换一个随机种子时速度误差可变动 35.4%。因此本节只报告该次训练的结果，不对耦合阶段的收益方向下结论。 | `:369-383`；【DRY】§5；v2 §0 R4（T0 实测 seed 42→43：0.0320841→0.0207178） | 否 |
| A11 | `:1198-1204`（表 5-8 的行名与列名"阶段内PDE约束"） | **换标签，数字全部保留** | 列名改为「阶段内残差惩罚项（启用 / 不启用）」；表题改为「表 5-8 阶段内残差惩罚项启用与否的最终误差对照」。 | 同 A9 | 是→表头文字，数值不动 |
| A12 | `:1214`「表5-8显示，阶段内PDE约束对速度场最终误差影响较小，对压力场影响更明显……压力场L2误差则由0.120579降至0.104091」 | **归因改写 + 现已可判"无可测贡献"（9/25 矩阵一手读数到位）** | 表 5-8 现在有两个版本可选，按产物到位程度决定：**（甲，立即可用）**沿用原单次读数，但把归因句换成：`表 5-8 显示，两组读数的速度场最终误差相差 0.000013，压力场最终误差相差 0.016488。在当前归一化实现下，该项主要改变的是速度剖面曲率而非压强平衡，压力侧差异是否来自方程约束无法判定；两行结果各自只来自一次训练。` **（乙，segment_01 回流入库后用）**：`在其余设置相同、只更换训练种子的条件下，启用与关闭阶段内残差惩罚项的五对配对差值为 +0.002、−0.002、−0.001、−0.005、−0.006 个百分点，均值 −0.002 个百分点（约 0.1%），配对差的组内标准差 0.003 个百分点（样本口径，t=1.57）。该差异与训练种子带来的散布（13%）相比小两个数量级，因此本文判定：在当前实现与当前归一化下，阶段内残差惩罚项对最终精度无可测贡献。` 两版都**保留原表数字**，只换解释与增加配对行。 | 读数源 `docs/revision/T5矩阵配对读数-20260925.md` 第二节（格1 vs 格2，`out/progress.jsonl`，85 条 train、obs_seed=0 固定⇒配对只换 train_seed）；sd-误差比本工单现算 = 1.87（Δmean 与 sd/√5 之比，样本口径）；原单次读数 `…mainline_v4/metrics.json#最终验证指标.rel_l2_p=0.104091` | 是→表 5-8 可增配对行，原值不动（run 级坐标见 §12 第 8 条的 .psv，入库后逐 run 挂） |
| A13 | 摘要 `:4`「训练由……组成的双模型PDE耦合PINN」「在轻度噪声下没有出现明显失稳」、绪论 `:11`「具有重建精度高、响应速度快、对噪声鲁棒性强、所需数据少等优点」、Abstract `:7`、结论 `:1260`、`:1263` | **降级：因果性"物理约束"→"损失中包含残差惩罚项"；删无凭据的通用宣称**（**待作者一句话可否决**：摘要与 Abstract 的这版措辞已按降级档写好，作者若否决只回退本行，不影响 A 类其余各条） | 摘要：`随后训练由速度模型和压力模型组成的双模型三阶段网络，其损失中包含连续性残差与动量残差惩罚项，并从稠密基线、稀疏观测、噪声扰动和几何变化等方面进行测试。` 绪论 `:11` 删「对噪声鲁棒性强」四字（该属性属 PINN 方法的通用宣称，本文未对其做专门验证），保留「重建精度高、响应速度快、所需数据少」并改为「在本文实验中表现出……」。结论 `:1260`：`在不同观测率和轻度含噪条件下，本文结果保持基本稳定。`（不写"因物理约束而稳定"） | 【物审】§3 第三行；`:286-291,369-383` | 否 |
| A14 | `:130`「耦合阶段再对两类子模型做低学习率协同修正」 | **换词** | 耦合阶段再对两类子模型做低学习率交替更新。（"修正"含收益暗示，本文不主张） | 同 A10 | 否 |
---

## 2. B 类 · 创新点 1 与 5.8 结尾（新措辞已定，不得再抬）

| # | 位置 + 原句摘引 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| B1 | `:24`「并在速度、压力和耦合三个阶段引入连续性约束与动量约束……有效避免训练过程中的干扰」 | **降级为系统设计特征** | 1. 构造面向二维稳态微流控内流的双模型分阶段训练流程。本文将速度和压力分别交给两个模型处理，通过速度、压力、耦合三个阶段组织训练次序，并把无滑移条件与压降要求写成可分离的构造项与损失项。在与单网络基线同观测点、同特征集、同网络规模的对照下，未观察到该结构的精度增益；其代价为 1.99 倍参数量与两倍前向计算（双模型稠密档 81.4 s/次、臂 A 单网络联合 139 s/次，均为本实例实测；参数量对齐不代表机时对等）。因此本文把它作为系统实现层面的设计特征报告，而不作为方法层面的精度优势。 | 【物审】§3 对 B1 的判决；v2 §0 R1/R2；图5-18 两侧 JSON（见 C1） | 否 |
| B2 | `:1217`「它的优势更集中在速度结构、壁面约束和压力一致性」 | **删"压力一致性"半边** | 但当前模型仍有不足：在参数偏移较大的测试样本上压力误差偏高，说明压力场对几何外推仍较敏感；双模型流程并非在所有指标上优于单网络，其读数差异集中在速度结构与近壁区域的构造满足程度上，压降误差仍需要通过损失权重或边界约束继续校正。 | 双模型压降 `pressure_drop_rel_error=0.1093`、单网络 `mean_pressure_drop_rel_error=0.0760912` ⇒ 压降一侧单网络更好 | 否 |
| B4 | 必做1 三臂的**可用度盘点**（9/26 终态刷新） | **状态刷新：test 口径已齐，撤掉本行原先两条"等数"的限定** | 三臂两档现在都有 test 口径，**唯一来源是 analyze**（`docs/revision/T5矩阵test口径读数-20260926.md` 第二节与四B 节）：稠密 双模型 0.028617±0.002162 / 臂A 0.039161±0.009313 / 臂B **0.019436±0.006923（该档最好）**；分层 5% 双模型 **0.031202±0.004515（n=5, obs_seed=0）** / 臂A **0.023144±0.004258（该档最好）** / 臂B 0.051029±0.008870（最差）。**（9/26 21:4x 统括官提出、本工单定档甲）** 该格原先挂的是 `0.029925±0.004515（n=11，含 T6 多点位）`，而同一行的两条配对差（`t5c21−t5c04=−0.008058`、`t5c23−t5c04=+0.019827`）只用 `obs_seed=0` 的五粒 ⇒ **均值与配对不同源，并排放就是给人一句『这两个数不是同一批』的口子**（两值差 0.001277，相对 4.27%）。本工单独立复核：由臂A 反推 0.023144+0.008058、由臂B 反推 0.051029−0.019827，两条都得 **0.0312020**，二者之差 3.5e-18 ⇒ 五粒值站得住。`0.029925（n=11）` **不删，但只留在 5.4 的『分层 vs 均匀』那一格**（那一格两臂都是 n=11，同源成立），表 5-10 里出现 0.029925 即判错。四条配对全 `W=0`（5 对同号），n=5 ⇒ 最小可达双侧 p=0.0625，按预注册口径**不写"显著"**。验证集口径另有一套（本工单从 run 级索引 obs_seed=0 现算：t5c20 稠密 0.033370±0.002270、t5c21 分层5% 0.013711±0.002088，同框双模型 0.023615/0.025508）⇒ **两套数不得同列混排**，第 5 章表只用 test 那套。**臂 B 的账本行 `rel_l2_*` 整列 NA**（MLP 的 metrics schema 与 PINN 不同；t5c22/t5c23 各 5 行本工单实测全 NA）⇒ 臂 B 的任何数只能走 `analyze_sweep.py` 的 supervised 根，**不许从账本或 PSV 索引重算**。本行原先两句作废并当场撤回：① "臂 A 只有验证集"；② "test 口径要等 `--eval-test-cases` 落到 runner pin 后重投 `--only-cells 20,21`"——该重投已执行（段 13：5 runs / 0 failures）。 | test 数 `T5矩阵test口径读数-20260926.md`（统括官侧 analyze 直读）；val 数与"NA 列"由本工单从 `T5矩阵run坐标索引-20260926.psv`（130 行=表头+129 条，sha256 `e9e4797e…`）现算 | 否 |
| B5 | 新增表 5-10 的**三臂对照列**（test 口径；与 D2 的臂 C 同一张表） | **填实 + 两面都写** | 表体（速度 `rel_l2_speed`，mean±样本 sd，n=5，test/mean-of-cases）：双模型（本文）0.028617±0.002162 ｜ 臂A 单网络联合 0.039161±0.009313 ｜ 臂B 纯数据 MLP 0.019436±0.006923（稠密档）；分层 5% 档依次 **0.031202±0.004515**（n=5，与配对同源）｜ 0.023144±0.004258 ｜ 0.051029±0.008870；臂C POD+LS 无种子散布、不训练，数值取 D2。表注三条缺一不可：① 全表 n=5、Wilcoxon 最小可达 p=0.0625 ⇒ 只报符号与幅度不报显著性；② 未做多重比较校正（本表 4 条配对）；③ 臂C 与三臂不同机时口径（零训练），其墙钟不可与本表 PINN 行直比；**④（9/26 定档甲新增）同一行并排的均值与配对差必须来自同一 `obs_seed` 子集**——稀疏档双模型只许写 0.031202（n=5），n=11 的 0.029925 归 5.4 节那一格；**⑤ 本表只有 `mean_of_cases` 一个口径**：臂 B 在 `pooled` 下结构上没有配对（其 `metrics_test_dense.json` 顶层无 `global_metrics` ⇒ pooled 键数=0），所以不得写『两口径都跑了』，要写就得给臂 B 两行另注 pooled 无对。**能写**：稠密监督下"去掉物理项与壁面构造反而更好"（0.019436 对 0.028617，低 32.1%，5/5 同号）、5% 稀疏下"该组合使速度误差低近四成"（0.031202 对 0.051029，低 38.9%，5/5 同号）、臂A 在稀疏档优于双模型（0.023144 对 0.031202，低 25.8%，5/5 同号）、"速度结构需要学习：零训练低阶基线在速度场上差 1–2 个数量级"。**不能写**："物理项普遍有益"（稠密档被自家数据否掉）、"优于所有基线"（稠密档输给臂 B）、任何"显著/统计上"字样。 | 同 B4；臂C 数值与工具见 D2 | 是→新增表 5-10（与 D2 合并为一张） |
| B3 | `:1219`（5.9 小结）「分阶段训练可以先稳定速度场，再通过压力模型和耦合阶段修正压力分布……双模型的价值不在于每个指标都更低，而在于把壁面约束、速度结构和压力一致性放到同一训练框架内」 | **整段替换（新措辞）** | 本章分析了双模型三阶段流程在二维稳态微流控流场重建任务中的表现。基线实验给出一次完整训练收敛后的误差水平；补充单网络对照后可以看到，本文未能在同观测、同特征、同网络规模的条件下观察到该结构的精度增益，其价值主要在于把壁面无滑移条件与压降要求写成可分离、可开关的构造项和损失项，便于在可视化工具中分别设置与查看。稀疏观测、采样策略和噪声实验进一步说明，有限测点下仍能恢复主要流场分布，但采样方式和观测质量会直接影响压力误差。 | 同上；创新点口径与 B1 一致 | 否 |

---

## 3. C 类 · 壁面残余必须改成"构造性指标"

| # | 位置 + 原句摘引 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| C1 | `:1215`「双模型的速度Rel-L2由4.11%降至3.21%，压力Rel-L2由7.53%小幅降至7.25%，壁面速度残余由23.33%降至0.01%以下；但压降误差由7.61%升至10.93%。因此双模型更有利于速度结构和壁面一致性」 | **换口径 + 撒归因（这条是 R1 的正面冲突点）** | 图 5-18 比较 5% 稀疏监督下的单网络 MLP 与双模型三阶段流程，两者使用同一批观测点、同一特征集与同一网络规模。在包含壁面点的整体口径下，双模型的速度 Rel-L2 为 3.21%、单网络为 4.11%，壁面速度残余分别为 0.01% 以下与 23.33%，压降误差分别为 10.93% 与 7.61%。需要说明两点：其一，整体口径的样本集中含 360 个壁面点，其速度真值约为 1e-30 量级，因此壁面残余的差距主要由硬壁面构造项保证，属于约束满足程度而非重建精度；其二，若只在 1261 个流体内部点上评价，双模型的速度 Rel-L2 为 0.0314、单网络为 0.0137，压力侧同样由单网络更低，方向与整体口径相反。因此本文不主张双模型在精度上优于单网络。 | 双 `RP/contraction_strict_geometry_sparse5_stagepde_20260503/evaluations/metrics_val_dense.json#case_metrics[0].rel_l2_speed=0.032084、wall_max_abs_u_pred=3.8395846786443144e-05`；单 `RS/contraction_single_mlp_geometry_sparse5_strict_20260503#summary.mean_rel_l2_speed=0.0410928、mean_wall_max_abs_u_pred=0.2332939、mean_pressure_drop_rel_error=0.0760912`；内部点口径见 v2 §0 R1（本轮复算）；构造项 `train_velocity_pressure_independent_strict_sparse.py:195-225` | 是→图 5-18 须拆成"内部点/含壁面点"两条读数（原数保留，新增列） |
| C3 | 图5-18 与 5.8（`:1215-1217`）＋必做1 回复 | **臂 B 两档方向相反，两条都要写** | 稠密档：纯数据 MLP（test/mean-of-cases）0.019436 优于双模型 0.028617，5 对配对全同号（Wilcoxon W=0，p=0.0625=n=5 最小可达；t=−3.308 p=0.0297，按预注册口径不写成显著）；稀疏 5% 档反过来：MLP 0.051029 vs 双模型 **0.031202**（双模型低 **38.9%**）——这里必须用五粒同源值，`0.029925` 是 n=11（含 T6 多点位）、只属于 5.4 的『分层 vs 均匀』那一格（见 B4 定档甲）。两句都须带 n=5、口径与「未做多重比较校正」。正文句：「在稠密监督条件下，加入物理项与壁面构造并未改善、甚至劣于同规模的纯数据网络；而在 5% 稀疏监督下该组合使速度误差下降约四成——说明其作用集中在观测信息不足的区间，而非普遍增益。」这为 B1 的「未观察到增益」补上了方向，但不构成显著性声称。 | `docs/revision/T5矩阵test口径读数-20260926.md` 臂B 两对配对段（n=5，由 `analyze_sweep.py --paired` 第二根产出） | 否 |
| C2 | `:983-984`（表 5-1 行「壁面u残余 3.84e-05」，对应节 5.8） | **改指标名与归类** | 指标项文字改为「壁面无滑移构造残差（构造性指标）」，并在表 5-1 中把它从"重建效果"分组移到"约束满足"分组；表注加一句：该值由硬壁面构造项直接保证，不反映流体内部精度。 | `metrics_val_dense.json#summary.mean_wall_max_abs_u_pred=3.8395846786443144e-05`；`:195-225`；298/360 行的预测速度精确为 0 | 是→表 5-1 指标项文字与分组，数值不动 |

---

## 4. D 类 · 5.6.2 消融换干净组读数

| # | 位置 + 原句摘引 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| D1 | `:1185`「加入几何增强编码后，测试集平均速度Rel-L2由0.5612降至0.0390，压力Rel-L2由11.7598降至0.1516」 | **换数（9/25 升级为全矩阵最强的一条：五种子配对）+ 声明两个口径 + 设一道前置核查** | 为检查几何增强编码的作用，本文固定壁面构造模式与训练预算，只更换输入特征集：basic 集（4 列，只能用软壁面惩罚）与 geometry 集（14 列，同一软壁面惩罚），各训练五个种子。速度 Rel-L2（**test / mean-of-cases**）由 0.539923±0.014799 降至 0.148723±0.011416，即 3.6 倍；同一对照的 val 口径为 0.51955±0.01322 降至 0.13622±0.01143（3.8 倍）——两列都进表，正文用 test 那一列；五对配对的差值全部同向（+39.58、+40.37、+35.49、+38.55、+37.67 个百分点），其 sd-误差比为 45.4（样本口径；格13 sd 0.013215、格14 sd 0.011429），是种子散布的约 20 倍。这是全矩阵中效应明显超出种子噪声的两条结果之一（另一条见 H4）。仅坐标输入无法直接表达点位与壁面、收缩段或入口剖面之间的关系，壁距、区域标签和轴向比例等特征补上了这部分信息。此前正文引用的 0.5612 与 11.7598 来自同时改变特征集与壁面构造模式的两条运行，不能把差异单独归因于几何增强编码，故予以替换；若采用 pooled（全部点合并计算一次）口径，同一对照为 0.5684 与 0.0471，两口径不可混用。**前置核查（写作闸门）**：basic 臂的 0.52 属"未拟合成功"而非"略差"，在正文把它解释为"该臂缺少可用的壁面执行机制"之前，必须先看其 `history.csv` 的损失轨迹是否连观测点拟合都没有收敛；若属收敛失败，措辞须改为"该设置在本文训练预算下未能收敛，因此该对照度量的是可训练性而非精度"。 | 读数 `docs/revision/T5矩阵配对读数-20260925.md` 第一节格13/格14 与第二节"特征消融"行；sd-误差比与倍数由本工单现算（0.51955/0.13622=3.814；38.332/(1.690/√5)=50.7）；旧数出处与 pooled 对照见 v2 附录 M「表5-1/5.6.2 消融」行；同壁面模式的历史对照 `docs/ablations/geometry_encoding_ablation_20260420/geometry_encoding_ablation_summary.csv#geometry_generalization_error`（0.5753→0.0308、0.2681→0.1843） | 是→图 5-14 换为该五种子配对的柱+误差棒；表 5-7 的 basic 行保留原值并加"单次训练"注 |

---

| D2 | 新增（必做1 臂 C，传统基线表 5-10） | **新增表 + 一句「两边都不好看」的定性** | 零训练的 POD + 观测点最小二乘（与格1/格4 同观测表；n_obs=63/74/56）：C-val speed **0.8102**（interior 0.7212）/ p 0.0309；C-test-1 speed 0.8251 / p 0.0584；C-test-2 speed 0.5344 / p **0.0176**。两面都要写：**速度场上它比 PINN 差 20–30 倍**（53–83% 对稀疏档约 2.5%，test 口径），但**压力场与我们同量级、C-test-2 上更好**（0.0176 对双模型稀疏档常见的 0.03–0.09）⇒ 必做1 不得写成「传统方法完全不行」（那是自捧），要写成「速度结构需要学习，压力幅值在该参数范围内可由低阶基线给出」。表注必须带：该臂**无种子散布**（一次运行、可重复），其墙钟与 PINN 墙钟不可直接比。**表注第二条（9/26 按统括官要求补：判据要和表注一起写，不能只报结论）**：阶数不是挑出来的——快照矩阵奇异值 `[2.486e+04, 3.505e+02, 1.847e+02, 9.053e+01, 1.194e-11, 4.317e-13]`（6 工况、自由度 8640），**第 4 个之后有 9–11 个数量级的悬崖**，在**声明的能量判据 0.9997** 下截断到 **r=1**（产物读回校验 `rank=1`）。表注文字：「基线阶数由保留 99.97% 快照能量的判据算出（r=1），非人工挑选；奇异值谱在第 4 阶之后下降 9–11 个数量级。」可加不可承诺的加强项：再跑一版 `--max-modes 4` 做并排敏感性（**未做**，正文不许写"已做阶数敏感性"）。 | 实例实测（pin fa81991：armC_selfcheck rc=0 wall=1 s；armC_real rc=0 wall=7 s，总 5,709 ms，无训练）；产物 `out/pod_baseline_contraction.json`；工具 `model/scripts/baselines_pod.py`；**奇异值谱与能量判据引 `docs/revision/T5矩阵test口径读数-20260926.md:92-97`（一手日志 `step_armC_real.txt`，此处为转引）** | 是→新增表 5-10 |

## 5. E 类 · 5.7 效率节整节重做

| # | 位置 + 原句摘引 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| E1 | `:1191`「本文选取收缩基线工况C-base与弯曲基线工况B-base，在同一服务器上分别测试PINN完整推理、PINN稀疏重建和FreeFem++ CFD求解的单次耗时」 | **补测量条件与口径变更声明** | 为比较重建方法与 CFD 求解的时间成本，本文在收缩基线工况 C-base 与弯曲基线工况 B-base 上分别测量单次推理、稀疏重建在线求解与 FreeFEM++ CFD 求解的耗时。测量条件：单线程 BLAS（`OMP/MKL/OPENBLAS/NUMEXPR_NUM_THREADS=1`）、`nice -n 10`、CPU 环境为魔搭 Notebook 8 核实例，计时器为 `time.perf_counter`，PINN 重复 15 次、CFD 重复 7 次并取中位数。本文另需说明口径变更：旧一轮计时使用 `/usr/bin/time` 的墙钟，该工具在本实例镜像中不存在，新计时改用 `date +%s%N` 整数运算，两者差异已计入 4.2 节的表注。 | `docs/benchmarks/pinn_vs_cfd_speed_benchmark_20260420.md:12-17`；`scripts/benchmark_pinn_vs_cfd.py:65-82,127-155`；v2 §8（镜像无 `/usr/bin/time`、无 `bc`） | 否 |
| E2 | `:1193` 与 `:1195`（0.111/0.242/0.481、0.117/0.394/3.552 与 4.34/1.98/30.44/9.01） | **换数（论文旧四组数无来源）** | 采用仓库内唯一可回读的计时产物：收缩流道 PINN 完整推理中位 0.083 s、稀疏观测在线重建 0.228 s、CFD 求解 0.501 s；弯曲流道分别为 0.118 s、0.390 s、3.638 s。对应单工况加速比为 6.04 倍、2.19 倍、30.72 倍、9.34 倍。 | 同上 md 表（该文件在 git 内仅 1 次提交 `565d346`） | 是→表/图 5-16、5-17 全部数值换成该组 |
| E3 | 新增段（放 E2 之后，取代 `:1195` 的结论句） | **新增训练入账与盈亏平衡工况数 K\*（9/25 起 B 用五种子中位数，不再用估计区间）** | 上述比值只反映单次计算成本，而 PINN 在使用前还需一次完整训练。按本实例五种子矩阵实测的训练墙钟**中位数**（全部由 run 级索引现算：收缩族稀疏各档 5% 档 53.7 s、1% 档 58.9 s、10% 档 53.6 s、15% 档 55.0 s，均匀臂 52.6–55.1 s；收缩族稠密 81.4 s（格1）；弯曲族稀疏 1% 107.8 s、5% 110.2 s、10% 113.5 s、15% 116.1 s；弯曲族稠密 **171.6 s（格19 五粒中位，单粒 s42=174.6 s 为区间上沿）**）与评估单价 1.533 s，若以 K 表示需要覆盖的工况数，则 CFD 累计耗时为 K·A，PINN 为 B+K·C，其中 A 为单工况 CFD 耗时、B 为一次训练耗时、C 为单工况推理耗时，盈亏平衡点为 K\*=B/(A−C)。据此，以完整推理口径计：收缩族稀疏观测需 129 个工况、稠密观测需 195 个；弯曲族稀疏观测 32 个、稠密 49 个。若按稀疏重建在线求解口径（C 更大），对应为 197、299、34、54 个。因此本文 6 倍到 31 倍的加速只在"同一模型覆盖大量工况"的前提下成立，单个工况的建模任务中 PINN 反而更慢。**该结论目前仍是混合口径**：A 与 C 取自旧主机 artifact（该主机产物已丢失、不可重测），B 取自本实例，两者不同机；同一台机上补测 A 之后（见 E5）才可去掉这一限定。 | 读数源 `docs/revision/T5矩阵配对读数-20260925.md` 第一节表下注（85 条 train 墙钟合计 5712 s ≈ 1.59 CPU·小时，`date +%s%N` 实测，镜像无 `/usr/bin/time`、无 `bc`）；A、C 见 E2；K\* 由本工单按上式现算 | 是→新增表 5-9（见下，数值已按实测单价更新） |
| E5 | 新增（5.7 表唯一还缺一手数的格子） | **补测 CFD 单机单价（排进下一上机批次，不新增机时）** | （不写进正文）在同一台 DSW 实例上用 `FreeFem++`（v4.9 已装成并可跑）对 C-base 与 B-base 各重复 7 次取中位，得到与 B、C 同机的 A 值，替换表 5-9 中来自旧主机的 A 列，并删去 E3 末句"混合口径"限定；同时按网格无关档位补一个 A'（细化网格的 CFD 单价），因为 A↑ 会使 K\*↓。 | 凭据 `paper-route2/实例环境实测-20260925.md`（FreeFEM v4.9 安装与可跑）；现用 A 值出处 `docs/benchmarks/pinn_vs_cfd_speed_benchmark_20260420.md:14-17` | 是→表 5-9 的 A 行换成同机读数 |
| E4 | `:1195`「对比传统方法，本文提出的PINN对于计算效率的提升效果十分显著」 | **撒回该句** | （删除。以 E3 的区间陈述替代，且必须同时给出 K\* 与"单工况更慢"这一侧。） | 同上 | 否 |

**表 5-9（E 类交付形状；B 已换成本实例五种子中位数，A/C 待 E5 同机补测）**

| 方法与口径 | 收缩 C-base (s) | 弯曲 B-base (s) | 备注 |
| --- | ---: | ---: | --- |
| CFD 单工况（A） | 0.501 | 3.638 | **旧主机 artifact，与 B 不同机 ⇒ 待 E5 补同机读数** |
| PINN 单工况推理（C，完整推理） | 0.083 | 0.118 | 不含训练 |
| PINN 稀疏观测在线重建（C'） | 0.228 | 0.390 | 含仿射校正与径向基插值，非第 5 章网络路径 |
| PINN 一次训练（B，稀疏 5% 档） | 53.7（格4） | 110.2（格16） | 本实例五粒中位，`date +%s%N`；格13/14 为 52.7/53.0 s |
| PINN 一次训练（B，稠密） | 81.4（格1 五粒中位） | **171.6（格19 五粒中位）** | 五粒区间 171.0–174.6 s；单粒 s42=174.6 s 只是区间上沿，此前表内写的 174.6 是单粒，现以五粒中位为准 |
| 盈亏平衡工况数 K\*（用 C） | 129（稀疏）/ 195（稠密） | 32 / 49 | K\*=B/(A−C) |
| 盈亏平衡工况数 K\*（用 C'） | 197 / 299 | 34 / 53 | 同上 |
| K→∞ 渐近加速比 | 6.04 / 2.19 | 30.72 / 9.34 | 推理 / 在线重建 |
| **臂 A（单网络联合）一次训练** | **139.6**（稠密档五粒中位，区间 136.7–146.7）／**131.2**（分层 5% 档中位，区间 129.5–142.2） | 无（本次只跑收缩族） | ≠ 双模型的 81.4 s，也≠ runner 里拍的 85 s；`K*=139/(0.501−0.083)=333`（推理）、`139/(0.501−0.228)=510`（在线重建） |
| **臂 B（纯数据 MLP）一次训练** | **199.3**（稠密档五粒中位，区间 **120.9–212.9**）／**101.5**（分层 5% 档中位，区间 72.4–123.4） | 无 | 由 `T5矩阵run坐标索引-20260926.psv` 现算（`metrics_root=supervised`，trainer 是 `train_supervised.py` 一条，计时窗含同调用内的 test 评估）。**该两档区间宽度都超过中位数的三分之一 ⇒ 只能引用区间，不许引用单个中位数**；散布来源未查（不同 trainer、不同批大小），正文若用它做 K\* 必须先说明这是未解释的散布 |
| 评估单价 | 1.533 | 1.533 | 本实例实测，用于说明评估机时可忽略 |

**单价归属规则（9/26 加，9/26 晚补臂 B）**：任何一臂的墙钟只允许引用**该臂该档自己的实测**；参数量对齐不等于机时对等——臂 A 用 0.973 倍参数却比双模型稠密档慢 1.7 倍（139.6 s vs 81.4 s），原因（每步多两次前向：观测集与稠密集各一次）与读数一起在表注里说明，不得合并成"同规模即同成本"。臂 B（无物理项的纯数据 MLP）稠密档反而最慢（中位 199.3 s）：**"去掉物理就更省机时"这句话在本实例上不成立**，三臂的机时排序是 双模型 81.4 < 臂A 139.6 < 臂B 199.3（稠密档）；臂 B 的区间跨 120.9–212.9 s，所以这条排序只写到"量级差别"为止，不写成精确比值。

---

## 6. F 类 · 新增表 4-4b 与措辞卫生

| # | 位置 + 说明 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| F1 | `:754` 之后（表 4-4 结束处） | **新增表 4-4b** | 表 4-4b 严格稀疏观测批的损失权重差异（在表 4-4 基础上另置零的项）：入口流量损失权重 inlet_flux_weight=0.0；出口压力损失权重 outlet_pressure_weight=0.0；压降损失权重 pressure_drop_weight=0.0；壁面损失权重 wall_weight=0.0。适用范围声明：表 4-4 与表 4-4b 分属两批训练，其权重不可互相代入；表 4-4b 的四项为整批共同配置，不是双模型与单网络之间的差异项。 | `results/pinn/contraction_strict_geometry_sparse5_stagepde_20260503/config.json#权重`；开关 `run_strict_sparse_experiments.sh:59-66`；44 条 argv 逐项比对见【DRY】§4 | 是→新增表，不改表 4-4 任何数字 |
| F2 | 「basic 臂无压力锚定」「四变量消融」类表述 | **核查后处置=本条不触发删句**：全文检索 `thesis.txt` 无"锚定"字样，仅 `:961` 出现 `basic(test平均)` 行名，故正文无需删句；该措辞存在于审计与既往讨论中，禁止被带入本次改写。另需在 4.4.5 与 5.6.2 的新表注里写明"两组之间变化的自变量为特征集与壁面构造模式两项"。 | 表注文本：本节对照组之间同时存在两项差异（特征集与壁面构造模式），因此读数差不可单独归因于其中一项。 | `grep -n "锚定" .scratch/thesis.txt` 无命中（本轮实测）；`train_velocity_pressure_independent_strict_sparse.py:171-173`（硬包络要求 `wall_distance_frac` ⇒ basic 与 hard 结构上不能共存） | 否 |

---

## 7. G 类 · B-test-2（θ=60° 转角泛化）——**统括官改判：补测，不删声称（9/25）**

改判依据：抛物入口的双模型主线 run `results/pinn/bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419/best.ckpt` 已在仓，其 `train_cases` 全为 θ=90°（B-base、B-train-1..3），而 `B-test-2` 是 θ=60° ⇒ 这正是"转角维度上的外推"，成本一次评估（实例评估单价 1.5–2 s）。因此 G 类的落点是**补一个读数并把它按 n=1 写进表**，而不是删掉声称。

| # | 位置 + 原句摘引 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| G4 | 9/26 补测读数到位后新增（本工单直读入库评估件，sha256 现算=de427996…80c，与统括官给的一致；`best.ckpt` 现算=61c71957…4228） | **统括官 9/26 裁决：主表加行（撤回我上一轮的"另立表 5-7b"改判），但我抓到的两处不同必须进表注** | 表 5-7 的 B 行是 `bend_strict_blunted_sparse5_stagepde_20260503`（削平入口、5% 稀疏）；转角补测用的是另一支模型 `bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419`（**抛物入口、稠密主线**）⇒ 两处条件都不同，把 B-test-2 直接塞进表 5-7 正是 I4 里我刚禁的那种跨批次横比。**加行可以，但表注必须写满这两条差异**：表 5-7 里既有的 B 行出自 `bend_strict_blunted_sparse5_stagepde_20260503`（削平入口、5% 稀疏观测），而新增的 B-test-2 行出自 `bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419`（**抛物入口、稠密主线**）⇒ 与旁边的 B-test-1 行相比差**两处**（入口剖面、观测条件），不是只差观测条件；`观测条件` 列（I4 加的那列）挡得住后一处，挡不住前一处，所以该行必须同时标 `模型批次=parabolic_mainline_v1_20260419`。表注收尾句：「本行与 B-test-1 行不同源（入口剖面与观测条件均不同），只可用于观察转角方向上的量级变化，不可与任何行作横比。」（我上一轮的"另立表 5-7b"方案**作废**，改为按此执行；同源于 20260419 的两行对照是：）B-test-1(θ=90°) speed **0.026912** / u 0.049348 / p **0.035984** / 压降 0.020407；B-test-2(θ=60°) speed **0.065474** / u 0.193153 / p **0.235530** / 压降 0.035673；倍数 speed ×2.43、u ×3.91、p ×6.55、压降 ×1.75。正文句：「在同一个稠密主线模型上，把测试几何从 θ=90° 换成 θ=60° 后，速度模 Rel-L2 由 2.69% 升至 6.55%、压力 Rel-L2 由 3.60% 升至 23.55%（同一次训练、同一评估口径，各一个工况，n=1）；转角方向的外推代价明显大于曲率半径方向，本文只报告这一个工况的结果，不作该维度的泛化断言。」　**（9/26 更正）不欠任何补测**：`evaluations/metrics_test.json` 顶层 `eval_cases=[B-test-1, B-test-2]`、`case_metrics` 里两个工况同在一枚文件 ⇒ 同一枚模型、同一次命令、同 `eval_source=dense`、同 `split_name=test`，表 5-7b 的两行本来就同批。**读数坐标的写法（本回合直读入库件实测，统括官 21:15 提醒后核）**：`case_metrics` 在文件里是**数组**（`"case_metrics": [`），每项自带 `case_id`；顺序位 0 → `case_id=B-test-1` speed 0.0269121…，顺序位 1 → `case_id=B-test-2` speed 0.0654741…。⇒ 引用只能写成 `case_metrics[0].case_id=B-test-1` / `case_metrics[1].case_id=B-test-2`，**既不许写 `case_metrics["B-test-2"]`（件里没有这个键），也不许裸写 `case_metrics[0]/[1]` 而不带 `case_id`** —— 装载成表以后它会呈现为 `0`/`1` 两个下标键（统括官实例侧读到的就是这种形态），把下标当工况名的消费者会把 0.0655 贴到 B-test-1 上。表 5-7 新行末尾加一句：「两项读数的工况名按 `case_metrics[i].case_id` 对应，不按下标」。（本机无 pandas，未复现"装载后是 0/1 键"那一步，故只钉引用写法、不描述对方的工具。） 唯一要保留的提示：**表 5-7b 的 B-test-1（0.026912）与表 5-7 里既有的 B-test-1 行（0.0274，出自 `bend_strict_blunted_sparse5_stagepde_20260503`）不同源**，两表之间不得互引数值。 | `model/results/pinn/bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419/evaluations/metrics_test.json#case_metrics[*]`（commit `8cdc43b` 入库，`eval_cases=[B-test-1, B-test-2]` ⇒ 同一模型两工况） | 是→表 5-7 主表加一行（带 观测条件=dense 与 模型批次 两处标注），不另立表 |
| G0 | 无（执行项，不在正文） | **补测安排** | （不写进正文）矩阵跑完后插入一次评估：`python3 scripts/evaluate_velocity_pressure_independent.py --family bend_2d --run-name bend_independent_geometry_notemplate_parabolic_mainline_v1_20260419 --eval-cases B-test-2 --split-name test --eval-source dense`。取 `evaluations/metrics_test.json` 的 `case_metrics[0].rel_l2_speed / rel_l2_p / pressure_drop_rel_error` 三个字段入表。 | v2 §「已定项 D1」与 §3 T7-①（ckpt 与 train_cases 已在仓；v2 原文记录 `train=B-base,B-train-1..3，θ=90°`）；单价见 v2 §0 T0 实测 | 是→表 5-7 新增一行 |
| G1 | `:411-414`（表 3-3 行：`B-test-2`、`4.0`、`60°`、`转角泛化测试工况`） | **说明列改为带证据档的说法（不再写"未评估"）** | 说明列写「转角泛化测试工况（单次评估 n=1；同模型 θ=90° 上为 0.026912/0.035984）」。**不写"未评估"，也不写"已验证可外推"**（两版都是越界：前者已过期，后者把 n=1 读成泛化结论）。 | 同 G0；`cases/bend_2d/data/B-test-2/field_dense.csv` 已在 | 是→表 3-3 说明列文字，工况行保留 |
| G2 | `:507`「测试集放在高曲率和不同转角条件上，以检验参数变化较大时的外推表现」 | **保留该句 + 加限定（补测后可留"不同转角"）** | 弯曲流道训练集以不同曲率半径比的基线工况为主，验证集用于观察训练阶段的泛化趋势；测试集放在高曲率与不同转角条件上，用来检查参数变化较大时的外推表现。其中转角维度只有 B-test-2 一个外推工况，其结果来自一次训练模型的单次评估（n=1）。 | 同 G0；转角维度在表 3-3 中确实只有 1 个 60° 工况 | 否 |
| G3 | 表 5-7 与 5.6.3（`:1170-1182`、`:1189`） | **表 5-7 加一行（统括官 9/26 定）+ 判据分支已照账执行** | 新增行四格照抄一手评估件：`B-test-2 / 弯曲流道 / dense（θ=60°）/ 0.065474 / 0.235530 / 0.035673`，并带 `模型批次=parabolic_mainline_v1_20260419`（见 G4）；表注写明「转角维度仅 1 个外推工况、单次评估、n=1」。正文 5.6.3 追加一句：「在 θ=60° 的 B-test-2 工况上，同一次训练的速度模 Rel-L2 为 6.55%、压力 Rel-L2 为 23.55%（θ=90° 的 B-test-1 在同模型上为 2.69% 与 3.60%）；该结果只来自单个工况的一次评估，本文据此说明模型在转角维度上未出现失效，但压力侧退化到曲率方向读数的 6.5 倍，因此不对该维度的泛化误差水平作统计性结论。」 **分支判据（9/25 写死，跑完不挪）**：该行压力 Rel-L2 >0.4 时追加范围声明并禁止 5.9 把转角列入已验证维度。**实测**：`rel_l2_p(B-test-2)=0.235530` ⇒ 不触发该分支；G1/G2 的基础措辞本身已带「转角仅 1 个外推工况、n=1」，无需追加。 | C-test-2 读数 0.0397/0.3127/0.3331 见 v2 附录 M 表5-3 行；`rel_l2_p` 字段名同 `train_supervised.py:622-663` | 是→表 5-7 新增行 + 表注 |


---

## 9c. J 类 · 「取回仓库即可重算 CFD 真值」这句：定档为**乙（改话）**（统括官 9/26 21:15 派单）

| # | 位置 | 处置 | 新文本（可直接粘贴） | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| J1 | 4.x 开源与复现声明（T11）＋ 6.x 局限性 ＋ 回复表第 15 行（自加格） | **改话 + 已知限制单列一格；本轮不改写 23 份 `.edp`** | 声明改为：「本文的 CFD 真值在同一台新机器上**逐位可复算**（5/5 工况去掉 CRLF 后 diff 行数 = 0）。复算入口分两档：有限 Re（NS）探针的主脚本 `C-base_ns_re{1e-3,1,10,50}.edp` 不含绝对路径，取回仓库即可跑；而入库的 23 份 Stokes 生成脚本（13 份弯曲族 + 10 份收缩族、共 46 处）把原作者机器的输出路径 `/root/dev/pinn_v3/…` 写死在 `ofstream` 语句里，**换机器直接跑会以 `err code 8` 退出**（FreeFem 既不建目录也不报权限错），需自建目录或改那一行写路径。生成器本身参数化正确，故本声明限定的是**入库文本**，不是生成流程。」局限性段同口径重述；任何位置不得保留无条件形式的「取回仓库即可重算全部 CFD 真值」。 | 计数与分布本回合现查：`grep -rl /root/dev/pinn_v3 --include=*.edp .` = 23 份；`grep -ro … \| wc -l` = 46 处；13 bend + 10 contraction；NS 四份主件命中 0，但 `C-base_ns_re1/probe_syntax.edp:88,96` 仍带 2 处 ⇒ 派单里「NS 四档自身可移植」**只对四份主件成立**，追加的语法探针不算，引用时别并进去。逐位凭据 `docs/revision/CFD真值同机复算-20260925.md:10,18`；派单 `docs/revision/派单-复现声明-edp硬编码旧主机路径-20260926.md` | 否 |
| J2 | （决策记录，不进正文）为什么不选甲 | **甲会消耗掉手里最强的那条复现凭据** | 改物 = 46 行参数化 ⇒ 23 份 `.edp` 字节改变 ⇒「5/5 工况逐位 diff=0」是对**旧字节**取的，改完即失效；9/29 前重取要重跑 5 工况并逐位重验，与「毕设侧不重跑」的既定裁决冲突。故走乙。**投稿侧若将来做甲**：同轮重取逐位凭据 + 附录保留旧件 sha256，否则等于用一条断掉的证据换一句更强的话。 | 旧字节所由见 `CFD真值同机复算-20260925.md:10,18` | 否 |

| J3 | 仓库对外文件 `README.md` / `docs/REPRODUCTION_GUIDE.md`（第四层「重新生成 CFD 真值」那一节） | **同口径的公开版限定；改动面在仓外的公开文件上，本工单只登记不代改** | 指南里「从几何与脚本出发重新生成 CFD 真值」（现 `docs/REPRODUCTION_GUIDE.md:38`）目前是**无条件**句 ⇒ 与 J1 同一处破口。最小改法：该节加两行注——「入库的 23 份 Stokes 生成脚本把输出路径写死为原作者机器的 `/root/dev/pinn_v3/…`（共 46 处），换机器直接跑会以 `FreeFem++ err code 8` 退出且不报权限错，需自建该目录或改 `ofstream` 那一行；`C-base_ns_re*.edp` 四份 NS 主件不含绝对路径，不受影响。」 | J1 的同一批现查计数（23 份 / 46 处 / 13 bend + 10 contraction）；命中行号本回合 grep 到 `:38`；本工单**未**编辑该公开文件（所有权不在本线，若要改请点一句） | 否 |

## 8. H 类 · 统计口径的全文声明

| # | 位置 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| H1 | 第 4 章末（表 4-7 指标定义之后）；**9/25 升级：这条要进正文，不能只放表注** | **新增"统计处理"一段（正文级，非脚注）** | 本文各表中每个配置对应一组固定训练预算下的运行结果，除特别说明外不含重复训练。需要明确本文能声称到什么程度：以配对符号秩检验为例，配对数为 5 时可达到的最小双侧 p 值为 2/2^5=0.0625，已在 0.05 之上，因此由五个训练种子构成的对照**在数学上不可能给出显著性结论**，本文对这些对照只报告均值与组内标准差，并逐个列出每种子的差值符号。要给出方向性的显著结论，需至少 8 个配对单元；本文的 T6 多点位批次已把「分层 vs 均匀 @5%」这一对做到 **n=11**（配对单位 =(obs_seed, train_seed)，孤儿 A=0、B=0），结果为 Wilcoxon W=25.0、双侧 **p=0.519531**、配对 t=−0.958、p=0.360848、平均差 −0.002071 ⇒ 这一处写「测了、没有差异」，不写「功效不足」。其余各格仍是 n=5（含 1% 档），不进入显著性陈述。在此口径下，全矩阵只有两处效应明显超出种子随机性：几何增强特征对照（3.8 倍，五对同向）与 1% 观测下的分层布点增益（见 5.4 节）；其余"谁比谁好"的差值均落在种子散布范围内，本文不对其下结论。 | 同上（v2 §5 统计设定；`docs/revision/SWEEP_DRYRUN.md` §7 的 n=5 最小可达 p=0.0625、n=8 为 0.0078）；两处效应见 D1 与 H4，其 sd-误差比由本工单现算（45.4 与 2.62，样本口径） | 否 |
| H2 | `:592` 附近与 5.5/5.6 各表读数处 | **新增噪声量级的实测限定（9/25 起改用配对数据，单次读数的类比只作辅证）** | 同配置只更换训练种子时，5% 稀疏档的组内标准差为 0.425 个百分点（速度 Rel-L2，五种子），而"分层 vs 均匀"两臂在同一档的配对差值均值只有 0.027 个百分点。因此本文任何单次读数的两臂对比，其差值小于该抖动量级时一律表述为"未观察到稳定差异"。 | `docs/revision/T5矩阵配对读数-20260925.md` 第一节格4/格8 行（mean±sd）与第二节"表5-5 的核心"行；辅证：T0 的 seed 42→43 单次读数（−35.4%） | 否 |
| H4 | 5.4 节 `:1120`、`:1123`（图 5-9 说明）、`:1126`（图 5-10 说明）、`:1130`（图 5-11 说明）——现文把"分层在压力上占优、速度上略逊"当作结论陈述 | **改写成一条曲线而不是一句断言（9/25 新增，本矩阵第二能写的结果）** | `采样策略的效果随观测率变化。在 1% 观测下，分层布点的速度 Rel-L2 为 3.25%、均匀布点为 4.48%，五对配对的差值均值为 −1.23 个百分点（相对均匀臂改善 27.5%，sd-误差比 2.62（样本口径），五个种子中四个同向），可以判定分层布点在极稀疏观测下有效。在 5% 观测下，两臂分别为 2.55% 与 2.58%，配对差值均值 −0.027 个百分点（sd-误差比 0.17），且单个反例种子（+0.62 个百分点）与其余四个种子之和（0.75 个百分点）同量级、剔除后均值也只有 −0.19 个百分点，因此该档两臂不可区分。10% 与 15% 档同样落在噪声内。而 5% 这一对在 T6 多点位批次已达 **n=11**（孤儿 0），Wilcoxon 双侧 p=0.519531、配对 t p=0.360848 ⇒ 该档写法从「落在种子散布内」升为「配对检验测过、无差异」。本文据此把结论写成"分层布点的增益只在观测率不高于 1% 时可区分，观测率上升后增益落入训练种子带来的抖动范围"，而不是"分层采样具有增益"。` | 同上读数源第一节格3/格5/格6/格7/格9/格10 与第二节 1% 行；本工单现算的 sd-误差比与两种分母（27.5% 对均匀臂、38.0% 对分层臂）——正文一律用"相对被改进的那条臂"即 27.5% | 是→表 5-5 增"观测率"行（1%/10%/15%）与 mean±sd 列 |
| H5 | 图 5-8（`:1114` 分区误差图）与 5.4 的"结构核心区覆盖"解释 | **保留为机理线索、降为未证** | 图 5-8 所示的分区误差差别仍可作为解释 1% 档增益的机理线索，但需注明：该图只对应一次训练，分区误差与整体误差之间的因果链本文未做干预实验（要证成需要单独设计配额扰动）。 | 同上；H4 的配对读数 | 否 |
| H3 | 全文措辞替换清单：`:1045`、`:1049`、`:1081`、`:1120`、`:1148`、`:1183`、`:1214`、`:1215`、`:1219`、`:1017` | **换词** | 把这些句子中的「优于/表明/说明…更/体现出」在 `n=1` 处统一替换为「在本次训练中，……的读数更低」；把「说明模型可以处理……」替换为「在本次训练中，模型在与训练集相邻的参数组合上给出的读数误差量级较低」。禁止保留无对照的方向性动词。 | v2 §0 R4；【DRY】§7 | 否 |

---

## 9. I 类 · 表注必须点名是哪个速度指标

| # | 位置 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| I1 | 表 5-4（`:1055-1080`）与表 5-7 弯曲行（`:1170-1182`）的列名「速度场L2误差」 | **加表注点名** | 表注：本表「速度场L2误差」列为速度模的相对 L2 误差，即对 ‖(u,v)‖ 逐工况计算 `‖pred−truth‖/(‖truth‖+1e-12)`；它与表 5-3、5-5、5-6 中的 u 分量相对 L2 误差不是同一指标。在弯曲流道上两者差异约为 1.7 倍（本表四档的速度模读数为 0.0248/0.0228/0.0237/0.0228，对应的 u 分量值为 0.0414/0.0388/0.0401/0.0378），因此跨表比较速度指标时必须先核对口径。 | `evaluations/metrics_val_dense.json#global_metrics.rel_l2_speed` 与 `#rel_l2_u`（run `RP/bend_strict_blunted_sparse{1,5,10,15}_stagepde_20260503`）；定义 `scripts/train_supervised.py:361-362,622-663` | 是→列名改「速度模L2误差」，数值不动 |
| I2 | 表 5-3、5-5、5-6 与表 4-7 指标定义 | **补口径列 + 聚合声明** | 表 4-7 增设两行定义：「u 分量相对 L2（`rel_l2_u`）」与「速度模相对 L2（`rel_l2_speed`）」，并注明收缩流道在两种口径下数值接近、弯曲流道相差约 1.7 倍。同时声明聚合方式：本文分栏均值采用"逐工况先算、再对工况取算术均值"（mean-of-cases）；若改为把全部点合并计算一次（pooled），同一对照的差值会变化（例如消融对照的速度读数 0.5612/0.0390 对应 pooled 口径的 0.5684/0.0471）。 | `compute_case_metrics`（`train_supervised.py:622-663`）与 `global_metrics` 字段；数值对照 v2 附录 M「表5-1/5.6.2 消融」行 | 是→表 4-7 增行 |

---

## 9b. 补漏与裁定新增行（关键词回查补出 A15-A17；统括官 9/25 裁定补入 I3、I4）

| # | 位置 + 原句摘引 | 处置 | 新文本 | 凭据 | 动表数 |
| --- | --- | --- | --- | --- | --- |
| A15 | `:833`「模型配置上，本文统一输入特征、输出形态与阶段内PDE约束……」、`:861`（第 4 章实验矩阵里的"阶段内PDE约束"项） | **换词（与 A9/A11 同一术语）** | 把"阶段内PDE约束"统一替换为"阶段内残差惩罚项"；`:861` 的实验矩阵行名同步替换，行内容与开关不变。 | 术语与 `:195-225,382-383` 的实际对象一致；本轮检索显示该词仍出现在 `:833`、`:861`、`:986` | 是→行名文字 |
| A16 | `:986`（表 5-1 行「阶段内PDE约束」） | **换标签** | 表 5-1 中该行改为「阶段内残差惩罚项（启用）」，与表 5-8 的 A11 改法一致；同行数值（`:987-990` 的最终压力场 L2 误差 0.1041 等）不动。 | 同 A12 | 是→表 5-1 行名 |
| A17 | `:847`「含噪观测鲁棒性实验」、表 5-1 的 `:930`、`:935`、`:940` 行名「含噪鲁棒性」、5.5 节 `:1148` 与相关结论句 | **降级术语 + 按 n=1 降噪 + 补上"只有一种噪声实现"这一层限定（混淆已排除，两行不作废）** | 标题与行名统一改为「含噪观测下的稳定性」。5.5 节结论句换成：`在 5% 观测点上叠加 1% 与 5% 两档高斯噪声、且噪声臂与无噪臂使用同一批观测点（仅数值被扰动）的条件下，五个训练种子的配对差值均值分别为 +0.082 与 −0.102 个百分点：叠加 1% 噪声后本次实现的速度 Rel-L2 反而下降约 3%，叠加 5% 噪声后上升约 4%。两档的组内标准差都很小（0.018 与 0.023 个百分点），说明噪声响应曲线在原点附近是平坦的，本文所测的这一量级噪声不承重。` 并必须附一条限定：`上述五个同向的配对只约束训练随机性；每一档噪声对应的是单一噪声实现（一个固定随机种子下的 63 点扰动），因此"加噪后误差反而下降"不能推广为"任意噪声实现都会如此"，它同样可能来自这一次实现的偶然性。要覆盖数据层随机性，需要对同一噪声强度另取多套观测点实现。` 同时删除 `:11` 把"对噪声鲁棒性强"列为本文方法优点的说法。 | 配对读数 `docs/revision/T5矩阵配对读数-20260925.md` 第二节"噪声 1%/噪声 5%"两行（格4→格11/格12）；**混淆核查（本工单直查代码）**：噪声由数据生成阶段写入 `cases/…/obs_sparse_5pct_noise_{1,5}pct.csv`，种子为 `args.seed+300+noise_pct`（`model/scripts/generate_contraction_case.py:156`），基线点集用 `args.seed+5`（`:151`）与干净档 `args.seed+pct`（`:140`）在 5% 档同为 47 ⇒ 与训练种子无关；训练侧 `--seed` 只进 `random/np/torch.manual_seed`（`train_velocity_pressure_independent_strict_sparse.py:103-106`）⇒ 配对里只变动 train_seed | 是→表 5-1 行名与表 5-6 增 mean±sd 列（原单次读数保留为参考） |
| I3 | 表 5-1（`:872`–`:1007` 多处「速度场L2误差」）、表 5-2（`:1024`）、表 5-3（`:1020`） | **统一表注，不改列名** | 表注：本章凡未标注"速度模"的「速度场L2误差」均指 u 分量的相对 L2 误差（`rel_l2_u`）；弯曲流道的表 5-4 与表 5-7 为速度模口径（见 I1）。两口径在收缩流道上实测差在 1e-6 量级以内（格4 五种子逐位比：`rel_l2_u`=0.0320857 对 `rel_l2_speed`=0.0320840，相对差 ~5e-5），在弯曲流道上相差约 1.7 倍；因此收缩族的表可以不加区分，弯曲族必须点名。 | 定义与字段见 `train_supervised.py:361-362,622-663`；弯曲族 1.7 倍的成对读数见 I1 凭据（0.0248/0.0228/0.0237/0.0228 对 0.0414/0.0388/0.0401/0.0378） | 是→仅加表注 |
| I4 | 表 5-7（`:1158-1182`）：C 行是 dense、B 行是 5% 稀疏，却同列比较 | **立即改文字（统括官 9/25 定，不等矩阵）** | 表 5-7 增设「观测条件」列并逐行填实（`C-test-1 dense`、`C-test-2 dense`、`B-test-1__ip_blunted 5% 稀疏观测`、补测后的 `B-test-2 dense`），表注加一句：`本表各行观测条件不同，dense 行与稀疏行的误差不可横向比较，只可用于观察同一观测条件下的工况间差异。` 5.6.3 `:1189` 中把 B 行与 C 行放在同一句里比较的写法拆成两句，各自只在自己观测条件内陈述。 **本列只挡住"观测密度"这一维，挡不住入口剖面**：表 5-7 里既有的 B 行出自削平入口批次（`bend_strict_blunted_sparse5_stagepde_20260503`），而 G4 新增的 B-test-2 行出自抛物入口的稠密主线批次（`…parabolic_mainline_v1_20260419`），所以「观测条件」列必须与 G4 的「模型批次」列**并存**，两列缺一不可（统括官 9/26 认下这一点，并撤回了自己原先"只填 dense"的措辞）。 | v2 附录 M 表5-7 行已标「⚠ C 行 dense、B 行 5%，同名指标不可横比」；表内现文即含「dense」「5%稀疏观测」两样文字（`:1163`、`:1180`） | 是→表 5-7 新增列，数值不动 |


---

## 10. 改完之后必须全绿的自检（同一批 grep 断言）

| 断言 | 命令意图 | 期望 |
| --- | --- | --- |
| 全文不再出现"尺度平衡"描述本文实现 | 检索 尺度平衡 | 只允许出现在"未做尺度平衡"的否定式句子里 |
| 不再出现"阶段内PDE约束"作为术语 | 检索 阶段内PDE约束 | 0 命中（改完为"阶段内残差惩罚项"） |
| 摘要/结论无"物理约束…因此…"因果句 | 人工读摘要+结论 | 0 命中 |
| "十分显著" | 检索 | 0 命中 |
| "鲁棒性" 只保留在文献综述对他人方法的描述里 | 检索 鲁棒性 | 本文自身实验的标题与行名 0 命中（`:11` 若保留须改为"在本文实验中……"） |
| 表 5-1 无"壁面u残余"裸指标名 | 检索 壁面u残余 | 0 命中（改为构造残差并归组） |
| 表 5-4/5-7 列名已点名速度模 | 检索 速度模 | ≥2 命中 |
| G 类已按"补测"落表（不是"未评估"） | 检索 未纳入双模型流场的评估 / 预留测试工况 | 0 命中；表 5-7 须有 B-test-2 行且带 n=1 限定 |
| 表 5-7 的横比已封住 | 检查表头 | 有「观测条件」列**和**「模型批次」列（I4 + G4，两列缺一即视为未封住）+ 不可横比表注 |
| 显著性口径在正文而不在脚注 | 检查第 4 章是否有"统计处理"独立段 | 有，且含 0.0625 这个数（H1） |
| 表 5-5 已升为曲线结论 | 检索 分层 | 5.4 节出现"1% 可区分、5% 及以上落入种子抖动"式表述；不得再出现单点的"分层采样有增益" |
| 矩阵读数的坐标可指 | 每条新数是否指向 .psv 的行或 run 目录 | 逐条可指；复算链见 §12 第 8 条，封存账本不回传是**决定**不是待办 |
| 同一行的均值与配对差同源 | 检查表 5-10 稀疏档双模型那一格 | 只能出现 **0.031202 (n=5, obs_seed=0)**；出现 0.029925 即判错（那是 n=11 的另一个样本集，只许留在 5.4 的 n=11 那一格，且那一格两臂同为 n=11） |
| 表 5-10 只用 mean_of_cases | 检索「pooled」「两口径」 | 臂 B 两行不得出现在 pooled 口径里（其评估件顶层无 `global_metrics` ⇒ pooled 键数=0）；表注⑤已钉 |
| 表 5-10 与三臂对照只用 test 口径 | 逐格检索 0.033370 / 0.013711（这两个是**验证集**的臂 A 数） | 第 5 章任何表、任何正文句里 0 命中；三臂四行（双模型/臂A/臂B/臂C）齐全，臂 B 两格只能引 `T5矩阵test口径读数-20260926.md` 第二节，**引账本或 .psv 即判错**（那 10 行 `rel_l2_*` 整列 NA，见 SWEEP_DRYRUN §11.5） |
| 三臂的机时没有被"同规模即同成本"合并 | 检索 表 5-9 的臂 A/臂 B 行 | 臂 A 139.6/131.2、臂 B 199.3/101.5（区间都要带上）；稠密档排序写成 双模型 81.4 < 臂A 139.6 < 臂B 199.3，**不得**出现"去掉物理项可省训练机时" |
| sd 全文只一把口径 | 检索 总体标准差 / ddof | 只允许出现在口径声明句与括注里；同一张表内不得两把并存 |
| 表 4-4b 存在且表 4-4 数字未变 | 逐格核对 | 4-4 数值与原文逐位相同 |
| 复现声明已限定、且限制成格 | 检索「取回仓库即可」 | 出现处必须带「NS 四份主件」限定；`err code 8` 那条限制在回复表第 15 行有独立一格（J1） |
| 表 5-7 新行的工况名来源可查 | 对照 `case_metrics[i].case_id` | 正文/表注里不得出现 `case_metrics["B-test-…"]` 这种按键名的引用；下标必须与 `case_id` 成对出现 |
| 全文 0.5612 / 11.7598 不再作为消融结论数 | 检索 | 只在"此前正文引用"的说明句里出现 |

---

## 11. 审稿意见逐条回复表（14 行）

档位定义：**改写** = 只动文字与表注；**重跑** = 需要新训练/新评估（本轮一律不承诺时间）；**撤回** = 删声称。

| # | 审稿原话（要点） | 处置档位 | 落盘点 | 未做的代价与延期申请 |
| --- | --- | --- | --- | --- |
| 1 | 创新点 1 声称双模型 PDE 耦合带来精度优势 | 撤回 + 改写 | 摘要、`:24`（B1）、`:1217`（B2）、`:1219`（B3） | 不做的代价：创新点与产物不符，属一票否决项。无需延期 |
| 2 | 表 5-5 单次读数即下"分层优于均匀"结论 | 改写（限定 + 声明） | H1、H2、H3；5.4 各句 | 不做的代价：统计口径仍不成立。无需延期 |
| 3 | 图 5-18 的对照不对等（含壁面点、特征集不同） | 改写 + 换口径 | C1 | 补内部点/含壁面点两列即可完成，无需重跑 |
| 4 | 5.6.2 消融同时改变两个变量 | 换数 | D1 | 用仓库内已有对照组，无需重跑 |
| 5 | 式 2-9 声称做了尺度平衡 | 撤回 + 事实陈述 | A1–A3、A4–A5 | 不做则与代码可核事实直接冲突。无需延期 |
| 6 | 表 5-8"不启用"行无 run 产物 | 撤回归因（本轮）+ 补齐已在途 | A11、A12；§12 第 4 条 | 已排程：T5 格2（`rev2609_t5c02__s42..46`，5 个种子）跑完即有一手产物，届时该行按 mean±std 重写并删证据档脚注；未完之前保留"派生产物"脚注，不假装一手 |
| 7 | 5.7 效率不含训练成本、结论夸大 | 换数 + 撤回 | E1–E4 | 全部数值已有实测与产物来源，无需重跑 |
| 8 | 表 4-4 与 strict 批权重串档 | 新增表 | F1 | 新增表 4-4b，不改旧表 |
| 9 | θ=60° 转角泛化是否被检验 | **已补测，且明确退化（9/26 读数到位）** | G0、G1、G2、G3 | 已评：B-test-2 在同模型上 speed 0.065474 / p 0.235530，对同模型 B-test-1（0.026912 / 0.035984）是 speed ×2.43、p ×6.55 ⇒ 回复句：「转角维度的外推已补测；结果显示该方向上误差量级明显上升（尤其压力），本文只报这一个工况的一次评估、不作泛化断言」。分支判据（p>0.4 才追加范围声明）实测 0.2355 不触发，判据不回头改；单价 1.533 s（一次 eval） |
| 10 | 指标名称与口径混用（速度 L2 有两义） | 改写（表注点名） | I1、I2 | 纯文字工作，无需重跑 |
| 11 | 含噪实验被解释为"物理项起作用" | 撤回 | A6 | 无需延期 |
| 12 | 壁面残余被当作学习成果 | 改写 + 归组 | C2 | 无需延期 |
| 13 | 建议补传统方法基线（插值/解析）与统计检验 | **改写 + 已补齐（三件套已写码、已跑完，test 口径读数已入库）** | 本文 6.x 局限性一段 + 新表 5-10（臂 A/B/C 对照，见 B5 与 D2） | 三件套均已交付并有实测：臂 A 单网络联合 PINN（`train_joint_upnp_pin.py`，参数量比 0.973 过闸门，两档各 5 粒跑完 480 epoch）、臂 B 纯数据 MLP（5 粒）、臂 C POD+观测点最小二乘（`baselines_pod.py`，零训练、rc=0、总 5,709 ms、rank=1 由能量判据 0.9997 算出）。**原先那句"上机前正文不得写与经典基线比较"的限制已解除，但只解除到 B5 允许的范围**：可写"速度结构需要学习——零训练低阶基线在速度场上差 1–2 个数量级"，可写"稠密档去掉物理项反而更好、5% 稀疏档方向反转"；**不可写**"优于所有基线"（稠密档臂 B 优于本文，臂 C 在 C-test-2 的压力误差 0.0176 也优于本文常见量级）、不可写任何"显著"字样。统计检验仍按 H1：只报 mean±样本 sd 与逐种子符号（n=5 配对 Wilcoxon 最小可达双侧 p=0.0625），未做多重比较校正，无需延期 |
| 14 | 建议修正物理项实现（导数量纲、全链求导） | 另文（路线二） | 本文不改实现、不改读数；局限性段声明"残差项的归一化与求导链在本实现下不完备，其后果已在 2.2.1 与 5.8 量化" | 改式子=换物理设定=第 5 章全部作废重跑，5 天内不可行，故归入后续工作 |
| 15 | （**自加，非审稿原话**）复现声明的可移植性边界 | 改话 + 单列限制（J1） | 4.x 开源与复现声明、6.x 局限性 | 本轮不重跑、不改写 23 份 `.edp`：声明限定为「NS 四份主件取回即跑；Stokes 入库文本需自建目录或改一行写路径」，并把这条写成表格里的一格而不是脚注。不做的代价：审稿人按声明直接跑 `.edp` 得到 `err code 8`，由我们自己发现的限制变成对方抓到的失配，代价翻倍。|

---

## 12. 未做与限制（不猜）

1. **A3/A13 里"其余 12 列被当常数"的量化影响**：【物审】§5.1 已声明该换算是反推（无 checkpoint 前向复核）。正文只写"作为常量输入处理"这一事实，**不写**"因此误差有多大"。
2. **`:1091` 与 `:1220` 的"5% 已接近稠密配置"（9/26 更新：矩阵已到终态 19 格 × 5 种子 = 95 次成功 + 1 次失败重试）** — 弯曲族同批次对照已齐且本工单已独立复算（从 run 级索引现算，非转抄汇总表）：稠密（格19）0.024347±0.001956 vs 分层 5%（格16）0.025082±0.002269（均样本 sd），五对配对差 −0.074 个百分点、Δsd 0.086（样本口径，sd-误差比 1.92）。**格19 现为五粒**：**0.024347±0.001956**（样本 sd；首版挂的 0.02252 是 s42 单粒，五粒里也不是最好，s43=0.02217）。弯曲稠密墙钟同步改为**五粒中位 171.6 s**（单粒 174.6 s 是上沿）。对稠密的逐种子配对差（正=稀疏更差）：分层 1% **+0.00212±0.00067（+8.7%，稠密好 5/5）**、5% +0.00074±0.00086（+3.0%，4/5）、10% +0.00067±0.00076（+2.8%，3/5）、15% −0.00020±0.00052（−0.8%）⇒ `:1091` 用 **test 口径**写：「弯曲族内分层 5% 为 0.032766±0.004282、稠密为 0.030402±0.003318，稀疏档比稠密差 +0.00236（+7.8%）」；上面那组 +0.00074（+3.0%）是 **val 口径**的配对差，只能用于说明「两档差值与种子散布同量级、方向为稠密略优」，不得当 test 表数值（test 下差距更大，见 §0 口径规则）；跨批次比较的老问题（表 5-4 内无 dense 行）仍要靠"只在同批次内比"的表注封住。**改写时禁止**把弯曲族的对子挪去支撑收缩族的"5% 已接近稠密"。落点：`:1091`、`:1220`。**n 的取法（9/26 定，两版不得并存）**：扩索引后 `t5c04/t5c08` 含 T6 多点位，全 obs_seed 算出来是 0.023602±0.004616 / 0.027019±0.002854（**n=11**）；只筛 `obs_seed=0` 才是主矩阵那 5 粒：0.025508±0.004250 / 0.025772±0.001713。规则：**与主矩阵同框的 mean±sd 一律筛 obs_seed=0（n=5）**；只有 5.4 的"分层 vs 均匀 @5%"配对用 n=11（配对单位=(obs_seed,train_seed)），并且那张表必须显式写 n=11。本工单里 0.02551/0.02577/0.425pp 之类的数都是 obs_seed=0 口径，引用时带 `(n=5, obs_seed=0)`。
**写死的限定（照《T5矩阵配对读数》第四节原样带上）**：只允许写到"差距小于种子散布"，**不许写等价/无差异**（未做 TOST；n=5 配对 Wilcoxon 最小可达双侧 p=0.0625>0.05）；**不得外推到收缩族**（那侧 分层10% −0.00150±0.00400、分层5% +0.00189±0.00494，符号都不稳）。凭据：`docs/revision/T5矩阵run坐标索引-20260926.psv`（现 130 行=表头+129 条，sha256 `e9e4797e…58230a`；**索引行=账本行≠唯一 run**：`t5c01__s44`、`rev2609b_t5c21__s42` 各占两行（先 rc=1 后 rc=0）⇒ 取终态必须按 (run, obs_seed) 取最后一行，得 **127 个终态成功 run**）；墙钟合计 95 条 rc=0 = **7152 s ≈ 1.99 CPU·小时**（本工单现算；统括官给 7153 s，差 1 s 属四舍五入口径）（收缩 4054 / 弯曲 3098）⇒ T5 三列时间表用这个数。
3. **E3 的 K\* 输入是混合档**：B 已换成本实例五种子中位数，但 A、C 仍来自旧主机 artifact（该主机已丢失、不可重测）⇒ 混合口径的限定句必须留在正文，直到 E5（同机 FreeFEM 补测 A）完成。K\* 只作量级结论，按"区间 + 公式"给，不给单点。
4. **表 5-8 的"不启用"臂——原"无凭据"判定已解除（9/25）** — 审计原判"该行只有 CSV、无 run 目录、无生成脚本"，现已由一手产物取代：`rev2609_t5c02__s42..46` 五个种子的 run 目录已产出（格2 = 稠密·阶段内 PDE 关），待 `segment_01.tar.gz` 回流入库后逐个挂 `results/pinn/<run>/metrics.json` 坐标。在此之前 A12 用甲版（单次读数、只换解释句），到位后换乙版（五对配对、判"无可测贡献"）。凭据：`T5矩阵配对读数-20260925.md` 第二节首行与其派生判读第 2 条。
5. **表 5-7 的 C 行 dense 与 B 行 5% 横比（统括官 9/25 定：立即改文字）** — 已升格为工单条目 **I4**（新增"观测条件"列 + 表注禁止横比），不再属于未做项。
6. **噪声档的覆盖不完整**：五种子矩阵跑的是 1% 与 5% 两档（格11/格12），**论文表 5-6 原有的 3% 那一行仍是单次读数**（且来自 obs_seed=0 的同一批点位）；表 5-6 换列时要把这一格标成 n=1，别让它混进"五种子"的口径。另外每一档噪声只有一种噪声实现 ⇒ A17 的"数据层随机性未覆盖"限定不可删。
7. **本工单所有段落号取自 `.scratch/thesis.txt`**，与 docx 页码/样式无关；粘贴回 docx 时需按样式重排（表 4-4b、表 5-9、表 5-7 新增列需新建表框）。
8. **矩阵读数的挂坐标规则（9/26 版）**：终态账本已回读（95 次成功 + 1 次失败重试，`obs_seed=0` 全矩阵固定 ⇒ 配对只换 train_seed），坐标源已入库（统括官 push；**9/26 晚刷新**：本工单首版引的 96 行版 `c80d6f5537a09509eb6487c14c22f9a13d96571049a483b1734f1e17b0f59eaf` 已被同名文件取代，以下为现算现值）：`docs/revision/T5矩阵run坐标索引-20260926.psv`（130 行=表头+129 条账本行 = **127 条 rc=0 + 2 条失败重试**（`rev2609_t5c01__s44__o0`、`rev2609b_t5c21__s42__o0`，各先 rc=1 后 rc=0）⇒ 唯一 (run,obs_seed) 为 **127**，取终态必须按键取末行；sha256 `e9e4797ec32d27a0293d544e6e4518f03a00e4921cc103fed4c51e38a058230a`；`metrics_root` 两类 pinn 119 / supervised 10，**supervised 那 10 行的 `rel_l2_*` 整列 NA** ⇒ 臂 B 不在此索引里取值，见 B4）。本工单的格1/2/4/8/13/14/16/19 均值与四条配对差已从该文件现算复核并与汇总表对齐（表5-8 −0.002 pp、消融 +38.333 pp、5% 档 −0.026 pp、1% 档 −1.234 pp）。**证据层次（照《T5矩阵配对读数》§四 的边界声明写，不得升格、也不得写成待办）**：正文与回复表引用一律指向该 .psv 的行（已入库：commit `c290e95`，其 96 行版 sha256 `c80d6f55…`；**9/26 扩到 129 行后被同文件取代**，现算 sha256=`e9e4797ec32d27a0293d544e…58230a`，凡引用旧哈希的文档都要改），`run` 即 `model/results/pinn/<run>/metrics.json` 的目录名。复算链只到这一层：**复算正文读数 → 用该 .psv；复算 .psv → 用实例侧封存的原始账本** `out/segment_01.tar.gz`（106,564 B，sha256 `f2e292f850adec28001539aec95b9d8e5c0de3b4a6defaa87a802cfa9110b80b`，9/25 20:04，在 NAS），配 `model/scripts/ops/build_runs_index.py` @ `1a6044f`。**该封存账本没有逐位回传，这是已知代价后的故意不做**（再取一次的收益只是把已经能对上的 96 行再对一遍），不是漏做：正文与回复表不得把它写成待办，也不得暗示读数不可核。我本轮独立核过的只有三件：由汇总表数值现算派生量（sd-误差比、倍数、两种相对分母、K\*）、噪声 RNG 与 train_seed 解耦（查代码）、表5-8 配对差与格1/格2 均值差自洽。该文首稿曾把格9/10/11 整体错行并配过一句编造的解释（已删）——这正是"必须回到原始产物挂坐标"的理由。
