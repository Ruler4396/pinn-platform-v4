# 交叉复核 C1/C2/C3（2026-09-24）

被复核方：本审计上一轮产物 `独立审计报告-v1-20260924.md`（下称【审计】）与方案线产物 `修订方案-v1-20260924.md`（§0 判决表 / §6.3 / §9 情形 3·4 / 附录 M）。
本轮实现全部为只读、自写，**未打开、未复用** `.scratch/plan_recheck_R1/R2/R3.py`、`recompute_R3_analytic/bend_profile/bend_vs_pinn.py`、`R1_out.txt`。脚本清单见文末附录。

---

# 结论前置

| 项 | 判决 | 一句话 |
| --- | --- | --- |
| **C1** | **【我错，方案线对】** | `run_strict_sparse_experiments.sh:59-66` 在 `run_dual` 公共体内、不随 `feature_mode` 分支；我用 `bash` 桩化实跑取到两臂完整 argv，逐 flag 比对：三项权重两臂**都是 0.0**。臂间变量只有 **2 个**（特征 4↔14、壁面 soft↔hard），不是 4 个。【审计】里"basic 臂无压力锚定"是把**两臂共同配置**当成了**臂间差异**。 |
| **C2** | **读数【成立】（与方案线零偏差）；但"换新主战场"的推论判【口径不够，重做后可定】** | 弧段解析 0.0357 / 双模型 0.0159（5%）与 0.0162（1%）我独立复现到小数第 4 位；等预算反对也被我证伪（1%=76 点时 PINN 仍优 2.20×，且 76→1145 点误差不变）。**但我补了一个零训练的 3 参数曲率剖面臂，它在弧段拿 0.0133（B-val）/0.0192（B-test-1），即追平或反超 PINN** → "PINN 在弧段有净增量"目前只相对于**一个被刻意削弱的抛物线基线**成立，而压力段解析仍优 35–40×。 |
| **C3** | **【成立】** | 9 工况全量复算：u 0.000190–0.001414、speed 0.000189–0.001321、p 0.000243–0.000942、压降 0.000078–0.000855，与方案线给的区间与三个引用工况**逐位一致**。"优 22×"与"压降优 609×"两句**同工况同行集**、可写进正文（须带工况名与限定）。【审计】的 0.0007–0.0014 只跑了 5 个工况，下限是覆盖不足所致（非冲突）。 |

**主战场该不该换到弯曲族：现在不换，但把它降级为"必做的第二个测量"而非"必做的替代项"。**理由：换战场的唯一依据是"弧段 PINN 优于解析"，而这条已被我的 3 参数曲率臂打成平手；同时弧段 PINN 在**压力**上比解析差 40 倍、出口直段差 4.2 倍。弯曲族值得报的是"**分区误差 + 准一维近似在何处失效**"，不是"PINN 在这里赢了"。

---

# C1 · R2 的理由错误

## 判决

**我错，方案线对。**【审计】第 0 屏 #2 与 A 节 #2 条目里写的"basic 分支**既没有壁面条件也没有压力锚定**"、"其压力侧 11.76 是配置产物（归因到臂）"，把三项权重的缺失算进了臂间差异；实为整批 strict 实验的共同配置。

## 我怎么验的（不是静态阅读，是实跑取 argv）

把脚本复制为 `.scratch/c1/traced.sh`（仅改 3 处：强制走训练分支、LOG_DIR 指向 .scratch、`nice`/`python3` 用桩替换成 echo），`bash traced.sh` → 44 条命令的完整 argv 落盘（`.scratch/c1/stubout.txt`），再逐 flag 比对（`.scratch/c1/diff_arms.py`）。**仓库文件未被写入，无任何真实训练/评估被启动。**

两臂（`contraction_strict_basic_sparse5_stagepde_20260503` vs `..._geometry_...`，同为 5% 观测）的全部 38 个 flag 中，只有 3 个不同，其中 1 个是 run-name：

| flag | basic | geometry | 是否独立变量 |
| --- | --- | --- | --- |
| `--feature-mode` | basic | geometry | **是（臂间变量①）** → `feature_cols` 4 ↔ 14 |
| `--velocity-wall-mode` | **soft** | **hard** | 由 `:29-32` 从 feature-mode 派生 → **臂间变量②**（硬包络有无） |
| `--drop-features` | 空串 | inlet_profile_star | 否：basic 的 4 列基特征本就不含 `inlet_profile_star`，实跑确认传的是空值（`--drop-features  -`） |
| `--wall-weight` | 0.0 | 0.0 | 相同 |
| `--inlet-flux-weight` | **0.0** | **0.0** | **相同 → 方案线对** |
| `--outlet-pressure-weight` | **0.0** | **0.0** | **相同 → 方案线对** |
| `--pressure-drop-weight` | **0.0** | **0.0** | **相同 → 方案线对** |
| epochs / lr / 连续性 / 动量 / 耦合权重 / `--max-physics-points` / `--strict-sparse-scalers` / 观测源 / 隐层 | 逐项相同 | 逐项相同 | — |

整批 15 个训练命令的三项权重取值集合均为 `{0.0}`，无例外。

保留成立的那半句："basic 臂**无任何无滑移执行机制**"——我自己查了实现：`train_velocity_pressure_independent_strict_sparse.py:221-225` 的包络只在 `mode=="hard"` 时乘，且 `:603`、`:731` 的壁面损失乘以 `args.wall_weight = 0.0`。soft 臂因此确实零边界执行。

## 口径声明

- 只读；对脚本的改动只存在于 `.scratch/c1/` 下的副本；`git status` 无仓库改动。
- 比的是**训练入口 argv**，不是 config 事后字段；两者此前已一致（basic：4 特征 + soft；geometry：14 特征 + hard）。

## 与方案线读数的差

零。方案线"59–66 行在公共体内、不分支"的表述与实跑 argv 完全一致。

## 对我自己报告的影响（用户关心项）

| 【审计】位置 | 原判决 | 更正后 | 严重度变化 |
| --- | --- | --- | --- |
| 第 0 屏 **#2**（5.6.2 消融 0.5612→0.0390 / 11.7598→0.1516） | 【撤数字，换实验】，理由="四/双变量 + basic 臂无壁面**也无压力锚定**" | 判决不变（**仍须撤这组数字**），理由改为：**2 个臂间变量且不可分**——特征 4↔14 与硬包络有无；且包络对速度和（经 `coupling-momentum-weight 10.0` 的动量残差）对压力**同时**起作用，所以 11.7598 与 0.5612 都不能归因给编码。仓库现成干净组给 0.5753→0.0308 与 0.2681→0.1843（31.2%） | **实质不变，措辞必须改**；但"无压力锚定"这句要删——它会在答辩上被人一眼挑软。另需把三项权重=0 这条**并入 #6（表4-4 串档）**，因为它是批次级问题 |
| 第 0 屏 **#11**（4.5"只改变一个主要因素"） | 【撤】 | **维持【撤】**：消融对 2 个变量、图5-18 对 4 个（参数量 ×1.99 / 每网络步数 ×0.24 / 选点规则 / 有无包络），2 也 ≠ 1 | 不降 |
| A 节 #2、#24 行的理由文字 | 同上 | 同步删"无压力锚定"字样 | — |
| 第 0 屏 #6（表4-4 与 strict 批次） | 【改：配置串档】 | **强度上升**：现在能证明是"整批 15 个 run 的公共体写死 0"，而非某臂特例 → 表4-4b 必须补，且正文凡引 5% 表处都要注明"压降/出口压力/入口流量三项约束在该批实验中未参与训练" | ↑ |
| 【审计】里我引用 11.7598 时未声明汇总口径 | — | 补一条：该值是 **mean-of-cases**；同一对 run 的 **pooled** 值为 speed 0.5684 / 0.0471、p **9.0377 / 0.2585**（本轮算）。按 pooled 口径"压力改善"从 77× 降为 35× | 新增，属我自己的口径漏洞 |

---

# C2 · 弯弧段上 PINN 是否优于解析式（能否成为新主战场）

## 判决

- **读数：成立。**弧段解析 0.0357、双模型 0.0159（5%）/0.0162（1%）、出口直段 0.0023 vs 0.0095、弧段压力 0.0010 vs 0.0380 —— 我用独立几何实现全部复现，**与方案线零偏差**。
- **推论（"不升 Re 也有可辩护净增量 ⇒ 换主战场"）：口径不够，重做后可定。**缺的不是我的实现细节，是**基线强度**与**种子重复**：我补了一个同样零训练、零 PDE、零 CFD 的 3 参数曲率剖面臂，它在弧段就把 2.2× 打平。

## 我的读数表（B-val__ip_blunted，interior 点，同一行集打分）

分段规则与点数占比（约束 #2、#4）：

| 段 | 定义（坐标规则） | interior 点数 | 占 interior |
| --- | --- | --- | --- |
| 入口直段 | `0≤x≤Lin=4` 且 `|y|≤0.5` | 2066 | 27.1% |
| **圆弧段** | 以圆心 (Lin, Rc)=(4, 3.5)、`θ∈[−90°, −90°+90°]`、`r∈[Rc−0.5, Rc+0.5]`；`eta = Rc − r` | **2556** | **33.5%** |
| 出口直段 | 以弧末端 `arc_end = 圆心 + Rc·(cosθ₁, sinθ₁)`，沿 `t_out=(cosα, sinα)` 投影 `0≤ξ≤6`、`|η|≤0.5` | 3011 | 39.4% |

优先级 = 入口→出口→弧（照 `bend_geometry.py:118-148` 的 `for seg_id,key in [(0,'in'),(2,'out'),(1,'arc')]` 顺序），容差 1e-3 同码。两案未分类点 = **0**。

速度模 rel-L2（`relative_l2` 逐字对齐，见下口径）：

| 臂 | 用了多少目标数据 | B-val 弧段 | B-val 入口 | B-val 出口 | B-test-1 弧段 |
| --- | --- | --- | --- | --- | --- |
| A 抛物 `1.5(1−(2η)²)` | 0（零参数） | **0.0357** | 0.0394 | **0.0023** | **0.0416** |
| A2 抛物 + 目标集上最优幅值（oracle，作弊上界） | 目标自身 1 参数 | 0.0357 | 0.0390 | 0.0023 | 0.0416 |
| B 入口 BC 实际剖面 `1.25(1−(2η)⁴)`（blunted） | 0 | 0.1457 | — | — | 0.1461 |
| **F 曲率剖面 `A(1−\|t\|^m)(1+S·t)`，参数只在 4 个训练工况的弧段上拟合** | **训练工况，不含目标** | **0.0133** | 0.0453 | 0.0224 | **0.0192** |
| G 25 桶剖面查找表（同样只拟合于训练弧段 + 目标幅值再拟合） | 训练工况（幅值用了目标） | 0.0137 | 0.0604 | 0.0400 | 0.0697 |
| 双模型 1%（76 观测点） | 76 点训练 | 0.0162 | 0.0384 | 0.0134 | 0.0189 |
| 双模型 5%（382 点） | 382 点 | **0.0159** | 0.0356 | 0.0095 | 0.0236 |
| 双模型 10% / 15% | 763 / 1145 点 | 0.0166 / 0.0160 | 0.0375 / 0.0358 | 0.0086 / 0.0085 | 0.0232 / 0.0211 |
| 压力 rel-L2（弧段） | — | 解析 **0.000956** vs 双模型 5% **0.038017**（解析优 39.8×） | | | B-test-1：解析 0.001176 vs 0.073123（优 62×） |

拟合出的 F 参数：`A=1.4997, m=2.00, S=+0.06`（训练弧段自身误差 0.0046）。**m 停在 2.00 = 幂次仍是抛物线，只有 6% 的一次反对称倾斜项起了作用**：0.0357 → 0.0133 全部由这一个参数换来。

## 口径声明（逐条对齐 5 条硬约束）

1. **指标定义**：`relative_l2(pred,truth) = ‖pred−truth‖ /(‖truth‖+1e-12)`，逐字照 `train_supervised.py:361-362`；汇总层照 `compute_case_metrics`（`:622-663`）与 `evaluate_velocity_pressure_independent.py:55-56`。**本报告主表用"同一行集上的 pooled"（单工况内池化）**；跨工况我另给 mean-of-cases 并注明（两者不同，方案线 M3 的说法成立，我上面的 C1 补漏即为例）。`speed = hypot(u,v)` 与 `:632-633` 同。
2. **同点集**：解析臂与 PINN 臂在**完全相同的行索引集合**上打分（同一 interior 集合 `boundary_type=='interior'`、同一分段判定）。对齐已验证：预测 CSV 与 `field_dense.csv` 逐行同 `x_star/y_star/u_star`，B-val 8385 行、B-test-1 8015 行，**错配 0 行**；且我在 ALL 行集上算出的双模型 5% 值 = **0.022786**，与仓库 `metrics_val_dense.json#summary.mean_rel_l2_speed = 0.0227858` 一致 → 行序假设被产物反证。
3. **等预算**：弯曲 interior 7633 → 观测点 1/5/10/15% = **76 / 382 / 763 / 1145**；收缩 C-val interior 1261 → **13 / 63 / 126 / 189**。两族"5%"绝对预算差 **6.06×**（方案线说 6×，一致）。我的处理：**不重训**，改用批次内已有的 1% 臂做预算匹配——弯曲 1%=76 点 ≈ 收缩 5%=63 点。结果：弯曲弧段在 76 点时 PINN 0.0162 vs 解析 0.0357（**PINN 优 2.20×**），而收缩在 63 点时 PINN interior 0.0314 vs 解析 0.000687（**解析优 45.6×**）→ **同一绝对预算下方向相反，"6 倍预算"不能解释这条翻转**。且弧段 PINN 误差在 76→1145 点间是平的（0.0162→0.0160），说明它不是观测受限、而是模型自己的误差地板。
4. **占比**：弧段 = interior 的 33.5%（B-val）/ 30.6%（B-test-1）。全场平均会把弧段的 2.2× 摊平到 interior 级 1.35×（5%）/1.23×（1%）→ 主战场若要成立，**必须分区报**，不能报全场。
5. **误差构成诊断**：弧段解析误差是**形状**不是幅值。两路独立证据：① 全局最优幅值 `α*=1.0012`，拟合后误差 0.035679（vs 0.0357，几乎不动）；② 按 η 分箱的弧段平均剖面（B-val）：η=−0.24 → 1.085、η=+0.24 → 1.167、η=−0.36 → 0.731、η=+0.36 → 0.687、η=0 → 1.494 —— 抛物线在中心 1.500 几乎正好（幅值对），两侧不对称（**剖面被曲率推歪**）。所以我实现下"幅值有偏"的嫌疑排除；PINN 捕捉到的确实是这个歪斜。
   → **但正因如此**，第 5 条反而支持我的 F 臂：一个只加 6% 一次倾斜项的零训练式子同样捕捉到了它（0.0133 < 0.0159）。

## 与方案线读数的差

| 量 | 方案线 | 我 | 差 |
| --- | --- | --- | --- |
| 弧段解析 speed（B-val） | 0.0357 | 0.035700 | 0 |
| 弧段双模型 5% / 1% | 0.0159 / 0.0162 | 0.015914 / 0.016175 | 0 / 0 |
| 出口直段 解析 vs PINN | 0.0023 / 0.0095 | 0.002251 / 0.009493 | 0 |
| 弧段压力 解析 vs PINN | 0.0010 / 0.0380 | 0.000956 / 0.038017 | 0 |
| 弯曲 interior / 5% 点数 | 7633 / 382 | 7633 / 382 | 0 |

**方案线没报、我补的**：B-test-1 弧段（解析 0.0416 / PINN5% 0.0236 / PINN1% 0.0189）、PINN 10%/15% 两档、幅值-形状分解、F/G 两个零训练强基线。

## 判决依据与"如果不可用，缺什么"

结论"**弧段上 PINN 优于解析式 2.2×**"作为**相对于抛物线基线**的读数可用；作为"**不升 Re 也有可辩护净增量 ⇒ 换主战场**"的论据**不可用**。要把它变成可用，缺 4 件（都在 6 天窗口内、成本从低到高）：

1. **把 F/G 这类"曲率修正剖面"正式纳入基线三件套**（§6.3 现在只有插值/POD/解析三臂，解析臂只写了抛物线版）。要求：拟合预算与 PINN 对齐（F 只用 4 个训练工况、零观测点；若 PINN 用 382 个观测点，必须也报 F 在 0 观测下的数），并注明 m 拟合结果停在 2.00、只有倾斜项在起作用。
2. **多种子**：2.2× 建立在 n=1。F vs PINN 的差是 0.0133 vs 0.0159（17%），与【审计】E 节已示的单次读数抖动同量级；没有 mean±std 就不能写"优于"。
3. **把弧段压力这条一起报**：弧段上解析优 40×（B-test-1 优 62×），而压力/压降正是筛选用量 → 单报速度会构成选择性报告。
4. **θ=60° 的 B-test-2 补评估**（【审计】B.4：该工况真值在、评估从不在）。曲率段的声称要靠"变曲率 + 变转角"两个方向都成立，现在只有 Rc 一个方向、且只有 2 个未见工况。

**建议改写（可直接用）**："在弯曲流道的圆弧段，准一维抛物剖面假设失效：零参数解析式速度误差 0.0357（B-val）/0.0416（B-test-1），而 5% 观测的双模型为 0.0159/0.0236；误差来源经分箱诊断确认为剖面形状倾斜而非幅值。允许一个反对称倾斜项（+6%）的零参数训练基线可将弧段误差降至 0.0133/0.0192，与模型同量级，故本节**不主张**模型在弧段的精度优势，仅以此说明弧段是该几何族中唯一非准一维的部分，且模型在该段的压力误差（0.038）显著大于解析式（0.001）。"

---

# C3 · 解析式在收缩族的量级

## 判决

**成立。**且【审计】给出的区间偏窄（我只跑了 5 个工况），全量 9 工况下限为 0.00019。

## 我的读数表（9 工况，全部行集，`relative_l2` 照仓库定义；p 出口参考=0 与真值同）

| 工况 | β | Lc/W | n 行 | u | speed | p | dp 真值 | dp 解析 | dp 相对误差 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C-base | 0.70 | 4.0 | 2113 | 0.000761 | 0.000737 | 0.000674 | 415.06 | 414.87 | 0.000456 |
| C-train-1 | 0.85 | 4.0 | 2339 | 0.000370 | 0.000367 | 0.000356 | 266.38 | 266.34 | 0.000133 |
| C-train-2 | 0.70 | 4.0 | 2113 | 0.000761 | 0.000737 | 0.000674 | 415.06 | 414.87 | 0.000456 |
| C-train-3 | 0.50 | 4.0 | 1843 | 0.001414 | 0.001321 | 0.000942 | 985.25 | 984.41 | 0.000855 |
| C-train-4 | 0.85 | 6.0 | 2176 | 0.000190 | 0.000189 | 0.000243 | 297.38 | 297.35 | 0.000078 |
| C-train-5 | 0.70 | 6.0 | 1914 | 0.000398 | 0.000385 | 0.000448 | 458.49 | 458.36 | 0.000272 |
| C-val | 0.50 | 6.0 | 1689 | 0.000719 | 0.000672 | 0.000604 | 1069.17 | 1068.62 | 0.000519 |
| C-test-1 | 0.60 | 5.0 | 1900 | 0.000758 | 0.000722 | 0.000666 | 638.35 | 638.02 | 0.000521 |
| C-test-2 | 0.40 | 6.0 | 1545 | 0.000922 | 0.000856 | 0.000590 | 1957.87 | 1956.80 | 0.000547 |

区间：**u 0.000190–0.001414 | speed 0.000189–0.001321 | p 0.000243–0.000942 | dp 0.000078–0.000855**；mean-of-cases：speed 0.000665、p 0.000577、dp 0.000426。

## 口径声明（含用户点名的两个坑）

1. **W(x) 与真值同源**：我用 `model/src/data/contraction_geometry.py:22-49` 的同一条 `smoothstep5` 与同一 `half_width` 分支（`x≤Lin → 0.5`；`x≥Lin+Lc → 0.5β`；中间 `0.5(1−(1−β)·smoothstep5(s))`），β/Lin/Lc 直接读该工况 `meta.json`。不存在"我自己拟合几何参数"→ 几何偏差项 = 0。压力用 `p(x)=∫ₓ^L 12/W(s)³ ds`（40001 点梯形积分），出口参考压 p(L)=0，与真值 `.edp` 的 `on(labOut, p=0)` 同锚。
2. **入口 BC 同式**：解析式的 `1.5(1−η²)` 与 FreeFEM 脚本里 `uIn(yy)=1.5(1−(2yy)²)` 字面相同（`contraction_freefem.py:80-83` 生成的 `.edp`）→ 解析式与真值共享边界条件，其残差只来自"局部平行流"这一近似，不是我把 BC 记错。
3. **指标与汇总**：`relative_l2` 同 `:361-362`；上表为**单工况内池化**；mean-of-cases 另给；两族 5% 的绝对点数（63 vs 382）已在 C2 口径 3 落盘。
4. **同点集对照**：PINN 臂我另在 C-val interior（1261 行、63 观测点、同一行集）复算：双模型 5% speed 0.031355 / p 0.068544；单网络 5% speed 0.013654 / p 0.062349；解析同点集 speed 0.000687 / p 0.000527。

## 与方案线读数的差

三个被论文引用的工况逐位相同：C-val 0.000672/0.000604/0.000519；C-test-1 0.000722/0.000666/0.000521；C-test-2 0.000856/0.000590/0.000547。dp 真值→解析：415.06→414.87、1069.17→1068.62、1957.87→1956.80 —— 三对全同。**差 = 0**。
与我自己【审计】的差：【审计】写 "u 0.0007–0.0014 / p 0.0006–0.0009 / dp 0.0005–0.0009"，系只取 5 个工况（C-base/C-val/C-test-1/C-test-2/C-train-3），未覆盖温和的 C-train-1/4/5（0.00019–0.00045）。**下限应改为 0.00019；上限不变。**

## 两句能否写进正文

| 句子 | 判决 | 依据与必须附带的限定 |
| --- | --- | --- |
| "比论文最好读数 0.0162 优 22×" | **可以写**，但只能限定在收缩族、速度模、同一工况 | 0.0162 与解析 0.000722 都在 C-test-1 的同一 1900 行点集上算（22.4×）。同族同量的另两行：C-val 32.9×、C-test-2 46.4×；若换成 5% 稀疏读数则更大（对双模型 interior 0.031355/0.000687 = 45.6×）。**必须带"收缩流道（等宽直段+平滑收缩，Re=0.0224）"与"不含弯曲弧段"**——弯曲弧段方向相反（见 C2），这就是方案线判我"外推越界"的那一条，我接受 |
| "压降 0.3331 优 609×" | **可以写**，同样限定 | 0.3331 是论文表5-3 C-test-2 行；解析在同工况 dp 误差 0.000547 → 608.9×。建议同时给绝对量：0.3331 对应 **0.29 Pa**（p 尺度 μU/W = 4.451e-4 Pa/单位，真值压降 0.87 Pa），否则读者不知道 33% 是不是要紧 |

## 不可用之处：无。但两处必须补进正文/表格的限定

1. 解析臂在**弯曲族弧段不成立**（C2），所以任何"全文级"的"解析优 22–609×"都须降级为"收缩族（及弯曲族的直段）"。
2. 解析臂对压力/压降的优势在弯曲族**同样成立**（弧段优 40–62×，出口直段优 4.2–4.4×），因此 §9 情形 3（"若五列 PINN 全败则停止美化"）的触发条件在弯曲族也部分成立：**不是全败，是速度一列小胜、压力一列大败**。

---

# 对我自己上一份报告的更正清单（4 条，需落到【审计】文本）

1. **第 0 屏 #2 / A#2 / A#24**：删去"basic 臂无压力锚定"及"四变量"字样；改为"臂间变量 = 特征集 + 硬包络（后者由前者派生），三项权重为整批 strict 共同配置（并入 #6）"。判决不变。
2. **第 0 屏 #6 / H.2**：升级表述——已实跑证明整批 15 个训练命令的 `--inlet-flux-weight/--outlet-pressure-weight/--pressure-drop-weight` 恒为 0.0，无例外；表4-4b 必补。
3. **R3 的外推范围（B.3、D.3、第 0 屏 #10、G 第一问第 2 条）**：把"解析式支配"限定为**收缩族与弯曲族的直段**；弯曲弧段方向相反（解析 0.0357 vs 模型 0.0159）。我在 G 里写的"零训练解析式在收缩族上 u/p 误差 0.0007–0.0014，比论文最好读数准 20–150×"这句本身限定了收缩族，可保留，但下限改 0.00019、且必须补一句"弯曲弧段不适用；且一个含 6% 倾斜项的三参数式在弧段与模型同量级"。
4. **口径披露**：凡"两工况平均"处须写明 mean-of-cases 还是 pooled。我报告里 0.5612 / 11.7598 是 mean-of-cases；对应 pooled 为 0.5684 / **9.0377**（几何臂 0.0471 / 0.2585）。按 pooled 口径，"压力改善 77×"应写成 35×。

---

# 附录 · 本轮自写脚本（全部只读，纯 stdlib，无 numpy/torch）

| 文件 | 用途 |
| --- | --- |
| `.scratch/c1/traced.sh` + `stub/{nice,python3}` + `stubout.txt` + `diff_arms.py` | 桩化实跑取 44 条命令 argv；逐 flag 比对两臂与全批权重 |
| `.scratch/c2_bend_arc.py` | 弧段/直段分段（照 `bend_geometry.py:62-108` 规则重写）、解析 vs 双模型同点集打分、幅值 oracle |
| `.scratch/c2b_shape_transfer.py` | 剖面形状分箱查找臂（只用 4 个训练工况弧段）+ 观测到的弧段剖面表 |
| `.scratch/c2c_arms.py` | 多基线臂 A/A2/B/F/G × PINN 1/5/10/15%，两案三段 |
| `.scratch/c3_contraction_analytic.py` | 9 工况 u/speed/p/dp 解析对比 + 与论文读数的比值 + 两族观测点数 |
| `.scratch/c2c3_addendum.py` | 行序对齐断言（错配 0）+ interior 同点集三方对比 + 预算翻转测试 |

未核实项（不猜）：PINN 弧段优势在多种子下的稳定性；θ=60° 工况的弧段行为（仓库无评估）；F 臂在更多曲率档（Rc=3.0/3.5 之外）的传递性。
