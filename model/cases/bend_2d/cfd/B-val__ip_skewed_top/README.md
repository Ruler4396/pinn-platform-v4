# B-val__ip_skewed_top CFD artifacts

本目录已生成 `B-val__ip_skewed_top` 的 FreeFEM++ CFD 数据：

- `B-val__ip_skewed_top_stokes.edp`：自动生成的稳态 Stokes 求解脚本
- `B-val__ip_skewed_top_raw.csv`：FreeFEM++ 原始导出结果

说明：

- 当前解为 dimensionless steady Stokes baseline
- 入口剖面：`skewed_top`
- 后处理结果已同步写入 data 目录的 `field_dense.csv`
