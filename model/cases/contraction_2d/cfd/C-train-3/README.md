# C-train-3 CFD artifacts

本目录已生成 `C-train-3` 的 FreeFEM++ CFD 数据：

- `C-train-3_stokes.edp`：自动生成的稳态 Stokes 求解脚本
- `C-train-3_raw.csv`：FreeFEM++ 原始导出结果

说明：

- 当前解为 dimensionless steady Stokes baseline
- 后处理结果已同步写入 data 目录的 `field_dense.csv`
