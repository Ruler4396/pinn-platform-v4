# C-val CFD artifacts

本目录已生成 `C-val` 的 FreeFEM++ CFD 数据：

- `C-val_stokes.edp`：自动生成的稳态 Stokes 求解脚本
- `C-val_raw.csv`：FreeFEM++ 原始导出结果

说明：

- 当前解为 dimensionless steady Stokes baseline
- 后处理结果已同步写入 data 目录的 `field_dense.csv`
